ID: STA-20260307-004
FECHA: 2026-03-07
PROYECTO: pronto-static
SEVERIDAD: alta
TITULO: arbol node_modules aparece como residuo versionable por gap en gitignore
DESCRIPCION:
  El repositorio mostraba `node_modules/` con cambios tracked/untracked masivos y la regla de ignore
  para `node_modules/` estaba comentada en `.gitignore`.
PASOS_REPRODUCIR:
  1. Ejecutar `git status` en `pronto-static`.
  2. Revisar `.gitignore` y comprobar que `node_modules/` estaba comentado.
RESULTADO_ACTUAL:
  `node_modules/` queda ignorado por `.gitignore` y fue removido del index con `git rm --cached -r node_modules` sin borrar la instalación local.
RESULTADO_ESPERADO:
  `node_modules/` debe ignorarse para evitar residuos de entorno local.
UBICACION:
  - pronto-static/.gitignore
  - pronto-static/node_modules/
EVIDENCIA:
  - `.gitignore` ahora contiene `node_modules/`
  - `git -C pronto-static rm -r --cached node_modules` ejecutado correctamente
HIPOTESIS_CAUSA:
  Configuracion temporal para builds offline que quedo activa en rama de desarrollo.
ESTADO: RESUELTO
SOLUCION:
  Se activó la regla `node_modules/` en `.gitignore` y se destrackeó el árbol completo desde Git sin eliminar los archivos del entorno local.
COMMIT: NO_COMMIT_LOCAL
FECHA_RESOLUCION: 2026-03-07