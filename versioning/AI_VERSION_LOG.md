# AI Version Log

Registro incremental obligatorio de cambios aplicados por agentes AI.

| FECHA | VERSION_ANTERIOR | VERSION_NUEVA | AGENTE | MODULOS | RESUMEN |
|---|---|---|---|---|---|
| 2026-03-08 | 1.0443 | 1.0444 | Augment Agent | pronto-client,pronto-api,pronto-libs,pronto-tests,pronto-docs,pronto-scripts/pronto-root | Corrección documentada de flujos cliente: BFF de órdenes ahora es proxy transparente, se arreglaron `customer/orders` y `request-check`, la validación de modifiers requeridos devuelve 400 en vez de 500, y quedaron verdes el e2e cliente y el Playwright de runtime/auth gate. |
| 2026-03-07 | 1.0436 | 1.0437 | Augment Agent | pronto-tests,pronto-docs,pronto-scripts/pronto-root | Exclusión segura del smoke script manual `pronto-tests/scripts/test_multi_console_auth.py` de la suite automatizada de pytest y validación completa de `pytest pronto-tests -q` en verde (`261 passed, 5 skipped`). |
| 2026-03-07 | 1.0435 | 1.0436 | Augment Agent | pronto-client,pronto-tests,pronto-docs,pronto-scripts/pronto-root | Corrección y validación completa de la suite funcional: BFF cliente alineado con `client-auth`, passthrough de `X-CSRFToken`/cookies/`Set-Cookie`, rutas de `sessions` y `feedback` corregidas, fixtures/tests stale actualizados, y rerun completo de `pronto-tests/tests/functionality` en verde (`216 passed, 3 skipped`). |
| 2026-03-07 | 1.0434 | 1.0435 | Augment Agent | pronto-static,pronto-client,pronto-docs,pronto-scripts/pronto-root | Corrección de drift de API parity en employees y clients: frontend employees alineado a `kitchen-start`, `kitchen-ready` y checkout por sesión; frontend cliente migrado a rutas canónicas `client-auth`, `customer/payments`, `customer/orders` y `customers/waiter-calls`. |
| 2026-03-07 | 1.0433 | 1.0434 | Augment Agent | pronto-employees,pronto-libs,pronto-static,pronto-client,pronto-tests,pronto-docs,pronto-scripts/pronto-root | Cierre del lote de bugs abiertos: `pronto-employees` quedó proxy-only para `/{scope}/api/*`, se eliminaron suites auth/JWT duplicadas en `pronto-tests`, se retiró el árbol estático legacy de `pronto-libs`, y `bindings.js`/`keyboard-shortcuts.js` fueron reemplazados por inicialización TS desde `pronto-static`. |
| 2026-03-07 | 1.0432 | 1.0433 | Augment Agent | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Corrección de autoridad de estados en pronto-libs: `order_service_impl` ahora usa `mark_order_awaiting_tip(...)` en vez de escribir `payment_status` directamente; `waiter-board` eliminó mutaciones hardcodeadas de `workflow_status/payment_status` y añadió regresiones unitarias para prevenir reintroducción. |
| 2026-03-07 | 1.0431 | 1.0432 | Augment Agent | pronto-api, pronto-employees, pronto-libs, pronto-static, pronto-docs, pronto-scripts/pronto-root | Corrección de bugs documentados: webhook Facturapi ahora valida firma cuando hay secreto y usa logging estructurado; sunset explícito en proxies deprecated de employees; eliminación de `fix_schema.py` ligado a `legacy_mysql`; limpieza/untracking de artefactos temporales y `node_modules` en `pronto-static`; actualización de tracker de errores. |
| 2026-03-07 | 1.0402 | 1.0403 | opencode | pronto-client, pronto-api, pronto-docs, pronto-scripts/pronto-root | Corrección arquitectónica P0: migración de endpoints de negocio desde pronto-client a pronto-api (split_bills, stripe_payments, waiter_calls, notifications, feedback_email); pronto-client ahora solo renderiza SSR/UI; documentación de error PRONTO-CLIENT-001 y cierre en tracker. |
| 2026-03-07 | 1.0403 | 1.0403 | opencode | pronto-client, pronto-api, pronto-docs, pronto-scripts/pronto-root | Corrección arquitectónica P0: headers DEPRECATED agregados a 17 archivos en pronto-client, plan de retiro detallado con 10 tickets de migración, documentación de migración en pronto-docs/migration-plans/ y pronto-docs/migration-tickets/, corrección de versión canónica. |
| 2026-02-13 | 1.0002 | 1.0003 | Codex | pronto-employees, pronto-client, pronto-static, pronto-api, pronto-libs, pronto-scripts | Corrección real de endpoints faltantes en runtime para clientes/empleados, validación de contenedores healthy, y consolidación de control de versión en .env/AGENTS. |
| 2026-02-13 | 1.0003 | 1.0004 | Codex | pronto-scripts | Corrección real de `rebuild`/sync estático: detección robusta del contenedor static y fix de typo `employee`->`employees` para evitar despliegues con assets desactualizados. |
| 2026-02-13 | 1.0004 | 1.0005 | Codex | pronto-static, pronto-docs, pronto-scripts | Corrección de flujo de menú cliente para IDs UUID (sin NaN), fallback robusto de placeholder de imágenes y normalización de tipado de carrito; registro y cierre formal del bug en tracker de errores. |
| 2026-02-13 | 1.0005 | 1.0006 | Codex | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Alineacion del set funcional Playwright a contratos/rutas vigentes (auth CSRF, menu API data.categories, empleados/login), eliminacion de pruebas legacy fragiles y validacion final en verde (`npm run test:functionality`: 35/35). |
| 2026-02-13 | 1.0006 | 1.0007 | Codex | pronto-api, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Fix CORS preflight para checkout cliente (`X-CSRFToken` permitido en `Access-Control-Allow-Headers`), rebuild de servicios y validación iterativa hasta suite funcional completa en verde (`35/35`). |
| 2026-02-13 | 1.0007 | 1.0008 | Codex | pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección de loop de sesión en login employees (401 en login ya no fuerza redirect), estabilización visual del login en modo invitado y prueba de regresión E2E para asegurar que no aparezca sidebar en `/employees/login`; suite funcional completa en verde (`36/36`). |
| 2026-02-13 | 1.0008 | 1.0009 | Codex | pronto-static, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Normalización visual de logins de empleados por rol (escala/layout uniforme) y corrección de ubicación de versión para mostrarla fija en viewport sin scroll. |
| 2026-02-13 | 1.0009 | 1.0010 | Codex | pronto-static, pronto-tests, pronto-scripts/pronto-root | Modularización del bundle employees: bootstrap separado, shell/components en import asíncrono, carga diferida de store de órdenes y chunking dedicado (router-vendor, pinia-vendor, employees-core, employees-store) para reducir main.js y mantener login/flujo estable. |
| 2026-02-13 | 1.0010 | 1.0011 | Codex | pronto-static, pronto-scripts/pronto-root | Fix final de login employees: eliminación del throw sintético de Sesión expirada en wrapper HTTP para preservar mensaje real del backend en login (Credenciales inválidas), rebuild de assets y reinicio de static. |
| 2026-02-13 | 1.0011 | 1.0012 | Codex | pronto-static, pronto-scripts/pronto-root | Corrección de login shell employees: reemplazo de dependencia global window.requestJSON por import tipado de core/http y omisión de fetchUser en rutas guest para eliminar errores 401/console noise al abrir login. |
| 2026-02-13 | 1.0012 | 1.0013 | Codex | pronto-libs, pronto-employees, pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Fix de login empleados por roles: `has_scope` ahora considera rol principal/jerarquía canónica, serialización UUID en JWT/JSON de auth, y ajuste de frontend para no redirigir en 403 dentro de login; validación completa con suite funcional en verde (`36/36`) y prueba dedicada de login por 5 roles (`5/5`). |
| 2026-02-13 | 1.0013 | 1.0014 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Corrección de `validate-seed.sh`: detección robusta de contenedor employees, rutas absolutas y fail-fast real (sin falso positivo de éxito); documentación de error resuelto y apertura de incidente por desalineación modelo/esquema de `pronto_areas` que rompe seed ORM. |
| 2026-02-13 | 1.0014 | 1.0015 | Codex | pronto-employees, pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix de 404 en employees para `/authorization-error` y `/favicon.ico`, y robustez de detección de login path en interceptor HTTP para evitar redirección errónea desde login; validado con Playwright y checks HTTP en 6081. |
| 2026-02-13 | 1.0015 | 1.0016 | Codex | pronto-static, pronto-employees, pronto-scripts/pronto-root | Corrección visual y de flujo en login employees: hardening del shell (Header/Sidebar/App/Dashboard), carga estática de `LoginPage` para eliminar mezcla de estado en login, compatibilidad de assets legacy en build de static y proxy server-side de `/assets/*` en employees para evitar fallos de chunks/CSS cross-origin. |
| 2026-02-13 | 1.0016 | 1.0017 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix de placeholder en menú cliente: el fallback ya no usa la ruta de restaurante para placeholder inexistente y ahora aplica el placeholder global canónico para eliminar 404/OpaqueResponseBlocking. |
| 2026-02-13 | 1.0017 | 1.0018 | Codex | pronto-static, pronto-employees, pronto-api, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Estabilización de login employees y flujos de módulos: hardening de detección JSON en auth por rol, proxy/aliases de assets legacy, tolerancia a `vite:preloadError`, rutas explícitas para chunks legacy, y resiliencia en notificaciones waiter para evitar 500 cuando falta `pronto_waiter_calls`; migraciones/init-check ejecutados en verde. |
| 2026-02-13 | 1.0018 | 1.0019 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Restricción de navegación por rol en employees: mesero limitado a Operación Mesero + Productos + Aditamientos + Mesas + Áreas; chef/cashier limitados a su módulo; guard de rutas actualizado para bloquear acceso por URL a módulos no permitidos. |
| 2026-02-13 | 1.0019 | 1.0020 | Codex | pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Endurecimiento final por scope activo: detección de contexto por ruta (`/waiter|/chef|/cashier`) + `APP_CONTEXT` + `active_scope` en App/Router para ocultar y bloquear módulos ADMIN en contexto waiter; compilación employees y pruebas Playwright (auth/orders/login flows) en verde. |
| 2026-02-13 | 1.0020 | 1.0021 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Corrección visual de barra superior employees: estilos críticos del `Header` reforzados inline (altura, avatar, alineación y tipografía) y shell principal con imports estáticos para evitar render roto por carga diferida de estilos/chunks. |
| 2026-02-13 | 1.0021 | 1.0022 | Codex | pronto-static, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Fix de despliegue static: Dockerfile ahora copia `pronto-static/scripts` para incluir `ensure-employees-legacy-assets.mjs`; rebuild exitoso de `static` + `employees`, assets actualizados en runtime (`main.js?v=1.0021`), y cierre formal de incidente en error tracker. |
| 2026-02-13 | 1.0022 | 1.0023 | Codex | pronto-employees, pronto-static, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Reubicación de versión en UI empleados: eliminación del badge flotante inferior, exposición de `system_version` en `APP_CONFIG` SSR y render de versión en footer del sidebar; rebuild de `static` + `employees` y validación visual en `http://localhost:6081/waiter/dashboard`. |
| 2026-02-13 | 1.0023 | 1.0024 | Codex | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Endurecimiento de pruebas de autenticación: `auth.spec.ts` y `test_login_flows.spec.ts` ahora realizan login real por POST con CSRF y credenciales seed; cualquier `401/403` de auth falla el test explícitamente, evitando falsos verdes de “ruta disponible”. |
| 2026-02-13 | 1.0024 | 1.0025 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Corrección de enrutamiento API en frontend clientes: `/api/*` vuelve a resolverse por host relativo (no forzado a 6082), eliminando preflight 404 en `POST /api/call-waiter`; se documenta reapertura y cierre formal en error tracker. |
| 2026-02-13 | 1.0025 | 1.0026 | Codex | pronto-static, pronto-employees, pronto-tests, pronto-scripts/pronto-root | Estabilización de login empleados: anti-caché en HTML/CSS versionados, corrección de tabs por rol en `DashboardView` (mesero sin módulos admin), y actualización de pruebas auth para credenciales seed actuales + endpoint canónico `API_URL` en validación de `/api/employees/auth/me`. |
| 2026-02-13 | 1.0026 | 1.0027 | Codex | pronto-static, pronto-scripts/pronto-root | Eliminada compatibilidad legacy de assets en build de empleados: se removió `node scripts/ensure-employees-legacy-assets.mjs` de `build:employees` y se eliminó el archivo `scripts/ensure-employees-legacy-assets.mjs`. |
| 2026-02-13 | 1.0027 | 1.0028 | Codex | pronto-static, pronto-scripts/pronto-root | Modularización real del bundle de empleados: rutas de `router/index.ts` convertidas a lazy imports para code-splitting por vista; `main.js` se redujo de ~82KB a ~20KB y los módulos quedaron en chunks independientes. |
| 2026-02-13 | 1.0028 | 1.0029 | Codex | pronto-tests, pronto-scripts/pronto-root | Endurecimiento anti-smoke en pruebas funcionales: se reemplazaron suites smoke de UI (`menu`, `orders`, `qa-complete`, `qa-cycle`) por flujos reales con login+CSRF+API/capacidades por rol, se eliminaron tests demo (`test_example.py`) y se reforzaron contratos SSR/login en `test_login_flows.spec.ts`. |
| 2026-02-13 | 1.0028 | 1.0029 | Codex | pronto-employees, pronto-docs, pronto-scripts/pronto-root | Corrección de carga cross-origin en SPA empleados: assets SSR cambian a rutas same-origin (`/assets/*`) para usar passthrough interno de employees y eliminar fallos CORS al cargar `main.js` y `chart.umd.min.js`; se registra incidente en error tracker. |
| 2026-02-13 | 1.0029 | 1.0030 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Restaurado registro de API en `pronto-client` (`api_bp` bajo `/api`) para resolver 404 en `POST /api/call-waiter` en host 6080; validación HTTP confirma `OPTIONS 200` y `POST` deja de responder 404. |
| 2026-02-13 | 1.0030 | 1.0031 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Hardening de consola employees: supresión de warning espurio en polling realtime ante fallos de red transitorios, normalización de `restaurant_assets` legacy para evitar 404 de logo (`/static/images/...`), y corrección de selectores CSS inválidos (llave huérfana + reglas scrollbar hover incompatibles). |
| 2026-02-14 | 1.0031 | 1.0032 | Codex | pronto-libs, pronto-client, pronto-api, pronto-employees, pronto-docs | Refactor completo de autenticación de clientes: Redis-backed customer_ref (TTL 60m), decorator @require_customer_session, endpoints web auth (/api/login, /api/register, /api/logout, /api/me), proxy orders con header X-PRONTO-CUSTOMER-REF, soporte para kind='kiosk', endpoints de gestión de kiosks, eliminación de client_auth.py legacy. |
| 2026-02-14 | 1.0032 | 1.0033 | Amp | pronto-client, pronto-docs | Fix BUG-20260214-003: /api/me ahora verifica is_revoked() antes de retornar datos. Fix BUG-20260214-004: auth_bp convertido a Blueprint propio, eliminado doble import y alias directo a api_bp. session.clear() añadido en login/logout. |
| 2026-02-14 | 1.0033 | 1.0038 | Amp | pronto-client, pronto-libs | Corrección de 6 bugs de seguridad en autenticación de clientes: BUG-001: notifications.py migrado de JWT a customer_ref/Redis auth + ownership check; BUG-002: Password hashing migrado de SHA256+pepper a PBKDF2 (werkzeug) con auto-rehash; BUG-003: /api/me ahora verifica revocación antes de retornar datos; BUG-004: auth_bp convertido a sub-blueprint propio, eliminado doble import; BUG-005: waiter_calls.py protegido con customer_ref auth guard; BUG-006: split_bills.py protegido con customer_ref auth guard en todos los endpoints. |
| 2026-02-14 | 1.0038 | 1.0039 | Amp | pronto-client | BUG-007: Corregido name shadowing en waiter_calls.py donde `with get_session() as session:` sobreescribía flask.session, causando UnboundLocalError en los auth guards recién agregados. Renombrado a `db_session` en get_waiter_call_status y get_waiter_call_status_alt. |
| 2026-02-15 | 1.0039 | 1.0040 | opencode | docker-compose, pronto-libs, pronto-api, pronto-client, pronto-docs | Corrección de 10 bugs de seguridad: SEC-001: CSRF client (falso positivo); SEC-002: API expuesta (puerto 6082 solo dev); SEC-003: HMAC auth (firma BFF->API); SEC-004: JWT iss/aud validation; SEC-005: JWT key separada; SEC-006: Refresh rotation; SEC-007: PII en JWT (include_pii=False); SEC-008: Redis password required; SEC-009: Rate limiting login; SEC-010: Email logging masking. |
| 2026-02-15 | 1.0040 | 1.0041 | Gemini CLI | pronto-api, pronto-employees, pronto-libs, pronto-static, pronto-docs, pronto-scripts | Auditoría integral y saneamiento: implementados +15 endpoints (branding IA, analytics reales, stats públicas, CRUD promociones, debug flows), corregido hash de agentes, eliminada fuga de estáticos en backend, corregidos falsos positivos de sesión y unificado wrapper HTTP en realtime. |
| 2026-02-18 | 1.0041 | 1.0042 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección de errores pendientes en API cliente: migración de converters a UUID para Order/DiningSession/SplitBill/SplitBillPerson, normalización de logging estructurado en orders.py, fix de sintaxis en shortcuts.py y saneamiento de payload JSON en solicitud de pago Clip. |
| 2026-02-18 | 1.0042 | 1.0043 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización documental de errores ya corregidos: actualización de `resueltos.txt` para marcar como RESUELTO el bloque BUG-20260214-001..007 (auth cliente), manteniendo trazabilidad consistente con archivos en `pronto-docs/resolved/`. |
| 2026-02-18 | 1.0043 | 1.0044 | Codex | pronto-docs, pronto-scripts/pronto-root | Validación de gates pre-boot ejecutada con éxito (`pronto-migrate --apply`, `pronto-init --check` con `pending=0 drift=0`) y cierre de pendientes 20260212 en `resueltos.txt`; se mantiene abierto el pendiente de parity al fallar `pronto-api-parity-check employees/clients`. |
| 2026-02-18 | 1.0044 | 1.0045 | Codex | pronto-client, pronto-static, pronto-docs, pronto-scripts/pronto-root | Resolución de parity phase 1: agregado endpoint `POST /api/login` y compatibilidad `GET /api/session/<uuid>/timeout` en client API; alineación de rutas frontend (`/api/session/*`, `/api/menu-items/*`), limpieza de referencias legacy ambiguas en employees, y verificación en verde de `pronto-api-parity-check clients` y `pronto-api-parity-check employees`. |
| 2026-02-18 | 1.0045 | 1.0046 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Corrección de bug de memory leak en listeners del flujo checkout cliente: `ThankYouPage` ahora usa `AbortController` para rebinding seguro de eventos y evitar acumulación al reinicializar módulo; incidente documentado y cerrado en error tracker. |
| 2026-02-18 | 1.0046 | 1.0047 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre formal de incidente `20260213_employees_realtime_css_logo_console_errors` tras verificación en código activo: filtrado de errores transitorios en realtime, normalización de `restaurant_assets` legacy y saneamiento CSS; actualización de tracker/resueltos. |
| 2026-02-18 | 1.0047 | 1.0048 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre formal de `BUG-2026-0216-003` (checkout-offers inline CSS): validado que el bloque inline fue migrado a `menu-modals.css` y actualizado tracker/resueltos. |
| 2026-02-18 | 1.0048 | 1.0049 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre documental de pendientes de prompts: `FEATURE-20260217-001` y `PROMPT-25` marcados como RESUELTO y movidos a `pronto-docs/resolved/`; actualización de `resueltos.txt` y versionado canónico. |
| 2026-02-18 | 1.0049 | 1.0050 | Codex | pronto-client, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Corrección de compliance final: eliminación de matches en gate `/<int:*_id>` mediante renombre de parámetros int en rutas API (sin cambio funcional), normalización de `ESTADO: RESUELTO` en `pronto-docs/resolved/` y cierre formal del incidente `ERR-20260218-GATE-VALIDATION-RESOLVED-CONSISTENCY`. |
| 2026-02-18 | 1.0050 | 1.0051 | Codex | pronto-docs, pronto-scripts/pronto-root | Ajuste documental en incidente resuelto `ERR-20260218-GATE-VALIDATION-RESOLVED-CONSISTENCY` para evitar falso positivo del gate (`ESTADO:\s*ABIERTO`) en texto descriptivo. |
| 2026-02-18 | 1.0051 | 1.0052 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de error tracker: se movieron 9 archivos con `ESTADO: RESUELTO` desde `pronto-docs/errors/` a `pronto-docs/resolved/` y se completaron entradas faltantes en `pronto-docs/resueltos.txt`. |
| 2026-02-18 | 1.0052 | 1.0053 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de formato en `pronto-docs/resueltos.txt` para eliminar espacios sobrantes en entradas nuevas de features resueltas. |
| 2026-02-18 | 1.0053 | 1.0054 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre formal de `PROMPT-24` (Contratos Publicos y API Parity) con evidencia de parity-check clients/employees en verde y gate de converters sin matches; movido a `pronto-docs/resolved/` y registrado en `resueltos.txt`. |
| 2026-02-18 | 1.0054 | 1.0055 | Codex | pronto-docs, pronto-scripts/pronto-root | Reparación de integridad en `pronto-docs/resueltos.txt`: se deshizo una concatenación accidental en una sola línea, restaurando entradas separadas por incidente (FEATURE-001..008 y PROMPT-24). |
| 2026-02-18 | 1.0055 | 1.0056 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre formal de `PROMPT-26` tras validar existencia y ejecutabilidad de scripts canónicos de mantenimiento/auditoría (`pronto-*`, `pre-commit-ai`, `run-audit.sh`); movido a `pronto-docs/resolved/`. |
| 2026-02-18 | 1.0056 | 1.0057 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre formal de `PROMPT-21` (Sistema de Gates y Agentes) con evidencia de scripts canónicos presentes, parity checks en verde y gate de authority de estados sin hallazgos; movido a `pronto-docs/resolved/`. |
| 2026-02-18 | 1.0057 | 1.0058 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre formal de `PROMPT-23` tras validación de máquina de estados canónica (`order_state_machine.py`), enums en `constants.py` y gate de authority de estados sin hallazgos; movido a `pronto-docs/resolved/`. |
| 2026-02-18 | 1.0058 | 1.0059 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de formato en `pronto-docs/resueltos.txt` para entradas `PROMPT-26`, `PROMPT-21` y `PROMPT-23` (sin espacios extra). |
| 2026-02-18 | 1.0059 | 1.0060 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre formal de `PROMPT-22` (Arquitectura P0) con evidencia de guardrails activos: ausencia de `session` en api/employees, soporte `PRONTO_ROUTES_ONLY`, checks pre-boot (`pending=0 drift=0`, `pronto-init --check` OK) y versionado root sincronizado. |
| 2026-02-18 | 1.0060 | 1.0061 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de formato en `pronto-docs/resueltos.txt` para la entrada de `PROMPT-22` (sin espacios extra). |
| 2026-02-18 | 1.0061 | 1.0062 | Codex | pronto-docs, pronto-static, pronto-libs, pronto-tests, pronto-scripts/pronto-root | Cierre de 3 incidencias de auditoría con evidencia actual: `AUDIT-20260215-FRONTEND` (deps lint/format + vitest presentes), `AUDIT-20260215-TEST-COVERAGE` (tests de `price_service` y `order_modification_service` existentes), `AUDIT-20260215-AUTH-WORKFLOW` (auditoría reporta cumplimiento y sin bugs bloqueantes). |
| 2026-02-18 | 1.0062 | 1.0063 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de formato en `pronto-docs/resueltos.txt` para entradas `AUDIT-20260215-*` (sin espacios extra). |
| 2026-02-18 | 1.0063 | 1.0064 | Codex | pronto-employees, pronto-docs, pronto-scripts/pronto-root | Hardening de shell employees: `templates/index.html` migra a bundle externo con meta CSRF y sin JS inline; cierre formal de `BUG-20260210-001-TEMPLATE-CLEANUP` y movimiento a `pronto-docs/resolved/`. |
| 2026-02-18 | 1.0064 | 1.0065 | Codex | pronto-libs, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Fix real de error en employees orders: `GET /api/orders` devolvía 500 por eager-load de `Order.customer` contra columna inexistente (`pronto_customers.email`). Se removió `joinedload(Order.customer)` y se corrigió manejo de warnings con IDs UUID; validado con Playwright `employees/orders.spec.ts` (2 passed). |
| 2026-02-18 | 1.0065 | 1.0066 | Codex | pronto-employees, pronto-static, pronto-docs, pronto-scripts/pronto-root | Cierre de `BUG-20250209-005-HYBRID-SSR-VUE`: verificado same-origin proxy en employees (`/assets` y `/api`), shell SPA actualizado y pruebas Playwright de orders en employees en verde. |
| 2026-02-18 | 1.0066 | 1.0067 | Codex | pronto-employees, pronto-docs, pronto-scripts/pronto-root | Cierre de `BUG-20250209-004-LEGACY-TEMPLATES`: eliminación de templates por rol en employees y consolidación en shell SPA único (`templates/index.html`) con assets compilados desde `pronto-static`. |
| 2026-02-18 | 1.0067 | 1.0068 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Cierre de `AUDIT-20260215-FRONTEND-TECH-DEBT`: documento de deuda duplicado y con ejemplos obsoletos; se normaliza cierre con inventario actual de módulos frontend y evidencia de refactorizaciones previas. |
| 2026-02-18 | 1.0068 | 1.0069 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Cierre de `AUDIT-20260215-CONFIG-COMPLEXITY`: validado que `pronto_business_config` ya no existe y la configuración activa usa `pronto_system_settings` sin escritura de `.env` en runtime en `set_config_value()`. |
| 2026-02-18 | 1.0069 | 1.0070 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Cierre de `AUDIT-20260215-DB-SCHEMA-DUPLICATION`: verificación de esquema runtime sin tablas legacy de permisos ni `pronto_business_config`; RBAC unificado y configuración activa en `pronto_system_settings`. |
| 2026-02-18 | 1.0070 | 1.0071 | Codex | pronto-docs, pronto-scripts/pronto-root | Cierre de `BUG-20250209-002-BUSINESS-LOGIC-REVIEW` como checklist macro supersedida por sistema activo de gates/agentes y seguimiento granular de incidencias en error tracker. |
| 2026-02-18 | 1.0071 | 1.0072 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Fix de idempotencia en migración `20260218_06__add_payments_table.sql` (`CREATE INDEX IF NOT EXISTS` para `ix_pronto_payments_session_id`), ejecución validada con `pronto-migrate --apply` + `pronto-init --check` (`pending=0 drift=0`) y cierre formal del incidente en error tracker. |
| 2026-02-18 | 1.0072 | 1.0073 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó checklist consolidado de bugs en `pronto-docs/errors/_reviews/20260218_bug_checklist.md` con estado por ticket (`[x]` resuelto / `[ ]` en seguimiento) para monitoreo continuo de resolución. |
| 2026-02-18 | 1.0073 | 1.0074 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de estado en `pronto-docs/resueltos.txt` para `ERR-20260218-MIGR-20260218-06-NONIDEMPOTENT-INDEX` (`N/A` -> `RESUELTO`) y regeneración del checklist consolidado de bugs. |
| 2026-02-18 | 1.0074 | 1.0075 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de estados de bugs históricos en `pronto-docs/resueltos.txt` (6 entradas `local-uncommitted`/`N/A` -> `RESUELTO`) y regeneración de checklist consolidado en `pronto-docs/errors/_reviews/20260218_bug_checklist.md`. |
| 2026-02-18 | 1.0075 | 1.0076 | Codex | pronto-docs, pronto-scripts/pronto-root | Se generó checklist de bugs por módulo en `pronto-docs/errors/_reviews/20260218_bug_checklist_by_module.md` para seguimiento operativo y priorización por proyecto. |
| 2026-02-18 | 1.0076 | 1.0077 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó checklist de bugs por severidad en `pronto-docs/errors/_reviews/20260218_bug_checklist_by_severity.md` (bloqueante/alta/media/baja/desconocida) para priorización semanal. |
| 2026-02-18 | 1.0077 | 1.0078 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó tablero ejecutivo `pronto-docs/errors/_reviews/20260218_bug_executive_top10.md` con Top 10 de riesgos históricos (BUG/ERR) ordenados por severidad y fecha. |
| 2026-02-18 | 1.0078 | 1.0079 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó reporte SLA de bugs en `pronto-docs/errors/_reviews/20260218_bug_sla_report.md` con promedio global, promedio por severidad y Top 10 por tiempo de resolución. |
| 2026-02-18 | 1.0079 | 1.0080 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó reporte de aging en `pronto-docs/errors/_reviews/20260218_bug_aging_report.md` con buckets de resolución (0-7, 8-30, 31-90, 90+, sin-dato) para control de deuda histórica. |
| 2026-02-18 | 1.0080 | 1.0081 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó dashboard maestro consolidado de bugs en `pronto-docs/errors/_reviews/20260218_bug_dashboard_master.md` con resumen ejecutivo e índice unificado de tableros (general, módulo, severidad, top10, SLA, aging). |
| 2026-02-18 | 1.0081 | 1.0082 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó tablero semáforo en `pronto-docs/errors/_reviews/20260218_bug_dashboard_semaforo.md` y se enlazó en el dashboard maestro para lectura ejecutiva en 10 segundos. |
| 2026-02-18 | 1.0082 | 1.0083 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó índice operativo `pronto-docs/errors/_reviews/README.md` como punto de entrada único para tableros de bugs (semáforo, maestro, checklist, severidad, top10, SLA, aging). |
| 2026-02-18 | 1.0083 | 1.0084 | Codex | pronto-docs, pronto-scripts/pronto-root | Se creó `pronto-docs/errors/_reviews/STATUS.md` como portada corta para standup y se enlazó en el índice `pronto-docs/errors/_reviews/README.md`. |
| 2026-02-18 | 1.0084 | 1.0085 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se creó `pronto-scripts/bin/refresh-bug-reviews.sh` para regenerar automáticamente dashboards de bugs (checklist, módulo, severidad, top10, SLA, aging, semáforo, master, status y README) y se validó su ejecución end-to-end. |
| 2026-02-19 | 1.0085 | 1.0086 | Kilo | pronto-employees, pronto-libs, pronto-tests, pronto-docs | Fix de 2 bugs bloqueantes: (1) Template index.html de employees ahora incluye window.APP_CONTEXT para Vue app, (2) customer_service.py ahora importa UUID correctamente. Tests de login actualizados para aceptar comillas dobles en APP_CONTEXT. |
| 2026-02-19 | 1.0086 | 1.0087 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Mejora de `refresh-bug-reviews.sh`: cálculo real de bugs abiertos desde `pronto-docs/errors/*.md` e integración de ese valor en checklist, semáforo, dashboard maestro y status standup. |
| 2026-02-19 | 1.0087 | 1.0088 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Ajuste de `refresh-bug-reviews.sh` para emitir rutas en Markdown con backticks en dashboards generados (master, semáforo, status) y validación de regeneración completa. |
| 2026-02-19 | 1.0088 | 1.0089 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | `refresh-bug-reviews.sh` ahora soporta `--date YYYY-MM-DD` (y `--help`) para cortes históricos reproducibles en dashboards; incluye validación de formato/fecha y pruebas de ejecución normal + fecha explícita. |
| 2026-02-19 | 1.0089 | 1.0090 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | `refresh-bug-reviews.sh` añade modo `--check` (exit 1 si hay bugs abiertos o en seguimiento), actualiza `--help` y documenta el comando en `pronto-docs/errors/_reviews/README.md`. |
| 2026-02-19 | 1.0090 | 1.0091 | Kilo | pronto-docs, pronto-static, pronto-employees, pronto-client | Auditoria cross-module & UX: detectados 3 bugs nuevos (ERR-20260219-MISSING-BACKEND-ENDPOINTS, ERR-20260219-HARDCODED-HOSTS, ERR-20260219-MISSING-PLACEHOLDER-ASSET). Verificacion OK de roles canonicos, estados, imports legacy y static_folder. Reporte consolidado en `pronto-docs/findings-autonomas/20260219_cross_module_ux_audit.md`. |
| 2026-02-19 | 1.0091 | 1.0092 | Codex | pronto-libs, pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix de bug bloqueante de menú: `/api/menu` devolvía 500 por error de mapper (`Payment` no resuelto en `DiningSession`). Se ajustó binding ORM en `pronto_shared/models.py`, se reconstruyó `client`, se validó `GET /api/menu` = 200 y se cerró incidente `ERR-20260219-MENU-MAPPER-PAYMENT` en error tracker. |
| 2026-02-19 | 1.0091 | 1.0092 | Kilo | pronto-static | Fix de bug ReferenceError: eliminada llamada a funcion inexistente initializeApp() en menu.ts y import no utilizado. |
| 2026-02-18 | 1.0092 | 1.0093 | Amp | pronto-libs, pronto-api, pronto-prompts | Auditoría P0 del flujo de órdenes: (1) Fix bug crítico en OrderStateMachine donde handlers no llamaban _change_status(), (2) Eliminación de ~40+ magic strings reemplazados por enums canónicos (OrderStatus, PaymentStatus, SessionStatus) en 13 archivos de servicios y rutas, (3) Actualización del inventario order_request_flow_files.md con 9 archivos faltantes. |
| 2026-02-19 | 1.0093 | 1.0094 | Codex | pronto-static, pronto-employees, pronto-client, pronto-docs, pronto-scripts/pronto-root | Cierre del barrido de errores similares al fallo de menú: ajuste de método explícito GET en `PaymentFlow.vue` para parity, validación de endpoints employees (`/api/branding/config`, `/api/sessions/anonymous`, `/api/sessions/merge`), cierre y movimiento de `ERR-20260219-HARDCODED-HOSTS` a `resolved`, actualización de `resueltos.txt` y refresco de dashboards con `refresh-bug-reviews.sh`. |
| 2026-02-19 | 1.0092 | 1.0093 | Kilo | pronto-docs, pronto-static | Resolucion de bugs: (1) ERR-20260219-MISSING-BACKEND-ENDPOINTS resulto falso positivo (endpoints existen, error causado por contenedor reiniciando), (2) ERR-20260219-MISSING-PLACEHOLDER-ASSET creado placeholder-food.png, (3) Fix LSP error parametro from no usado en router. |
| 2026-02-19 | 1.0094 | 1.0095 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Se corrigió bug de auth en employees: `auth-interceptor.ts` eliminó llamado a endpoint inexistente `/api/employees/auth/refresh` y usa flujo canónico de 401 con `checkAuthAndRedirect`. Se documentó/cerró `ERR-20260219-EMPLOYEES-AUTH-REFRESH-MISSING`, se movió a `resolved`, se actualizó `resueltos.txt` y se refrescaron dashboards. |
| 2026-02-19 | 1.0095 | 1.0096 | Codex | pronto-employees, pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix de realtime employees: se implementó blueprint `routes/api/realtime.py` con `GET /api/realtime/orders` y `GET /api/realtime/notifications`, registro en `api/__init__.py`, validación runtime (antes 404, ahora 401 sin auth), cierre de `ERR-20260219-EMPLOYEES-REALTIME-ENDPOINTS-MISSING` y refresco de dashboards de errores. |
| 2026-02-19 | 1.0096 | 1.0097 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de tracking: se corrigieron en `pronto-docs/resueltos.txt` las entradas `ERR-20260218-OSM-HANDLERS` y `ERR-20260218-MAGIC-STRINGS` de `pendiente` a `RESUELTO`, y se regeneraron dashboards con `refresh-bug-reviews.sh`. |
| 2026-02-19 | 1.0097 | 1.0098 | Codex | pronto-employees, pronto-docs, pronto-scripts/pronto-root | Fix de endpoint faltante para validación de notificaciones en employees: se agregó `GET/HEAD /api/orders/<uuid:order_id>` en `routes/api/orders.py`, se corrigió decorador compatible con Flask (`route(..., methods=['HEAD'])`), se validó runtime (401 sin auth, no 404), y se cerró `ERR-20260219-EMPLOYEES-ORDER-HEAD-MISSING`. |
| 2026-02-19 | 1.0098 | 1.0099 | Kilo | pronto-docs, pronto-static, pronto-tests | Verificacion final de hardcoded hosts (falso positivo - ya usa variables de entorno). Todos los bugs reportados resueltos. Tests: 20 passed, 1 failed (chef_notifications E2E). Sistema estable con 6 servicios healthy. |
| 2026-02-19 | 1.0098 | 1.0099 | Codex | pronto-scripts, pronto-scripts/pronto-root | Mejora de `pronto-api-parity-check`: filtrado de `unknown_methods` cuando la ruta está cubierta por backend (exacta o por prefijo dinámico) y ajuste de `missing_unknown_method` para evitar falsos positivos de rutas base (`/api/session`). Resultado: parity `employees` sin unknown/warnings y parity `clients` sin missing_unknown espurio. |
| 2026-02-19 | 1.0099 | 1.0100 | Kilo | pronto-static | Fix de TypeError en use-menu.ts: corregido acceso a data.categories en lugar de tratar la respuesta como array. Eliminada funcion no usada getApiBase en http.ts. |
| 2026-02-19 | 1.0100 | 1.0101 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix de parity y endpoints en clientes: corrección de rutas de auth en `debug_panel.html`, método explícito `GET` para `/api/session/{id}/orders`, implementación de `POST /api/orders/send-confirmation` en BFF cliente (proxy a `send-ticket-email`), ajuste de `checkout.html` para enviar `session_id`, cierre de `ERR-20260219-CLIENTS-TEMPLATE-HOST-API-CALLS` y validación parity `clients` + `employees` en `ok: true`. |
| 2026-02-19 | 1.0101 | 1.0102 | Codex | pronto-docs, pronto-scripts/pronto-root | Normalización de tracker: actualización de `pronto-docs/resueltos.txt` para marcar como `RESUELTO` tres incidencias ya cerradas (`ERR-20260219-EMPLOYEES-AUTH-REFRESH-MISSING`, `ERR-20260219-EMPLOYEES-REALTIME-ENDPOINTS-MISSING`, `ERR-20260219-EMPLOYEES-ORDER-HEAD-MISSING`). |
| 2026-02-19 | 1.0100 | 1.0101 | Kilo | pronto-static, pronto-client, pronto-employees | Fix de imagenes placeholder: actualizados paths relativos a usar static_host_url de APP_CONFIG. Agregado cache buster a menu.js y main.js de employees. |
| 2026-02-19 | 1.0102 | 1.0103 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Auditoría cruzada front↔backend/static en `pronto-client`, `pronto-employees`, `pronto-api`, `pronto-static`; documentación de 4 bugs abiertos en `pronto-docs/errors/` (CSRF checkout, UUID feedback, feedback cross-host/auth-CSRF, mismatch de env key para base URL de employees). |
| 2026-02-19 | 1.0103 | 1.0104 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección de 4 bugs de paridad/auth en clientes: CSRF en checkout y feedback, proxy BFF `POST /api/feedback/bulk`, parsing UUID en ruta `/feedback`, ajuste de clave de entorno `EMPLOYEE_API_BASE_URL`; incidencias cerradas y movidas a `pronto-docs/resolved/`. |
| 2026-02-19 | 1.0104 | 1.0105 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se agregó checklist ejecutable `pronto-revision-checklist.sh` para auditoría total de `pronto-api`, `pronto-employees`, `pronto-client`, `pronto-static` (inventario, parity, gates P0/P1 y assets), y se generó reporte inicial en `pronto-docs/errors/_reviews/20260219_full_revision_checklist.md`. |
| 2026-02-19 | 1.0105 | 1.0106 | Codex | pronto-api, pronto-docs, pronto-scripts/pronto-root | Corrección P0 de seguridad: eliminación de `@csrf.exempt` en `POST /api/employees/auth/login` (pronto-api), cierre de incidente `ERR-20260219-EMPLOYEES-AUTH-LOGIN-CSRF-EXEMPT`, regeneración de checklist total con estado `APPROVED`. |
| 2026-02-19 | 1.0106 | 1.0107 | Codex | .github/workflows, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Integración del gate de auditoría total en CI: `pronto-revision-checklist.sh --check` agregado a `pronto-audit.yml` y `dispatch-pronto-audit.yml`, con artefactos del checklist en ambos workflows. |
| 2026-02-19 | 1.0101 | 1.0102 | Kilo | pronto-libs | Fix de SystemSetting: corregidos nombres de atributos (config_key, config_value) en settings_service.py. Agregado metodo get_typed_value() al modelo. |
| 2026-02-19 | 1.0106 | 1.0107 | Kilo | pronto-client, pronto-employees, pronto-static, pronto-api | Resueltos 3 hallazgos de auditoria: (1) kiosk password movido a env var, (2) useFetch.ts con credentials include, (3) TODO feedback.py implementado. |
| 2026-02-19 | 1.0107 | 1.0108 | Kilo | pronto-tests | Fix de test chef_notifications.spec.ts: reescrito para usar request API en lugar de page.goto(). Eliminada dependencia de Python inline. 21/24 tests pasando (3 skipped por falta de credenciales). |
| 2026-02-19 | 1.0102 | 1.0103 | Codex | pronto-docs, pronto-scripts/pronto-root | Barrido final de errores similares: parity `clients` y `employees` en limpio (`ok: true`, sin unknown/missing), normalización de estados en `pronto-docs/resueltos.txt` para incidencias ya cerradas que figuraban como `N/A`/`local-uncommitted`, y regeneración de dashboards. |
| 2026-02-19 | 1.0103 | 1.0104 | Codex | AGENTS.md, pronto-api, pronto-employees, pronto-client, pronto-static, pronto-docs, pronto-scripts/pronto-root | Se aplicó la convención de rutas: contexto público hacia `/public/*` y autenticación hacia `/auth/*` (manteniendo aliases legacy para compatibilidad), con actualización de consumidores frontend y bump de versión del sistema. |
| 2026-02-19 | 1.0108 | 1.0109 | Codex | pronto-docs, pronto-scripts/pronto-root | Reconciliación de versionado del workspace: se alineó `PRONTO_SYSTEM_VERSION` en root y snapshot versionado (`.env`, `.env.example`, `pronto-root/.env`, `pronto-root/.env.example`) al siguiente valor canónico tras cambios concurrentes y se validó consistencia de tracker/parity. |
| 2026-02-22 | 1.0109 | 1.0110 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección de render roto en menú clientes: se agregaron estilos de compatibilidad para runtime Vue en `menu-updates.css` (tabs, search y layout), se documentó/cerró incidente `ERR-20260222-CLIENT-MENU-VUE-STYLES-MISSING` y se alineó `PRONTO_SYSTEM_VERSION` en root y snapshot versionado. |
| 2026-02-22 | 1.0110 | 1.0111 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Reapertura y fix de cache-busting para menú clientes: `index.html` ahora versiona `menu.css` y `menu-updates.css` con `system_version` para invalidar cache immutable y aplicar estilos nuevos en navegador. |
| 2026-02-22 | 1.0111 | 1.0112 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Reapertura de bug visual en menú clientes: fallback de imagen actualizado a `placeholder-food.png` en Vue y bundle estático servido, fallback CSS para imágenes vacías, cache-busting adicional en CSS de `index.html`, y cierre documental del incidente reabierto. |
| 2026-02-22 | 1.0112 | 1.0113 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Mejora visual de menú cliente cuando API retorna `image_path=null`: fallback inteligente por keywords en `ProductCard` y bundle runtime de `menu.js`, evitando imagen única repetida en todos los productos; cierre documental del incidente. |
| 2026-02-22 | 1.0113 | 1.0114 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Ajuste final de cache-busting en cliente: `base.html` deja de usar `menu.js?v=cachebust2` y usa versión canónica con `system_version+rev`, evitando carga del bundle legado en paralelo y aplicando fallback visual actualizado de forma consistente. |
| 2026-02-22 | 1.0114 | 1.0115 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix UX/flujo de llamada a mesero: campana deshabilitada sin sesión activa, guard clause en `callWaiter()` para evitar request inválido, estilos visuales de estado deshabilitado y cache-busting de `base.js` para aplicar el bundle actualizado. |
| 2026-02-22 | 1.0115 | 1.0116 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Ajuste responsive integral del menú cliente: unificación de grid/cards para eliminar desalineación y variación de tamaño entre tarjetas, refuerzo mobile breakpoints, cache-busting de CSS en `index.html`, y registro/cierre documental de incidencia visual. |
| 2026-02-22 | 1.0116 | 1.0117 | Codex | pronto-static, pronto-libs, pronto-scripts/init, pronto-docs, pronto-scripts/pronto-root | Cobertura total de imágenes de ejemplo para catálogo de menú (94/94 rutas `image_path` del seed con archivo físico), generación de faltantes con recursos gratuitos, y alta de catálogos de referencia en `pronto-scripts/init/assets/menu-images/` + seed de referencia `0395__image_assets_reference.sql` para nuevos ambientes. |
| 2026-02-22 | 1.0117 | 1.0118 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Mejora de UX en cards de menú cliente: botón `+` de agregar reforzado (visible en imagen y CTA), badge/overlay claro de `Agotado` para productos no disponibles y normalización defensiva de `is_available` para evitar inconsistencias visuales. |
| 2026-02-22 | 1.0118 | 1.0119 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix UX modal cliente: el modal de producto (`#item-modal`) ahora se cierra al hacer clic en el backdrop (fuera del formulario), alineado con comportamiento de tecla ESC. |
| 2026-02-22 | 1.0119 | 1.0120 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix del carrito cliente: se agregaron bindings explícitos de click para botones de carrito (`data-toggle-cart` y sticky bar), exposición global de `showCart/toggleCart/closeCart` y tipado global para asegurar apertura del panel al hacer clic. |
| 2026-02-22 | 1.0120 | 1.0121 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Composición visual del menú cliente sobre CSS runtime: refinado de header/search/tabs, estados activos más claros, compactación de espaciados, grilla más equilibrada y cards con jerarquía/contraste uniformes para mejorar percepción estética en desktop y mobile. |
| 2026-02-22 | 1.0121 | 1.0122 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Mejora de UX del menú de avatar: modo compacto para usuario logueado (`profile-modal--compact`) con dropdown anclado arriba-derecha, avatar y acciones estilizadas, y fallback mobile tipo sheet; se corrigió apariencia rota del panel de perfil. |
| 2026-02-22 | 1.0122 | 1.0123 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Cache-busting del menú cliente para aplicar de inmediato estilos del dropdown de perfil: actualización de `rev` en `menu.css` y `menu-updates.css` dentro de `index.html`. |
| 2026-02-22 | 1.0123 | 1.0124 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Persistencia versionada del dropdown de perfil: estilos `profile-modal--compact` añadidos en `base.html` (trackeado) para evitar dependencia de `menu-updates.css` ignorado y mantener UX consistente del avatar desplegable. |
| 2026-02-22 | 1.0124 | 1.0125 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Reapertura fix carrito cliente: fallback SSR en `base.html` para enlazar `data-toggle-cart`/`sticky-cart-btn` y exponer `showCart/toggleCart/closeCart` con apertura/cierre legacy cuando el binding Vue no está activo. |
| 2026-02-22 | 1.0125 | 1.0126 | Codex | pronto-client, pronto-api, pronto-libs, pronto-static, pronto-docs, pronto-employees, pronto-scripts/pronto-root | Implementacion de mesa obligatoria al confirmar orden en Tab Detalles: resolucion server-side (kiosk>qr>manual), persistencia de table context en customer_session_store (sin flask.session), lock QR/kiosk, gate TABLE_REQUIRED/TABLE_LOCKED_BY_QR, UI de combo de mesa, cierre de incidente ERR-20260222-TABLE-REQUIRED-DETALLES, y ajustes de parity/entorno (Stripe opcional + ruta /api/customer/orders). |
| 2026-02-23 | 1.0126 | 1.0127 | Codex | pronto-api, pronto-client, pronto-libs, pronto-static, pronto-docs, pronto-scripts/pronto-root | Hardening final de mesa obligatoria por autoridad de DiningSession: gate `TABLE_SESSION_MISMATCH` + reconciliación de table context (`table_source=session`), limpieza legacy realtime/waiter modules para `pronto-no-legacy`, contratos OpenAPI alineados, mapping UX de errores en checkout y validación smoke de códigos `TABLE_REQUIRED`, `TABLE_LOCKED_BY_QR`, `KIOSK_TABLE_NOT_CONFIGURED`, `TABLE_SESSION_MISMATCH`. |
| 2026-02-23 | 1.0127 | 1.0128 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix del carrito cliente: eliminación del doble toggle por click en botones de carrito al unificar claves de binding (`cartToggleBound`/`cartOpenBound`) en fallback SSR para no duplicar listeners con Vue; alta de incidente `ERR-20260223-CART-DOUBLE-TOGGLE-CLICK` y actualización de versión canónica. |
| 2026-02-23 | 1.0128 | 1.0129 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Ajuste UX solicitado: se deshabilitó por completo la notificación de antigüedad de productos del carrito ("Tienes X producto(s)... por más de 10 minutos"), removiendo sus disparadores en store de clientes y regenerando bundle `assets/js/clients/base.js`. |
| 2026-02-23 | 1.0129 | 1.0130 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix de carrito en modal de producto: `addToCartFromModal` dejó de ser simulado y ahora persiste ítems reales vía `window.addToCart`, con soporte de cantidad (`adjustModalQuantity`) y actualización de totales del modal. |
| 2026-02-23 | 1.0130 | 1.0131 | Codex | pronto-libs, pronto-scripts/pronto-root | Normalización de distribución de catálogo en init: `seed.py` ahora soporta IDs explícitos UUID al crear entidades seed y aplica perfil de menú (paquetes/aditamentos/solos, quick-serve de bebidas y tiempos de preparación). Además se aplicó la distribución al catálogo existente en DB (deduplicado previo), dejando refrescos/bebidas como quick-serve con prep corto. |
| 2026-02-23 | 1.0131 | 1.0132 | Codex | pronto-libs, pronto-scripts/pronto-root | Prevención de productos duplicados al cargar menú: deduplicación defensiva por nombre normalizado en `get_full_menu`, bloqueo de creación/edición de ítems duplicados por categoría en `menu_service`, y normalización de búsqueda de seed para evitar reinserciones equivalentes. |
| 2026-02-23 | 1.0132 | 1.0133 | Codex | pronto-client, pronto-scripts/pronto-root | Ajuste de alineación del footer en clientes: `footer-hours-btn` queda orillado a la derecha en desktop mediante layout de grid en `base.html`, manteniendo comportamiento responsive en móvil. |
| 2026-02-23 | 1.0133 | 1.0134 | Codex | pronto-client, pronto-static, pronto-libs, pronto-scripts/pronto-root | Recuperación de arranque de `pronto-client`: se reconstruyó `routes/api/orders.py` (Blueprint + `_forward_to_api` + endpoint `/api/orders/send-confirmation`) para eliminar `NameError: orders_bp`. Además, hardening de atajos con fallback por defecto en `/api/shortcuts` y normalización de combos en `keyboard-shortcuts.js`. Servicio validado en `healthy` y `/health` OK. |

| 2026-02-23 | 1.0134 | 1.0135 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix del flujo de carrito cliente: implementación de `window.proceedToCheckout` + wiring de tabs (`menu/details/orders`) en `client-base.ts` para que "Ir a pagar" abra siempre la vista de Detalles/checkout; cierre documental del incidente ERR-20260223-CHECKOUT-SIN-ACCION. |
| 2026-02-23 | 1.0134 | 1.0135 | Codex | pronto-libs, pronto-api, pronto-scripts/pronto-root | Ajuste de lógica de combos en seed: combos ahora se construyen dinámicamente con productos existentes (no plantilla fija), heredan aditamientos de productos base y agregan aditamientos específicos por combo (`<Combo>: Extras del Combo`). Se reemplazaron grupos legacy por grupos incluidos basados en catálogo (`Paquete: Bebida incluida`, `Paquete: Guarnición incluida`) y se validó en runtime con `pronto-api` rebuild + normalización aplicada. |

| 2026-02-23 | 1.0135 | 1.0136 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Flujo checkout con auth obligatorio: `proceedToCheckout` valida sesión (`/api/auth/me`), abre modal login/registro si falta auth y, tras `auth-success`, reanuda automáticamente al tab `Detalles` para confirmar orden. |

| 2026-02-23 | 1.0136 | 1.0137 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix visual del modal de auth en clientes: `profile-modal` elevado a `z-index:11050`, ajuste de `max-height/overflow` y padding interno de `.profile-view` para evitar superposición con tabs sticky y contenido pegado a orillas. |
| 2026-02-23 | 1.0135 | 1.0136 | Codex | AGENTS.md, pronto-docs, pronto-scripts/pronto-root | Documentación formal de regla de negocio de combos/paquetes y aditamientos: alta de sección P0 en `AGENTS.md` (3.2) y documento funcional en `pronto-docs/business-rules/combos-paquetes-y-aditamientos.md` con criterios de aceptación y checklist de verificación. |

| 2026-02-23 | 1.0137 | 1.0138 | Codex | pronto-libs, pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix auth login 500: verificación de credenciales ahora soporta hashes `pbkdf2/scrypt` y captura `ValueError` de bcrypt (`Invalid salt`) para retornar fallo de autenticación controlado (401) en lugar de 500. |

| 2026-02-23 | 1.0138 | 1.0139 | Codex | pronto-client, pronto-static, pronto-docs, pronto-scripts/pronto-root | Avatar UX en clientes: sin sesión se fuerza icono anónimo blanco/negro; con sesión se asigna icono por defecto configurable desde perfil mediante picker de iconos, persistido en localStorage y reflejado en botón de perfil/modal. |
| 2026-02-23 | 1.0139 | 1.0140 | Codex | pronto-client, pronto-static, pronto-scripts/pronto-root | Distinción visual explícita del estado de perfil en clientes: botón/avatar marca `invitado sin sesión` vs `sesión activa` con estilos diferenciados (anillo/indicador), y actualización de `title`/`aria-label` dinámica según autenticación. |
| 2026-02-23 | 1.0140 | 1.0141 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix de acordeón en tab Detalles: se agregó binding explícito para minimizar/expandir (`click` y `Enter/Espacio`) en headers de checkout, con sincronización de `aria-expanded`; se documentó resolución del incidente. |
| 2026-02-23 | 1.0141 | 1.0142 | Codex | pronto-libs, pronto-client, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Unificación de autenticación segura: hash moderno PBKDF2 para nuevos passwords, helper `verify_and_upgrade_credentials` con compatibilidad transicional (bcrypt/sha256 legacy), migración automática de hash en login de clientes/empleados y eliminación de logs sensibles de credenciales. |
| 2026-02-23 | 1.0142 | 1.0143 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix UX del footer cliente: se añadió binding de `#footer-hours-btn` para abrir modal de horarios cuando existe y fallback visible (scroll + resaltado de sección) cuando no hay modal disponible. |
| 2026-02-23 | 1.0143 | 1.0144 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix de soporte técnico en clientes: implementación de `setupSupportModal/openSupportModal/closeSupportModal` (bind de click en link footer, cierre por overlay/ESC y submit del formulario con validación y notificación). |
| 2026-02-23 | 1.0144 | 1.0145 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | UX de tabs cliente: `Órdenes` ahora se deshabilita automáticamente cuando no hay órdenes activas, se habilita con badge cuando sí existen, y se bloquea navegación a vista vacía. |
| 2026-02-23 | 1.0145 | 1.0146 | Codex | pronto-libs, pronto-employees, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Enforcement de auth sin legacy: se eliminó compatibilidad transicional de hashes legacy en runtime (solo verificación de hashes modernos), se alineó `auth_hash` a VARCHAR(255) en modelos/init/migraciones, se agregó migración `20260223_01__auth_hash_column_length_255.sql`, y se removieron defaults inseguros de `SECRET_KEY`/`PASSWORD_HASH_SALT` en scripts de init/seed. |
| 2026-02-23 | 1.0146 | 1.0147 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Ajuste visual del modal de registro en clientes: layout y scroll interno del tab `Registrarse` corregidos para mostrar el formulario completo con paddings y espaciado consistentes. |
| 2026-02-23 | 1.0147 | 1.0148 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix del tab Detalles en clientes: sincronización del resumen de pedido con el carrito reactivo para mostrar productos, precio unitario y total por línea, actualización de contador/total y cache-busting de `base.js` para forzar carga del bundle corregido. |
| 2026-02-23 | 1.0148 | 1.0149 | Codex | pronto-libs, pronto-scripts/pronto-root | Seed canónico ajustado para demo de disponibilidad: `Combo Doble` y `Combo Familiar` se fuerzan a `is_available=false` en los flujos de seed (`load_seed_data` y `_ensure_seed_data_impl`) para visualizar estado agotado en menú cliente. |
| 2026-02-23 | 1.0149 | 1.0150 | Codex | pronto-client, pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix de confirmación de pedido en tab Detalles: submit de `checkout-form` ahora crea orden real (`POST /api/customer/orders`) con items del carrito y CSRF, se añadió proxy BFF en `pronto-client` para `customer/orders`, se reforzó feedback de errores/éxito y `client-base` ahora soporta `session_id` UUID para refrescar órdenes activas. |
| 2026-02-23 | 1.0150 | 1.0151 | Codex | pronto-client, pronto-static, pronto-libs, pronto-docs, pronto-scripts/pronto-root | Fix crítico de confirmación de orden: rotación CSRF post login/registro (backend devuelve csrf_token + frontend actualiza meta token), proxy BFF de órdenes robusto en contenedor (fallback de API base cuando apunta a localhost), y autenticación interna BFF→API con X-Pronto-Internal-Auth para evitar rechazo CSRF inter-servicio. |
| 2026-02-23 | 1.0151 | 1.0152 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix definitivo de CSRF en confirmar pedido: nuevo endpoint autenticado `/api/auth/csrf` y retry automático en checkout (`index.html`) para refrescar token y reintentar una vez la creación de orden ante 400 CSRF. |
| 2026-02-23 | 1.0152 | 1.0153 | Codex | pronto-client, pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix de sesión fantasma en checkout: reconciliación temprana con `/api/auth/me` en perfil para no confiar ciegamente en localStorage y manejo explícito de 401 en confirmar pedido (limpia estado local, notifica sesión expirada y abre login). |
| 2026-02-23 | 1.0153 | 1.0154 | Codex | pronto-client, pronto-static, pronto-docs, pronto-scripts/pronto-root | Hardening de auth cliente para checkout: login/register ahora retornan `customer_ref`, `/api/auth/me` expone `customer_ref`, el frontend persiste `pronto-customer-ref` y checkout/envíos autenticados incluyen `X-PRONTO-CUSTOMER-REF`; además `customer_session_required` reconstruye sesión desde header válido para evitar 401 con UI logueada. |
| 2026-02-23 | 1.0154 | 1.0155 | Codex | AGENTS.md, pronto-client/AGENTS.md, pronto-static/AGENTS.md, pronto-scripts/pronto-root | Regla P0 declarada: autoridad única de API y negocio en `:6082/api/*`; prohibido endpoints/lógica de negocio fuera de `pronto-api` (incluye pronto-client y pronto-employees como SSR/UI únicamente). |
| 2026-02-23 | 1.0155 | 1.0156 | Codex | pronto-libs, pronto-api, pronto-scripts/pronto-root | Política de expiración de sesión alineada por tipo: clientes 1h (TTL Redis), kiosko sin expiración operativa (Redis sin TTL + token/cookie de larga duración), y empleados 24h (`access_token` cookie en login/refresh). |
| 2026-02-23 | 1.0156 | 1.0157 | Codex | pronto-api, pronto-libs, pronto-scripts/pronto-root | Expiración de sesión administrable desde /admin: nuevas llaves `client_session_ttl_seconds`, `kiosk_session_non_expiring`, `employee_session_ttl_hours`; runtime de JWT/cookies/Redis migrado a leer SystemSetting; seed actualizado para persistir defaults y disponibilidad en panel de configuración. |
| 2026-02-23 | 1.0157 | 1.0158 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Checkout robusto ante 409 de mesa/sesión: al recibir `TABLE_LOCATION_MISMATCH`/`TABLE_SESSION_MISMATCH`, el cliente sincroniza la mesa retornada por backend (`current_table_id/current_table_code`) y reintenta una vez automáticamente la creación de orden. |
| 2026-02-23 | 1.0158 | 1.0159 | Codex | pronto-api, pronto-docs, pronto-scripts/pronto-root | Confirmación de pedido robusta ante desalineación de mesa: `/api/customer/orders` deja de responder 409 por mismatch de mesa en sesión activa y alinea automáticamente al `table_id` autoritativo antes de crear la orden. |
| 2026-02-23 | 1.0159 | 1.0160 | Codex | pronto-static, pronto-scripts/pronto-root | Corrección de tracker de órdenes en clientes: `getTrackerStatus` ahora devuelve estado canónico (`new|queued|...`) y evita warnings `Unknown status: "Pedido recibido"` al renderizar mini-tracker/polling. |
| 2026-02-23 | 1.0160 | 1.0161 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix del tab Órdenes en clientes: se añadió render explícito de órdenes activas (`renderActiveOrders`) y recarga al entrar al tab (`refreshActiveOrders`), eliminando la vista en blanco tras confirmar pedido. |
| 2026-02-23 | 1.0161 | 1.0162 | Codex | AGENTS.md, pronto-scripts/pronto-root/AGENTS.md | Se agrega excepción P0 controlada para BFF proxy técnico temporal (solo transporte hacia :6082/api/*, sin lógica de negocio, con trazabilidad y plan de retiro deprecado). |
| 2026-02-23 | 1.0162 | 1.0163 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix de IDs en tracker/tab de órdenes: se elimina coerción numérica que convertía UUID a `0`, se usa display ID estable y se recompila/redeploya frontend para evitar `Orden #0` repetido. |
| 2026-02-23 | 1.0163 | 1.0164 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Fix crítico de call waiter: resuelto `UnboundLocalError` por sombreado de `session` en `/api/call-waiter` (renombre a `db_session`) y corrección de ruta/param en endpoint alterno de estado de llamada. |
| 2026-02-23 | 1.0164 | 1.0165 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Rehidratación robusta tras hard refresh: sesión cliente ahora se reconstruye siempre desde `/api/sessions/me` y se elimina cast numérico de `session_id` para compatibilidad UUID, evitando pérdida de órdenes activas. |
| 2026-02-23 | 1.0165 | 1.0166 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix de rehidratación tras refresh: parseo de `/api/sessions/me` alineado a `session_id` canónico (con fallback legacy) y preservación de sesión local ante errores transitorios de red/autenticación. |
| 2026-02-23 | 1.0166 | 1.0167 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix de loop de refresh en cliente: se elimina redirección automática en `checkActiveOrdersGlobal` cuando no hay órdenes activas y se limita limpieza de sesión local a estados finales (sesión cerrada/pagada). |
| 2026-02-23 | 1.0167 | 1.0168 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Fix definitivo de loop de refresh: en `SESSION_CLOSED` se elimina redirección forzada (`resetClientSession`) y se reemplaza por limpieza local no disruptiva (sin recargar página). |
| 2026-02-24 | 1.0168 | 1.0169 | Codex | pronto-api, pronto-scripts/pronto-root | Fix de sesión cliente: /api/sessions/me y /api/sessions/validate ahora aceptan estados activos (open, active, awaiting_tip, awaiting_payment) y solo marcan SESSION_CLOSED en estados terminales, evitando pérdida visual de órdenes activas. |
| 2026-02-24 | 1.0169 | 1.0170 | Codex | pronto-static, pronto-employees, pronto-scripts/pronto-root | Fix de loop en /waiter: App.vue de empleados importa correctamente initDraggableModals y TableAssignmentModal (evita fallo de inicialización), y template SSR de empleados usa cache busting real en main.js con system_version para evitar chunks obsoletos en navegador. |
| 2026-02-24 | 1.0170 | 1.0171 | Codex | pronto-employees, pronto-scripts/pronto-root | Fix CORS/modulos en login employees: template index.html corrige render seguro de STATIC_HOST_URL (evita &#34;&#34; y SyntaxError en linea 12) y se valida serving same-origin de /assets tras rebuild completo de servicios. |
| 2026-02-24 | 1.0171 | 1.0172 | Codex | pronto-static, pronto-scripts/pronto-root | Fix de loop en login employees: guard de router evita recarga redundante a /<scope>/login cuando ya esta en esa ruta y navega a vista guest interna, evitando abortos de XHR (NS_BINDING_ABORTED) por navegacion repetida. |
| 2026-02-24 | 1.0172 | 1.0173 | Codex | pronto-static, pronto-scripts/pronto-root | Ajuste de enrutado employees: router base unificada a /<scope>/ y dashboard en /dashboard para eliminar loop entre /waiter/login, /waiter/dashboard/ y /waiter/dashboard/waiter/login. |
| 2026-02-24 | 1.0173 | 1.0174 | Codex | pronto-static, pronto-libs, pronto-api, pronto-scripts/pronto-root | Fix integral de autenticación de empleados: agregado authStore.login con CSRF en staff login, reparación masiva de credenciales (email_hash/auth_hash) para empleados activos y hardening en Employee.set_password para re-sincronizar email_hash automáticamente. |
| 2026-02-24 | 1.0174 | 1.0175 | Codex | pronto-libs (seed), pronto-scripts/pronto-root | Hardening de seeds init para autenticación de empleados: reconciliación idempotente de email/email_hash/auth_hash en seed canónico y uso de set_password en rutas de creación/actualización de empleados para evitar desincronización futura. |
| 2026-02-24 | 1.0175 | 1.0176 | Codex | pronto-libs (seed), pronto-scripts/pronto-root | Ajuste final en seeds de init: se eliminan asignaciones directas de auth_hash en seed legado interno y se unifica a Employee.set_password para garantizar sincronización de email_hash en toda ruta de seed. |
| 2026-02-24 | 1.0176 | 1.0177 | Codex | pronto-static (employees UI), pronto-scripts/pronto-root | Corrección visual de panel waiter: se completan estilos faltantes en WaiterBoard.vue y se elimina id legacy dashboard-metrics-root para evitar inyección visual anómala (círculo gigante). |
| 2026-02-24 | 1.0177 | 1.0178 | Codex | pronto-static (employees auth/router), pronto-scripts/pronto-root | Fix anti-loop en employees: redirección 401 ahora scoped a /<scope>/login con guard anti-reentrada, logout scoped, y guard del router evita fetchUser en rutas guest para reducir navegación abortada/NS_BINDING_ABORTED. |
| 2026-02-24 | 1.0178 | 1.0179 | Codex | pronto-employees (SSR template/context), pronto-scripts/pronto-root | Fix definitivo de loop por chunks en employees: cache-bust de main.js desacoplado de PRONTO_SYSTEM_VERSION del contenedor (asset_cache_bust por request) para evitar mezcla de chunks viejos/nuevos y NS_BINDING_ABORTED al navegar. |
| 2026-02-24 | 1.0179 | 1.0180 | Codex | pronto-employees (assets proxy), pronto-scripts/pronto-root | Fix de módulos ES en employees: assets_passthrough deja de redirigir a static cross-origin (9088) y ahora hace fallback server-side entre upstreams, evitando bloqueos CORS/Failed to load module y loop por NS_BINDING_ABORTED. |
| 2026-02-24 | 1.0180 | 1.0181 | Codex | pronto-employees (assets upstream fallback), pronto-scripts/pronto-root | Hardening final de proxy de assets: upstreams internos por defecto (static/pronto-static-1) y normalización de fallback localhost->host.docker.internal para evitar 502 en main.js/chunks desde contenedor employees. |
| 2026-02-24 | 1.0181 | 1.0182 | Codex | pronto-static (employees App init), pronto-scripts/pronto-root | Fix de race en inicialización employees: validación de auth secuencial previa a fetchConfig en rutas protegidas; corta init al detectar no autenticado para evitar abortos paralelos y rebotes percibidos como loop. |
| 2026-02-24 | 1.0182 | 1.0183 | Codex | pronto-static (employees router/app/vite), pronto-static assets build, pronto-employees runtime | Mitigación definitiva del loop en employees: eliminación de code-splitting en target employees (bundle único main.js), reemplazo de imports dinámicos en router/App y despliegue sin chunks para evitar fallos intermitentes de carga de módulos (NS_BINDING_ABORTED/Failed to load module). |
| 2026-02-24 | 1.0183 | 1.0184 | Codex | pronto-employees (SSR template/context), pronto-scripts/pronto-root | Fix de carga de módulo employees: se eliminó query cache-bust por request en main.js (timestamp variable), dejando src estable same-origin para evitar abortos/failed module intermitentes en Firefox durante navegación y auth redirects. |
| 2026-02-24 | 1.0184 | 1.0185 | Codex | pronto-employees (app routing guard), pronto-scripts/pronto-root | Hardening de acceso employees sin sesión: `/` y `/employees/*` ahora redirigen server-side a `/<scope>/login` si no hay JWT, evitando boot innecesario de SPA, `auth/me` abortado y warnings de no autenticado en vistas dashboard públicas. |
| 2026-02-24 | 1.0185 | 1.0186 | Codex | pronto-static (employees login redirect), pronto-static assets build, pronto-scripts/pronto-root | Fix de loop en employees: login de staff deja de redirigir a ruta legacy /employees/dashboard/* y usa ruta canónica /<scope>/dashboard/<view>, evitando rebotes entre aliases, abortos de /api/auth/me y warnings de no autenticado. |
| 2026-02-24 | 1.0186 | 1.0187 | Codex | pronto-libs (security_middleware), pronto-employees (asset proxy headers), pronto-scripts/pronto-root | Fix definitivo de loop/cache en employees: se desactiva cache immutable para `/assets/js/employees/*` y `/assets/css/employees/*` (no-store), evitando que Firefox ejecute bundles obsoletos y falle con `main.js`/`auth/me` abortado. |
| 2026-02-24 | 1.0187 | 1.0188 | Codex | pronto-static (employees App/auth store), pronto-static assets build, pronto-scripts/pronto-root | Hardening anti-loop final en employees: App fuerza redirect inmediato a `/<scope>/login` cuando `fetchUser` falla en ruta protegida (location.replace) y auth store deduplica `fetchUser` concurrente para evitar abortos por carreras entre router y bootstrap. |
| 2026-02-24 | 1.0188 | 1.0189 | Codex | pronto-employees (scope dashboards), pronto-scripts/pronto-root | Hardening server-side de dashboards por scope: cada ruta `/<scope>/dashboard` valida explícitamente `active_scope` antes de renderizar SPA y redirige a login si no coincide, evitando bootstrap de `main.js` en sesiones inválidas. |
| 2026-02-24 | 1.0189 | 1.0190 | Codex | pronto-employees (SSR current_user), pronto-static (employees auth bootstrap), pronto-static assets build, pronto-scripts/pronto-root | Eliminado fetch inicial innecesario de `auth/me` en dashboard cuando backend ya provee sesión: se inyecta `window.__PRONTO_CURRENT_USER__` desde SSR y el auth store arranca hidratado, reduciendo abortos/loops por navegación en carga inicial. |
| 2026-02-24 | 1.0190 | 1.0191 | Codex | pronto-static (employees router/login flow), pronto-static assets build, pronto-scripts/pronto-root | UX de rutas dashboard employees: ruta canónica de mesero pasa a `/<scope>/dashboard` (sin sufijo `/waiter`), login de staff redirige a esa ruta y se mantiene compatibilidad de URL legacy `.../dashboard/waiter` mediante redirect interno de router. |
| 2026-02-24 | 1.0191 | 1.0192 | Codex | pronto-employees (SSR assets manifest/template), pronto-scripts/pronto-root | Fix visual de employees dashboard: index SSR ahora inyecta explícitamente el CSS principal de Vite (`/assets/js/employees/assets/main-*.css`) resolviéndolo desde manifest, evitando render sin estilos cuando falla la carga/inyección CSS por runtime del módulo. |
| 2026-02-24 | 1.0192 | 1.0193 | Codex | pronto-static (employees waiter board layout), pronto-scripts/pronto-root | Ajuste de layout en tabla de órdenes de meseros: columnas con anchos controlados, truncado seguro de ID de orden y overflow/padding horizontal corregido para respetar el espacio derecho del módulo sin desbordes. |
| 2026-02-24 | 1.0193 | 1.0194 | Codex | pronto-static (employees waiter board layout), pronto-scripts/pronto-root | Refinamiento de tabla de órdenes de meseros: columnas en proporción responsive del ancho disponible, eliminación de padding interno que desplazaba el borde derecho y ajuste de celda de notas para mejor alineación con el layout del módulo. |
| 2026-02-24 | 1.0194 | 1.0195 | Codex | pronto-static (employees waiter board interactions), pronto-scripts/pronto-root | Reintegración de formularios flotantes legacy en tablero de meseros: click en ID de orden abre detalle de orden (con acción cancelar), click en mesa abre detalle de mesa, y cancelación ejecuta `/api/orders/<id>/cancel` con modal de motivo reutilizando IDs/estructura legacy (`order-floating-panel`, `cancel-order-modal`). |
| 2026-02-24 | 1.0195 | 1.0196 | Codex | pronto-static (employees waiter board layout), pronto-scripts/pronto-root | Ajuste de alto del frame de órdenes para evitar sobrecrecimiento en navegador: contenedor principal y tabla migrados a layout flex elástico (`height/min-height/flex`) usando el espacio real del módulo en lugar de `max-height` basado en viewport global. |
| 2026-02-24 | 1.0196 | 1.0197 | Codex | pronto-static (employees waiter board columns), pronto-scripts/pronto-root | Ajuste de columnas del tablero de meseros: alineación explícita de celdas/headers para evitar corrimiento visual a la derecha y abreviación de ID de orden en columna (`primeros 4 ... últimos 4`) manteniendo el valor completo en tooltip y en acciones. |
| 2026-02-24 | 1.0197 | 1.0198 | Codex | pronto-employees (api roles), pronto-static (employees tables/areas views), pronto-scripts/pronto-root | Fix de navegación Mesas/Áreas en scope waiter: habilitado acceso de lectura (`GET /api/tables`, `GET /api/areas`) para waiter/admin/system, manteniendo mutaciones en admin/system, y vistas Vue en modo solo lectura para waiter para evitar 403 + redirect forzado al hacer clic en módulos. |
| 2026-02-24 | 1.0198 | 1.0199 | Codex | pronto-libs (modifiers_service), pronto-employees (api modifiers), pronto-api (employees modifiers route), pronto-static (employees modifiers UI), pronto-scripts/pronto-root | Fix de Aditamentos y Modificadores: se corrigen campos legacy incompatibles con el modelo actual (UUID/display_order/min_selection), se normaliza serialización/validación de grupos y opciones, y el frontend consume `/api/modifiers/groups` para eliminar el error de carga de grupos. |
| 2026-02-24 | 1.0199 | 1.0200 | Codex | pronto-static (employees Header/DashboardView), pronto-scripts/pronto-root | Mejora visual y de layout en dashboard empleados: se eliminan alturas rígidas/estilos inline que recortaban elementos en cabecera, se ajusta espaciado y responsive para evitar cortes, y se rediseña el bloque “Panel de Operaciones” con jerarquía visual (eyebrow, subtítulo y badge de módulo activo). |
| 2026-02-24 | 1.0200 | 1.0201 | Codex | pronto-static (employees Sidebar/DashboardView), pronto-scripts/pronto-root | Mejora UX de selección en dashboard empleados: se corrige estado activo duplicado (Meseros quedaba activo por match parcial) usando `exact-active-class` en sidebar y tabs, y se rediseñan estados hover/active con pills más cómodas, mejor contraste y respuesta visual más limpia. |
| 2026-02-24 | 1.0201 | 1.0202 | Codex | pronto-static (employees), pronto-scripts/pronto-root | Corrección de errores de consola en dashboard empleados: se eliminaron directivas Tailwind runtime (`@tailwind`, `@apply`) en `employees/index.css`, se normalizaron URLs absolutas de assets a same-origin para mitigar ORB/CORS en imágenes de menú, y se agregó fallback robusto de imagen en `AdminProductCard` para evitar bloqueos de recursos. |
| 2026-02-24 | 1.0202 | 1.0203 | Codex | pronto-employees, pronto-static, pronto-scripts/pronto-root | Reapertura resuelta de errores de consola employees: canonicalización server-side de CSS hash legacy (`main-*.css` -> hash vigente del manifest), cache-bust de CSS SSR, y hardening de realtime polling (storage key por endpoint + reutilización singleton de clientes globales) para evitar ruido/duplicación en `/api/realtime/*`. |
| 2026-02-24 | 1.0203 | 1.0204 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Nuevo script operativo `pronto-abc.sh` en `pronto-scripts/bin` para ABC de datos de negocio (órdenes/sesiones/feedback/pagos/cancelaciones/estados/asignación de mesas/limpieza de áreas-mesas-aditamientos-productos-empleados-clientes/settings), con guardas de seguridad `dev/test`, `--yes` obligatorio y sin uso de DROP/TRUNCATE. |
| 2026-02-25 | 1.0204 | 1.0205 | Codex | pronto-employees, pronto-scripts/pronto-root | Fix de permisos en productos para scope waiter: `PUT /api/menu-items/<id>` permite a `waiter` actualizar únicamente `is_available` (disponibilidad) y bloquea otros campos, evitando redirección por 403 al usar el switch “Disponible” en `/waiter/dashboard/products`. |
| 2026-02-25 | 1.0205 | 1.0206 | Codex | pronto-static (employees tables/areas), pronto-scripts/pronto-root | Fix de panel Mesas en scope waiter: normalización defensiva de respuestas `/api/tables` y `/api/areas` (incluye payload anidado legacy `areas.data.areas`) para evitar crash `TypeError: ... id ... null` en `TableManager`, más alineación de tipos `TableRecord/AreaRecord` a `string | number` para compatibilidad UUID/legacy. |
| 2026-02-25 | 1.0206 | 1.0207 | Codex | pronto-static (employees areas/tables), pronto-scripts/pronto-root | Hardening adicional de vistas waiter Áreas/Mesas: se agrega `safeAreas` en frontend para filtrar elementos nulos antes de render (`v-for :key=\"area.id\"`), eliminando crash residual `can't access property \"id\", b is null` al abrir `/waiter/dashboard/areas`. |
| 2026-02-25 | 1.0207 | 1.0208 | Codex | pronto-static (employees sessions/payment), pronto-scripts/pronto-root | Fix de vista Sesiones en scope waiter: `SessionsBoard` corrige `watch` faltante (runtime error), cambia fuente de sesiones activas a `/api/sessions/all` con parser robusto de payload y refresco periódico, y alinea tipos UUID para `sessionId` en `SessionCard` y `PaymentFlow`. |
| 2026-02-25 | 1.0208 | 1.0209 | Codex | pronto-static (employees sidebar UX), pronto-scripts/pronto-root | Ajuste de foco visual en sidebar de dashboard: separación de estilo entre `Navegación principal` y `Módulos de operación`; el foco fuerte se reserva a módulos, y `Tablero Principal` usa estado suave cuando solo existe una navegación principal para evitar doble resaltado con módulos activos. |
| 2026-02-25 | 1.0209 | 1.0210 | Codex | pronto-static (employees tables UI), pronto-scripts/pronto-root | Rediseño de visualización de áreas/mesas en `/waiter/dashboard/tables`: se elimina vista gráfica tipo canvas/zoom/drag y se reemplaza por diseño simple y limpio por secciones de área con tarjetas de mesa, filtros por área (incluye “Sin área”), métricas visibles y acciones CRUD preservadas para admin/system. |
| 2026-02-25 | 1.0210 | 1.0211 | Codex | pronto-static (employees dashboard labels), pronto-scripts/pronto-root | Renombre de tab `sessions` en dashboard para mayor coherencia con su contenido de caja: etiqueta actualizada a `Caja y Cierres` en navegación superior de `/waiter/dashboard` y en configuración de módulos del shell employees, manteniendo intacta la ruta `/dashboard/sessions`. |
| 2026-02-25 | 1.0211 | 1.0212 | Codex | pronto-static (employees status labels), pronto-docs (error tracking), pronto-scripts/pronto-root | Corrección de etiqueta de estado en Caja y Cierres: se reemplaza `Consumo activo` por etiquetas canónicas (`open`=`Cuenta abierta`, `active`=`Cuenta activa`, etc.) en `SessionCard` y `CashierBoard`; además se documenta y cierra el incidente `ERR-20260225-SESSION-STATUS-LABEL` en flujo de errores/resueltos. |
| 2026-02-25 | 1.0212 | 1.0213 | Codex | pronto-api (employees menu_items), pronto-libs (menu_validation), pronto-docs (error tracking), pronto-scripts/pronto-root | Fix de disponibilidad de productos para mesero: `PUT /api/menu-items/<id>` ahora permite scopes `waiter|admin|system` con restricción de campos para waiter (`is_available` únicamente) y validación de update parcial en `MenuValidator` sin requerir `category`; incidente cerrado `ERR-20260225-WAITER-MENU-AVAILABILITY-FORBIDDEN`. |
| 2026-02-25 | 1.0213 | 1.0214 | Memo | pronto-libs, pronto-api, pronto-employees, pronto-static, pronto-docs | Multi-console authentication completo: cookies namespaced `access_token_{scope}` por consola, extractor JWT endurecido sin fallback legacy, proxy técnico `/<scope>/api/*` en employees (deprecated), frontend scope-aware con `apiUrl()`, logout scoped que solo borra cookies del scope actual. |
| 2026-02-25 | 1.0214 | 1.0215 | Memo | pronto-libs, pronto-api, pronto-client, pronto-docs | Corrección arquitectónica de autenticación de clientes: cookie renombrada de `access_token` a `pronto_client_session` (BUG-003), eliminada propagación de cookie JWT en orders.py (BUG-002), token extractor soporta client session cookie, documentación canónica en `AUTH_ARCHITECTURE.md`. Clientes usan Session+Redis (NO JWT), empleados usan JWT Stateless. |
| 2026-02-26 | 1.0215 | 1.0216 | Amp | pronto-libs, pronto-client, pronto-static | Limpieza frontend legacy: eliminados `pagination.js` y `reports.js` (reemplazados por Vue `usePagination.ts` y `ReportsManager.vue`), removida referencia en `_dashboard_scripts.html`, eliminados 4 inline scripts de `base.html` (QA fixes → `domCleanup.ts`, shortcuts modal → `useShortcutsModal.ts`, Lucide init y cart fallback → `base.ts`). Solo quedan inline scripts de inyección de datos Jinja2→JS. |
| 2026-02-26 | 1.0216 | 1.0217 | Amp | pronto-client, pronto-static | Fix placeholder path (`branding/placeholder.png` → `icons/placeholder.png`), fix logo extension (`logo.png` → `logo.jpg` en BrandingSettings.vue), reemplazo masivo de 47 `alert()` bloqueantes por `window.showToast?.()` no-bloqueante en 24 archivos de employees Vue. |
| 2026-03-03 | 1.0217 | 1.0218 | Codex | pronto-static (employees sidebar), pronto-docs, pronto-scripts/pronto-root | Rediseño del sidebar compartido para admin/empleados: se eliminan estilos azules hardcodeados, el menú lateral ahora deriva fondo/hover/activos de `brand_color_primary` y `brand_color_secondary`, y se sincroniza el versionado root junto con el incidente documentado en error tracker. |
| 2026-03-03 | 1.0218 | 1.0219 | Codex | pronto-static (employees admin dashboard/sidebar), pronto-docs, pronto-scripts/pronto-root | Reorganización de navegación en admin: el sidebar deja solo `Dashboard` como contenedor de tableros/gestión, `Meseros/Cocina/Caja` pasan a tabs del dashboard, gestión permanece como tabs superiores y el tab principal se renombra a `Tablero`. |
| 2026-03-03 | 1.0219 | 1.0220 | Codex | pronto-static (employees admin shell), pronto-docs, pronto-scripts/pronto-root | Hardening del shell admin en rutas internas: `Sidebar` y `DashboardView` resuelven el contexto `admin/system` de forma consistente en tabs como `Productos`, ocultando módulos laterales heredados y preservando el mismo layout visual del dashboard. |
| 2026-03-03 | 1.0220 | 1.0221 | Codex | pronto-static (employees admin scope resolution), pronto-docs, pronto-scripts/pronto-root | Ajuste final del shell admin: `App.vue` prioriza el `app_context` SSR para evitar que vistas internas como `Productos/Aditamientos/Mesas/Áreas/Caja y Cierres` reintroduzcan menú lateral duplicado; la navegación queda persistente por tabs. |
| 2026-03-03 | 1.0221 | 1.0222 | Codex | pronto-static (employees admin employees view), pronto-docs, pronto-scripts/pronto-root | Se conecta la ruta `/admin/dashboard/employees` a la vista administrativa existente `EmployeesManager.vue` para mostrar el ABC de personal en el panel derecho y se habilitan acciones admin/system aun cuando el bootstrap no incluya permisos explícitos. |
| 2026-03-03 | 1.0222 | 1.0223 | Codex | pronto-static (employees sidebar), pronto-docs, pronto-scripts/pronto-root | Ajuste puntual del sidebar admin: al entrar a `Empleados` y otras rutas administrativas se oculta de forma forzada el bloque `Módulos de operación`, evitando duplicidad con la navegación administrativa principal. |
| 2026-03-03 | 1.0223 | 1.0224 | Codex | pronto-static (employees roles/rbac), pronto-docs, pronto-scripts/pronto-root | La vista `Roles y Permisos` se alinea con el RBAC real: `use-rbac` consume el contrato actual del backend, el editor crea/actualiza roles con `name/display_name/description` y permisos reales, y el panel documenta roles canónicos, funciones y control de acceso efectivo para empleados, admin y system. |
| 2026-03-03 | 1.0224 | 1.0225 | Codex | pronto-static (employees sidebar/admin shell), pronto-docs, pronto-scripts/pronto-root | Reapertura de shell administrativo en `Roles y Permisos`: `Sidebar.vue` ahora detecta rutas administrativas por scope, route name y pathname, y filtra internamente las secciones a solo `ADMIN`, evitando que se rendericen módulos operativos en `/admin/dashboard/roles`. |
| 2026-03-03 | 1.0225 | 1.0226 | Codex | pronto-static (employees admin config), pronto-docs, pronto-scripts/pronto-root | Fix de carga en `Configuración del Sistema`: `SystemSettings.vue` deja de leer `res.configs` y ahora consume el shape real `data.configs` de `/api/config`, con fallback defensivo a shape legacy, restaurando la renderización de parámetros en `/admin/dashboard/config`. |
| 2026-03-03 | 1.0226 | 1.0227 | Codex | pronto-static (employees dashboard shell), pronto-docs, pronto-scripts/pronto-root | Fix de scroll en vistas largas del dashboard: `DashboardView.vue` habilita scroll vertical en el contenedor compartido del módulo, resolviendo el bloqueo de desplazamiento en `/admin/dashboard/modifiers` y otras pantallas extensas. |
| 2026-03-03 | 1.0227 | 1.0228 | Codex | pronto-static (employees dashboard tabs), pronto-docs, pronto-scripts/pronto-root | Ajuste de tabs en `/admin/dashboard`: `DashboardView.vue` separa tabs administrativos de tabs operativos y selecciona el grupo correcto según la vista actual, haciendo que `Reportes` y `Configuración` muestren sus propias opciones administrativas. |
| 2026-03-03 | 1.0228 | 1.0229 | Codex | pronto-static, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Hardening final de `Roles y Permisos`: `App.vue` deja de redirigir rutas hijas válidas al `dashboard-index`, el frontend RBAC usa el endpoint real `/api/roles/roles`, `EmployeesManager` alinea la carga de roles, y el shell SSR corrige el link principal de CSS usando `assets_css_employees`, eliminando el error de MIME y estabilizando tabs/estilos/scroll en `/admin/dashboard/roles`. |
| 2026-03-03 | 1.0229 | 1.0230 | Codex | pronto-static (employees admin employees view), pronto-docs, pronto-scripts/pronto-root | Rediseño visual de `/admin/dashboard/employees`: `EmployeesManager.vue` deja de depender de clases utilitarias inexistentes y adopta CSS scoped real con encabezado administrativo, métricas, buscador, tabla de personal y modal consistente con el shell de administración. |
| 2026-03-03 | 1.0230 | 1.0231 | Codex | pronto-static, pronto-libs, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Cierre funcional de `/admin/dashboard/employees`: `EmployeesManager.vue` corrige el parseo de roles con el wrapper HTTP, `employee_service.list_employees()` deja de ordenar por la columna inexistente `name_encrypted`, y el shell SSR de `employees` vuelve a resolver el CSS principal con `assets_js_employees`, eliminando el `500` de `/admin/api/employees` y los MIME errors de estilos en runtime. |
| 2026-03-03 | 1.0231 | 1.0232 | Codex | pronto-static (employees cashier board), pronto-docs, pronto-scripts/pronto-root | Rediseño visual de `/admin/dashboard/cashier`: `CashierBoard.vue` deja de renderizarse sin estilos y ahora usa CSS scoped real para toolbar, buscador, métricas, tabla y badges de pago, validado en runtime sin errores de consola. |
| 2026-03-03 | 1.0232 | 1.0233 | Codex | pronto-static (employees admin navigation), pronto-docs, pronto-scripts/pronto-root | Reorganización de navegación en `/admin`: `Cocina` y `Caja` salen del tabstrip del tablero principal, pasan a la navegación principal lateral, y `DashboardView.vue` separa tabs secundarios por contexto (`Tablero`, `Cocina`, `Caja` y vistas administrativas). |
| 2026-03-03 | 1.0233 | 1.0234 | Codex | pronto-static (employees dashboard tabs), pronto-docs, pronto-scripts/pronto-root | Ajuste puntual de copy en el tablero administrativo: el tab de la ruta `waiter` deja de mostrarse como `Tablero` y ahora se etiqueta como `Mesero`, sin modificar rutas ni estructura. |
| 2026-03-03 | 1.0234 | 1.0235 | Codex | pronto-static (employees waiter/kitchen), pronto-docs, pronto-scripts/pronto-root | Fix del modal de mesa en `/admin/dashboard/waiter` y paridad en cocina: la resolución de mesas deja de depender solo de `/api/table-assignments/my-tables` y ahora mezcla también `/api/tables`, permitiendo que admin/system encuentren zona, estado y capacidad de la mesa real asociada a la orden. |
| 2026-03-03 | 1.0235 | 1.0236 | Codex | pronto-static (employees waiter board), pronto-docs, pronto-scripts/pronto-root | Rediseño del tab `Mesas` compartido por `/admin` y `/waiter`: se corrige el cálculo de totales para evitar `$NaN`, se enriquece el agrupado con área/color reales desde el catálogo de mesas y se rehacen las tarjetas para mostrar zona, total y estados por orden en un layout amplio y legible. |
| 2026-03-03 | 1.0236 | 1.0237 | Codex | pronto-static (employees admin roles), pronto-docs, pronto-scripts/pronto-root | Simplificación de `Roles y Permisos` al modelo real del proyecto: la vista queda solo en modo consulta para roles canónicos, desaparece la UI de roles personalizados/CRUD y las tarjetas canónicas se renderizan en modo solo lectura. |
| 2026-03-03 | 1.0237 | 1.0238 | Codex | pronto-static (employees admin employees), pronto-docs, pronto-scripts/pronto-root | Corrección del parseo de roles en `EmployeesManager`: la vista de empleados deja de esperar un shape legacy (`data.roles`) y ahora unwrapea el envelope real de `/api/roles/roles?include_inactive=true`, eliminando el fallback falso y el error de consola sobre formato inesperado. |
| 2026-03-03 | 1.0238 | 1.0239 | Codex | pronto-static (employees admin employees), pronto-docs, pronto-scripts/pronto-root | Corrección completa de `EmployeesManager`: la vista ahora entiende el payload paginado real de `/api/employees`, refleja el personal existente en `/admin/dashboard/employees`, expone acciones de `Editar`, `Activar/Desactivar` y `Eliminar`, y se validó en runtime tras reiniciar `pronto-employees-1`. |
| 2026-03-03 | 1.0239 | 1.0240 | Codex | pronto-static (employees admin config), pronto-employees (SSR config API), pronto-docs, pronto-scripts/pronto-root | Reapertura de `Configuración del Sistema`: `pronto-employees` deja de exponer el mapa plano legacy en `/admin/api/config` y vuelve al contrato canónico `data.configs[]`; en paralelo `SystemSettings.vue` mantiene compatibilidad defensiva con ambos shapes para que `/admin/dashboard/config` vuelva a renderizar parámetros reales sin depender de un único formato. |
| 2026-03-03 | 1.0240 | 1.0241 | Codex | pronto-static (employees admin/shared reports, employees manager), pronto-libs (analytics), pronto-docs, pronto-scripts/pronto-root | Corrección de errores de consola en `employees`: `EmployeesManager` normaliza envelopes de roles sin caer en fallback falso; `ReportsManager` deja de usar `/api/reports` inexistente y consume los endpoints reales de analytics con tolerancia a fallas parciales; `employee_analytics` deja de depender de `Employee.name_encrypted`, restaurando `waiter-tips` en admin. |
| 2026-03-03 | 1.0241 | 1.0242 | Codex | pronto-static (employees kitchen board), pronto-docs, pronto-scripts/pronto-root | Revisión de `Cocina` con criterio operativo homogéneo: `KitchenBoard.vue` deja de duplicar órdenes `ready` entre `Activas` y `Seguimiento`, alinea el badge de `Activas` con el dataset realmente visible y preserva el tab `Mesas` usando órdenes en curso + listas para no perder contexto de mesa. |
| 2026-03-03 | 1.0242 | 1.0243 | Codex | pronto-static (employees dashboard shell/sidebar), pronto-docs, pronto-scripts/pronto-root | Ajuste del shell admin para tabs persistentes de mesero: `Productos` y `Aditamientos` dejan de activar el grupo de `Cocina` en `/admin` y permanecen en el grupo `Mesero/Productos/Aditamientos/Mesas/Áreas`; además el acceso principal del lateral se renombra de `Dashboard` a `Meseros`. |
| 2026-03-03 | 1.0243 | 1.0244 | Codex | pronto-static (employees kitchen shell), pronto-docs, pronto-scripts/pronto-root | Separación real entre tabs de `Cocina` y `Meseros`: se crean rutas dedicadas `kitchen-products` y `kitchen-modifiers`, `DashboardView.vue` preserva el grupo `Cocina/Productos/Aditamientos` al navegar, y el sidebar mantiene activo `Cocina` en `/admin/dashboard/kitchen/products` y `/admin/dashboard/kitchen/modifiers`. |
| 2026-03-03 | 1.0244 | 1.0245 | Codex | pronto-static (employees router guard), pronto-docs, pronto-scripts/pronto-root | Hardening de rutas legacy de cocina: el guard compartido redirige navegación heredada desde `Cocina` hacia `products/modifiers` al namespace correcto `kitchen-products/kitchen-modifiers`, evitando que bundles cacheados o enlaces viejos reactiven el foco de `Meseros`. |
| 2026-03-03 | 1.0245 | 1.0246 | Codex | pronto-employees, pronto-api, pronto-docs, pronto-scripts/pronto-root | Enforcement de desactivación de empleados en autenticación: todas las rutas SSR de login por consola (`waiter`, `chef`, `cashier`, `admin`, `system`) ahora rechazan `employee.is_active = false`, y `auth/me` en employees/api también invalida sesiones activas de cuentas desactivadas. |
| 2026-03-03 | 1.0246 | 1.0247 | Codex | pronto-employees (console proxy CSRF), pronto-docs, pronto-scripts/pronto-root | Corrección del borrado de empleados en admin: el proxy SSR de `pronto-employees` deja de validar mutaciones JSON contra `request.form`/cookie legacy y usa `validate_csrf()` con el token real del dashboard, eliminando el falso `403 CSRF_ERROR` que redirigía a `authorization-error` al usar `Eliminar` en `/admin/dashboard/employees`. |
| 2026-03-03 | 1.0247 | 1.0248 | Codex | pronto-static (employees admin UI), pronto-docs, pronto-scripts/pronto-root | Ajuste visual del panel de empleados: el botón `Editar` en `/admin/dashboard/employees` adopta un estilo primario con fondo, borde y hover perceptibles para que se identifique claramente como acción clickeable junto a `Desactivar` y `Eliminar`. |
| 2026-03-03 | 1.0248 | 1.0249 | Codex | pronto-static (employees admin employees modal), pronto-docs, pronto-scripts/pronto-root | Corrección del modal de edición de empleados: `EmployeesManager.vue` deja de depender de un listado vacío de `/api/roles/roles`, fusiona siempre los roles canónicos del sistema y preserva el rol actual del empleado al abrir `Editar`, restaurando el combo de `Rol` y su preselección en `/admin/dashboard/employees`. |
| 2026-03-03 | 1.0249 | 1.0250 | Codex | pronto-static (employees admin scope-aware employee management), pronto-api (employees scope guard in code), pronto-docs, pronto-scripts/pronto-root | Ajuste de scope en la administración de empleados: `/admin/dashboard/employees` ahora oculta cuentas `system`, limita el combo de `Rol` a `waiter/chef/cashier/admin`, preselecciona correctamente los roles en alta/edición y reserva la gestión de `system` para `/system`; además se dejó guard backend en código para rechazar operaciones `system` desde scope `admin`. |
| 2026-03-03 | 1.0250 | 1.0251 | Codex | pronto-static (employees admin roles UI), pronto-docs, pronto-scripts/pronto-root | Simplificación final de `Roles y Permisos`: se elimina el bloque “Roles canónicos configurados en el sistema” y su estado vacío asociado, dejando la vista alineada con el modelo real sin roles personalizados ni inventario dinámico renderizado en esa sección. |
| 2026-03-03 | 1.0251 | 1.0252 | Codex | pronto-static (employees admin dashboard tabs), pronto-docs, pronto-scripts/pronto-root | Ajuste puntual del shell administrativo: `/admin/dashboard/employees` deja de renderizar el grupo completo `Empleados/Roles/Reportes/Configuración` y ahora muestra solo el tab `Empleados`, manteniendo la vista alineada con la intención de navegación de ese módulo. |
| 2026-03-03 | 1.0252 | 1.0253 | Codex | pronto-static (employees admin dashboard tabs), pronto-docs, pronto-scripts/pronto-root | Ajuste puntual del shell administrativo: `/admin/dashboard/roles` deja de renderizar el grupo completo `Empleados/Roles/Reportes/Configuración` y ahora muestra solo el tab `Roles`, manteniendo la vista alineada con la intención de navegación de ese módulo. |
| 2026-03-03 | 1.0253 | 1.0254 | Codex | pronto-static (employees admin dashboard tabs), pronto-docs, pronto-scripts/pronto-root | Ajuste puntual del shell administrativo: `/admin/dashboard/reports` deja de renderizar el grupo completo `Empleados/Roles/Reportes/Configuración` y ahora muestra solo el tab `Reportes`, manteniendo la vista alineada con la intención de navegación de ese módulo. |
| 2026-03-03 | 1.0254 | 1.0255 | Codex | pronto-static (employees admin dashboard tabs), pronto-docs, pronto-scripts/pronto-root | Ajuste puntual del shell administrativo: `/admin/dashboard/config` deja de renderizar el grupo completo `Empleados/Roles/Reportes/Configuración` y ahora muestra solo el tab `Configuración`, manteniendo la vista alineada con la intención de navegación de ese módulo. |
| 2026-03-03 | 1.0255 | 1.0256 | Codex | pronto-static (employees admin config UI), pronto-docs, pronto-scripts/pronto-root | Mejora de usabilidad en configuración de negocio: cada parámetro en `/admin/dashboard/config` ahora expone un botón explícito `Editar` por opción y entra en modo de edición con acciones `Cancelar`/`Guardar`, en lugar de mostrar solo `Guardar` permanentemente. |
| 2026-03-03 | 1.0256 | 1.0257 | Codex | pronto-static (employees admin config color picker), pronto-docs, pronto-scripts/pronto-root | Mejora de edición de colores en configuración: los parámetros de color de `/admin/dashboard/config` ahora muestran un color picker al entrar en modo `Editar`, manteniendo el valor hexadecimal visible y sincronizado para `brand_color_primary`, `brand_color_secondary` y opciones equivalentes. |
| 2026-03-03 | 1.0257 | 1.0258 | Codex | pronto-static (employees admin config seconds validation), pronto-docs, pronto-scripts/pronto-root | Validación semántica de parámetros en segundos: las opciones `*_seconds` de `/admin/dashboard/config` ahora se tratan como segundos reales, aceptando solo enteros entre `1` y `3600`, con `step=1` y mensaje de error cuando el valor no es coherente con esa unidad. |
| 2026-03-03 | 1.0258 | 1.0259 | Codex | pronto-static (employees admin config currency selector), pronto-docs, pronto-scripts/pronto-root | Mejora de edición de moneda en configuración: `currency_code` deja de ser texto libre y ahora se edita con un combo de monedas canónicas (`MXN`, `USD`, `EUR`, `CAD`), normalizando el valor ISO a mayúsculas y validando que solo se guarden opciones permitidas. |
| 2026-03-03 | 1.0259 | 1.0260 | Codex | pronto-static (employees admin config currency symbol selector), pronto-docs, pronto-scripts/pronto-root | Mejora de edición del símbolo de moneda en configuración: `currency_symbol` deja de ser texto libre y ahora se edita con un combo de símbolos canónicos, validando que solo se guarden opciones permitidas para la configuración administrativa. |
| 2026-03-03 | 1.0260 | 1.0261 | Codex | pronto-static (employees admin config font catalog), pronto-docs, pronto-scripts/pronto-root | Mejora de edición tipográfica en configuración: se crea el catálogo estático `assets/fonts/catalog.json` como fuente de verdad para fuentes seleccionables y `font_family_body` / `font_family_headings` pasan a editarse con combo controlado basado en ese catálogo. |
| 2026-03-03 | 1.0261 | 1.0262 | Codex | pronto-employees (admin config SSR API), pronto-libs (business config service), pronto-docs, pronto-scripts/pronto-root | Corrección de persistencia de configuración en `/admin`: los cambios sobre parámetros como `orders.show_estimated_time` vuelven a guardarse en la fila canónica de `pronto_system_settings` usando UUID, y el servicio deja de intentar escribir `updated_by` con UUID sobre una columna integer legacy. Validado end-to-end con cambio real en UI y verificación directa en PostgreSQL. |
| 2026-03-03 | 1.0262 | 1.0263 | Codex | pronto-static (employees admin config logo picker), pronto-docs, pronto-scripts/pronto-root | Mejora de edición del logo en configuración administrativa: `restaurant_logo_url` deja de capturarse como texto libre y ahora usa file picker conectado al upload canónico de branding, colocando la ruta devuelta por el backend en el campo de configuración. |
| 2026-03-03 | 1.0263 | 1.0264 | Codex | pronto-libs (table service, models), pronto-employees, pronto-docs, pronto-scripts/pronto-root | Corrección de creación de mesas y aplicación de `table_base_prefix`: `Table` vuelve a crear registros usando el UUID generado por PostgreSQL y `TableService` transforma números simples en códigos persistidos con el prefijo base configurado. Validado end-to-end cambiando temporalmente el prefijo a `Z`, creando una mesa por `/admin/api/tables` y verificando en PostgreSQL el valor `A-Z98`, con restauración posterior del prefijo a `M`. |
| 2026-03-03 | 1.0264 | 1.0265 | Codex | pronto-static (employees pagination), pronto-employees (public config), pronto-api (public config), pronto-docs, pronto-scripts/pronto-root | Corrección de paginación por defecto: `system.api.items_per_page` vuelve a gobernar el bootstrap real de `window.APP_CONFIG.items_per_page`, las capas compartidas de paginación dejan de depender de defaults hardcodeados y el store prioriza la llave canónica sobre `items_per_page` legacy. Validado en navegador sin `localStorage`: al subir temporalmente el parámetro a `25`, `APP_CONFIG.items_per_page` reflejó `25`; al restaurarlo a `10`, volvió a `10`. |
| 2026-03-03 | 1.0265 | 1.0266 | Codex | pronto-employees (SSR public config), pronto-libs (business config service), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Validación integral de parámetros configurados en `/admin/dashboard/config`: se corrigió el bootstrap público SSR para exponer `restaurant_name` y `waiter_can_collect`, la capa compartida de config ahora normaliza `boolean` igual que `bool`, y se consolidó un suite live en `pronto-tests` que muta todos los parámetros seteados, verifica persistencia en PostgreSQL y restaura sus valores originales; validado con `2 passed`. |
| 2026-03-03 | 1.0266 | 1.0267 | Codex | pronto-employees (SSR config scope filter), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección del panel de configuración en `admin`: `/{scope}/api/config` ahora filtra parámetros por scope real de consola usando `X-App-Context`, evitando que `/admin/dashboard/config` muestre llaves `system.*` cuyos botones `Guardar` terminaban en 403. Se añadió cobertura live para inventario scope-aware y se endureció el helper de login del suite frente a restarts; validado con `3 passed`. |
| 2026-03-03 | 1.0267 | 1.0268 | Codex | pronto-static (employees waiter/kitchen/cashier boards), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Restauración del botón `Filtros` y del formulario flotante en los tableros operativos de mesero, cocina y caja, incluyendo su uso desde `/admin` al reutilizar los mismos componentes Vue. Se añadió `FloatingFiltersModal.vue`, persistencia local por tablero y un spec de Playwright que valida la apertura del panel en los tres tableros administrativos; validado con `npm run build:employees` y `1 passed` en `operational-filters.spec.ts`. |
| 2026-03-03 | 1.0268 | 1.0269 | Codex | pronto-static (employees waiter board mesas tab), pronto-docs, pronto-scripts/pronto-root | Corrección visual de las órdenes dentro de las tarjetas de mesa en el tab `Mesas` del tablero de mesero: cada orden ahora usa segmentos alineados para código, estado y total, con layout de grid estable y fallback responsivo. Validado con `npm run build:employees`. |
| 2026-03-03 | 1.0269 | 1.0270 | Codex | pronto-static (employees auth/runtime/config bootstrap), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección de `authorization-error` espurio en `/waiter` tras salir de `admin`: el shell deja de pedir `/api/config` desde scopes no autorizados, el runtime autenticado apaga realtime/notificaciones al entrar a rutas guest/error, y el cliente realtime mantiene el scope correcto en `/<scope>/api/realtime/*`. Se añadió cobertura Playwright para el bootstrap waiter y se verificó manualmente que `/waiter` entra al dashboard con sesión waiter válida y redirige a `/waiter/login` sin sesión. |
| 2026-03-03 | 1.0270 | 1.0271 | Codex | pronto-static (employees authorization error UX), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección de la acción `Ir a Login` desde `authorization-error`: ahora limpia primero la sesión del scope actual vía `/${scope}/logout` antes de aterrizar en el login, evitando que una cookie stale relance el loop hacia `/waiter/dashboard` y termine otra vez en 403. Se agregó cobertura Playwright para el botón y se validó manualmente dentro de la SPA. |
| 2026-03-03 | 1.0271 | 1.0272 | Codex | pronto-static (employees scoped auth error routing), pronto-employees (scoped SSR auth error routes), pronto-libs (scope guard public error path), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección del loop de autorización en `chef` y `cashier`: los redirects 403 ahora apuntan al `authorization-error` del scope actual, el SSR expone `/<scope>/authorization-error` en todas las consolas, `App.vue` trata esa vista como guest para no disparar `auth/me`, y la ruta de error queda pública en los guards scoped. Se añadió cobertura Playwright para `chef` y `cashier`, validando que `Ir a Login` aterriza en `/<scope>/login`. |
| 2026-03-03 | 1.0272 | 1.0273 | Codex | pronto-static (employees admin runtime, reports), pronto-docs, pronto-scripts/pronto-root | Validación funcional de `/admin` y corrección de dos fallas bloqueantes: el shell administrativo deja de disparar realtime operativo en loop contra `/admin/api/realtime/*`, y `Reportes` carga `Chart.js` desde el asset local con destrucción correcta de instancias al reconsultar. Validado en navegador real sobre `/admin/dashboard/employees`, `/admin/dashboard/reports` y `/admin/dashboard/cashier`, sin warnings ni `504`. |
| 2026-03-03 | 1.0273 | 1.0274 | Codex | pronto-static (employees header/app logout), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección del logout colgado en consolas de empleados: `Cerrar Sesión` ahora navega con `href` real al logout scopeado y el shell detiene el runtime autenticado antes de salir, evitando que el dashboard quede pensando sin aterrizar en login. Validado manualmente desde el menú de usuario en `/waiter/dashboard` y con Playwright sobre `waiter-scope-runtime.spec.ts` (`4 passed`). |
| 2026-03-03 | 1.0274 | 1.0275 | Codex | pronto-static (employees app shell), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección del shell faltante tras login SPA en las consolas de empleados: la detección de rutas guest/login pasó a depender de `route.path` reactivo en lugar de `window.location.pathname`, permitiendo que el sidebar y el header se monten inmediatamente después del `router.push()` del login sin requerir refresh manual. Validado con `npm run build:employees` y `5 passed` en `waiter-scope-runtime.spec.ts`. |
| 2026-03-03 | 1.0275 | 1.0276 | Codex | pronto-static (employees sessions board), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Mejora visual del selector `Cuentas por Pagar / Órdenes Cerradas` en `Caja y Cierres`: el tab activo ahora tiene contraste, elevación y badges claros, mientras el inactivo conserva hover y borde visibles. Aplica a todas las consolas que reutilizan `SessionsBoard`. Validado con `npm run build:employees` y `1 passed` en `sessions-tabs.spec.ts`. |
| 2026-03-03 | 1.0276 | 1.0277 | Codex | pronto-static (employees sidebar), pronto-docs, pronto-scripts/pronto-root | Limpieza del sidebar de `waiter`: se eliminó la entrada redundante `Meseros` del bloque `Módulos de operación > Operación`, manteniendo ese acceso solo en la navegación principal. El cambio afecta únicamente el render visual del scope `waiter` y fue validado con `npm run build:employees`. |
| 2026-03-03 | 1.0277 | 1.0278 | Codex | pronto-static (employees waiter shell tabs/sidebar), pronto-docs, pronto-scripts/pronto-root | Ajuste del shell de `waiter`: el lateral deja de mostrar módulos de gestión y la navegación secundaria queda solo en los tabs superiores; además se agregó `Caja y Cierres` al grupo de tabs de mesero. Validado con `npm run build:employees` y comprobación visual en `/waiter/dashboard/waiter`. |
| 2026-03-03 | 1.0279 | 1.0280 | Codex | pronto-static, pronto-api, pronto-libs, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se implementó la edición de perfil en el menú de usuario de todas las consolas employees (`:6081`) con modal visual para datos de contacto y cambio de contraseña, persistiendo en `pronto_employee_preferences` (`key=profile`) y bloqueando la modificación del email asignado por admin; además se agregó seed init idempotente para cargar preferencias base en primer uso. |
| 2026-03-03 | 1.0278 | 1.0279 | Codex | pronto-static (employees sessions naming), pronto-docs, pronto-scripts/pronto-root | Renombrado global del copy `Control de Caja y Cierres` a `Control de Caja`, y ajuste de la etiqueta de navegación/tabs `Caja y Cierres` a `Caja` en el shell compartido de empleados. Validado con `npm run build:employees`. |
| 2026-03-03 | 1.0280 | 1.0281 | Codex | pronto-employees (orders request-payment), pronto-static (employees realtime), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección del flujo de acción en `waiter`: `Solicitar pago` vuelve a funcionar usando el servicio canónico de waiter calls y la mesa asociada a la sesión, mientras el polling realtime scope-aware baja su `timeout_ms` a 4500 para evitar `504` bajo el proxy SSR de 5s. Validado con `npm run build:employees`, prueba real contra `/waiter/api/orders/:id/request-payment` y `6 passed` en `waiter-scope-runtime.spec.ts`. |
| 2026-03-04 | 1.0282 | 1.0283 | Codex | pronto-static (employees realtime/direct endpoint), pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección final del runtime de `waiter`: el long-poll realtime deja de pasar por el proxy scopeado `/<scope>/api/realtime/*` y consume `/api/realtime/*` directo contra el mismo origen con `X-App-Context`, eliminando los `504` por self-proxy en long-poll; además se endurece el módulo legacy `employee-events.ts` para no arrancar fuera de su root y se actualiza la cobertura Playwright del flujo `waiter` (`6 passed`). |
| 2026-03-04 | 1.0281 | 1.0282 | Codex | pronto-static, pronto-api, pronto-employees, pronto-libs, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Flujo completo de llamada a administrador con acuse: se persisten solicitudes `admin_call` como notificaciones pendientes para `admin/system`, el panel administrativo puede marcarlas como leídas (`/api/notifications/admin/confirm/:id`) y el empleado solicitante recibe notificación realtime de confirmación; además se reforzó el gating del botón de campana cliente para requerir login + mesa activa y se agregó prueba Playwright `admin-call-confirmation.spec.ts` del flujo waiter→admin→confirmación. |
| 2026-03-04 | 1.0283 | 1.0284 | Codex | pronto-employees, pronto-docs, pronto-scripts/pronto-root | Corrección del modal `Editar perfil` en consolas de empleados: el bootstrap SSR ahora hidrata email/rol/nombre faltantes desde DB cuando el JWT no trae PII completa y `/employees/me/preferences` vuelve a usar el servicio compartido canónico para devolver `assigned_email`. Validado por API en `/waiter/api/employees/me/preferences` y visualmente en el modal de perfil de `/waiter/dashboard`. |
| 2026-03-04 | 1.0284 | 1.0285 | Codex | pronto-employees, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Validación integral del modal `Editar perfil` en `/waiter/dashboard`: se detectó que faltaba `PUT /employees/me/password`, se implementó el endpoint canónico en `pronto-employees` y se agregó un spec Playwright que valida email asignado, persistencia de perfil, cambio de contraseña y restauración completa al estado original (`1 passed`). |
| 2026-03-04 | 1.0285 | 1.0286 | Codex | pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Fix de rutas de `authorization-error` en `chef/cashier`: el interceptor HTTP ya no redirige a login cuando un `401` ocurre estando en `/<scope>/authorization-error`, permitiendo renderizar la pantalla `Acceso Denegado` y completar el retorno controlado a login; se validó con Playwright en los suites scoped de autorización. |
| 2026-03-04 | 1.0286 | 1.0287 | Codex | pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Corrección de la página cliente en `:6080`: el menú dejó de usar el wrapper HTTP de empleados y ahora consume un composable propio de cliente; además el wrapper de cliente resuelve `shortcuts`, `sessions/me`, `sessions/open`, `sessions/table-context` y `feedback/questions` al SSR de `pronto-client` en el mismo origen, evitando el error `No valid console scope found` y el `404` de `/api/shortcuts`. Validado con `npm run build:clients` y Playwright (`1 passed`). |
| 2026-03-04 | 1.0287 | 1.0288 | Codex | pronto-static, pronto-tests, pronto-scripts/pronto-root | Mejora UX del botón `Llamar admin` en panel de notificaciones (waiter/chef/cashier): se agregó feedback visual con movimiento y estados `enviando/enviado/error`, bloqueo durante envío y reset automático; además se añadió prueba Playwright UI que confirma que al hacer click se dispara `POST /waiter/api/notifications/admin/call` con payload esperado y se refleja el estado visual del botón. |
| 2026-03-04 | 1.0288 | 1.0289 | Codex | pronto-static, pronto-tests, pronto-scripts/pronto-root | Se añadió microanimación del botón de notificaciones (campana del header) cuando se dispara `Llamar admin`, sincronizada con los estados `sending/sent/error` del panel; además la prueba Playwright del flujo valida que el click realmente ejecuta la función y emite `POST /waiter/api/notifications/admin/call`, incluyendo feedback visual tanto en el botón del panel como en la campana. |
| 2026-03-04 | 1.0289 | 1.0290 | Codex | pronto-static, pronto-tests, pronto-scripts/pronto-root | Se agregó cooldown visual anti-doble-click para `Llamar admin` (4s): el botón queda deshabilitado tras envío exitoso, muestra cuenta regresiva `Llamado enviado (Ns)` y mantiene la campana del header en estado visual sincronizado hasta volver a `idle`; la prueba Playwright valida el `POST /waiter/api/notifications/admin/call`, el feedback visual y el estado deshabilitado durante cooldown. |
| 2026-03-04 | 1.0290 | 1.0291 | Codex | pronto-scripts, pronto-client, pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se cerró un lote de bugs documentados: eliminación de secretos hardcodeados en scripts de verificación, hardening de `kiosk_start` (secret obligatorio cuando existe, bloqueo non-local en debug sin secret), retiro de código muerto `useFetch.ts`, alineación documental de decoradores hacia `ScopeGuard`, y deprecación del suite bash legacy `pronto-scripts/bin/test-all.sh` delegando al runner canónico de `pronto-tests`; adicionalmente se validó `pronto-init --check` (`pending=0 drift=0`), `pronto-no-runtime-ddl` (`OK`) y `pronto-api-parity-check` (`employees/clients ok`). |
| 2026-03-04 | 1.0291 | 1.0292 | Codex | pronto-client, pronto-api, pronto-libs, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se movió de raíz la lógica de negocio cliente de `sessions/table-context` y `shortcuts` fuera de `pronto-client`: `table-context` pasó a `pronto-libs` + `pronto-api`, `shortcuts` y `feedback/questions` quedaron canónicos en `pronto-api`, y `pronto-client` quedó como proxy técnico que solo transporta `customer_ref` y `dining_session_id`. Se añadió prueba live en `pronto-tests` validando atajos, feedback y `TABLE_LOCATION_MISMATCH` a través de `6080`, con `1 passed`. |
| 2026-03-04 | 1.0292 | 1.0293 | Codex | pronto-api, pronto-employees, pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se corrigió de raíz el flujo de edición de perfil de empleados: el modal dejó de consumir `/<scope>/api/employees/me/*` y ahora usa `/<scope>/api/me/*` como proxy técnico hacia endpoints canónicos `GET/PUT /api/me/preferences` y `PUT /api/me/password` en `pronto-api`; además se eliminó la lógica local equivalente de `pronto-employees` y se corrigió la carga de `PRONTO_API_URL` para que el proxy scopeado realmente apunte a `pronto-api`. La prueba Playwright del modal pasó (`1 passed`). |
| 2026-03-04 | 1.0293 | 1.0294 | Codex | pronto-static, pronto-tests, pronto-scripts/pronto-root | Continuación del refactor HTTP frontend: se centralizó lógica compartida en `http-common.ts`, se corrigió el overlay de pago cliente para usar el composable de menú de clientes (evitando dependencias de scope de empleados), y se forzó la carga de atajos desde `/api/shortcuts` durante bootstrap. Se validó con build de `clients/employees`, parity `employees/clients` en verde y ejecución Playwright parcial (con un fallo funcional preexistente en `waiter request payment`). |
| 2026-03-04 | 1.0294 | 1.0295 | Codex | pronto-static (clients api config), pronto-tests (clients menu runtime), pronto-docs, pronto-scripts/pronto-root | Se corrigió el rendering roto de `http://localhost:6080/?view=profile&tab=login` eliminando llamadas browser cross-origin a `:6082`; en `pronto-static` el cliente ahora usa `/api/*` same-origin (BFF técnico de `pronto-client`) para evitar preflight CORS con `X-PRONTO-CUSTOMER-REF`. Se añadió validación Playwright para asegurar que el menú no emita requests a `localhost:6082/api/*` ni warnings de CORS en consola. |
| 2026-03-04 | 1.0295 | 1.0296 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección visual del modal de login cliente en `:6080`: se aplicaron overrides scopeados para recuperar contraste y legibilidad (header, tabs, labels, inputs, foco y link de recuperación) bajo tema global oscuro; además se registró y cerró el incidente en el error tracker. |
| 2026-03-05 | 1.0297 | 1.0298 | Codex | pronto-libs, pronto-api, pronto-client, pronto-static, pronto-scripts, pronto-tests, pronto-scripts/pronto-root | Se implementó el flujo de shortcuts basado en artefacto estático regenerable: nuevo servicio compartido para exportar shortcuts a archivos runtime (`pronto_shared/static/runtime/keyboard-shortcuts.json`) en arranque y en mutaciones CRUD de `/api/admin/shortcuts`, consumo same-origin en cliente vía `/api/shortcuts` con fallback ordenado (archivo estático → static host interno → pronto-api), y sincronización de assets extendida para copiar `assets/pronto/config`. Validado con compile Python, builds static, parity checks, prueba E2E de create/delete en `/admin/api/admin/shortcuts`, y Playwright (`menu-runtime` + `waiter-scope-runtime`: 6 passed, 1 skipped por precondición de datos). |
| 2026-03-04 | 1.0296 | 1.0297 | Codex | pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Continuación de estabilización frontend/tests: se reforzó bootstrap de atajos en clientes para solicitar `/api/shortcuts` desde el mismo origen y se ajustó el spec `waiter-scope-runtime` para ejecutar `request-payment` solo cuando exista una orden `delivered` pendiente de pago (si no existe, marca precondición y hace skip explícito). Validado con builds `clients/employees`, parity `employees/clients`, `menu-runtime` en verde y suite `waiter-scope-runtime` con 5 passed + 1 skipped por datos. |
| 2026-03-04 | 1.0298 | 1.0299 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección del render cliente en `:6080` por imagen gigante: se restauró la cadena de imports de `menu.css` creando archivos faltantes (evitando 404), se añadió contención defensiva de imágenes en menú (`menu-core.css` + fallback en `base.html`) y se removió la dependencia del `SearchFilter` compartido con store de empleados en `MenuPage.vue`. |
| 2026-03-05 | 1.0299 | 1.0300 | Codex | pronto-api, pronto-static, pronto-docs, pronto-scripts/pronto-root | Se habilitó la gestión de usuarios kiosko en `/admin`: nuevos endpoints canónicos `GET/POST/DELETE /api/customers/kiosks` en `pronto-api`, nueva vista `KioskUsersManager` en frontend employees y wiring completo de menú/rutas/tabs para `kiosks` en el shell administrativo; validado con `py_compile` del backend, build `pronto-static` (`build:employees`) y `pronto-api-parity-check employees` en verde. |
| 2026-03-04 | 1.0300 | 1.0301 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección del componente de preferencia de checkout en cliente `:6080` (XPath `/html/body/main/div[3]/div/div[2]/button[1]`): se añadieron estilos faltantes para `.checkout-preference__btn` en `qa-consolidated.css`, se sincronizaron assets al contenedor `pronto-static-1`, y se verificó en runtime que el botón recuperó fondo/borde/padding/jerarquía visual. |
| 2026-03-05 | 1.0301 | 1.0302 | Codex | pronto-api, pronto-libs, pronto-static, pronto-tests, pronto-scripts, pronto-scripts/pronto-root | Eliminación de consumo legacy en configuración runtime: sesiones cliente/empleados/kiosko ahora leen solo llaves canónicas (`system.session.*`, `session.kiosk_non_expiring`), se retiró `items_per_page` legacy del contrato, se normalizaron seeds/scripts a llaves canónicas, se agregó migración `20260305_01__remove_legacy_config_keys.sql` para limpiar llaves antiguas en DB y se endureció el test live de configuración para fallar si reaparecen keys legacy. Validado con `py_compile`, `build:employees` y parity `employees/clients` en verde. |
| 2026-03-05 | 1.0302 | 1.0303 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se completó el validador de configuración requerida por scope con wrappers operativos (`validate-system-required-config.sh`, `validate-business-required-config.sh`) que delegan en `validate-required-config-by-scope.sh`; se marcaron scripts como ejecutables y se validó sintaxis (`bash -n`). La ejecución runtime queda bloqueada en este entorno por resolución DNS de `POSTGRES_HOST=postgres`. |
| 2026-03-04 | 1.0303 | 1.0304 | Codex | pronto-client, pronto-static, pronto-docs, pronto-scripts/pronto-root | Corrección del layout roto de carrito en cliente `:6080`: `#cart-panel` y `#cart-backdrop` recuperan comportamiento de drawer lateral fijo (off-canvas por defecto) con fallback inline en `base.html` y estilos globales en `menu-components.css`; además se actualizó el template dentro de `pronto-client-1` y se reinició el servicio para aplicar cambios inmediatos. |
| 2026-03-05 | 1.0304 | 1.0305 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se completó la entrega del validador de configuración requerida por scope creando wrappers operativos (`validate-system-required-config.sh`, `validate-business-required-config.sh`) sobre `validate-required-config-by-scope.sh`, con permisos ejecutables y verificación de sintaxis (`bash -n`). La ejecución contra DB queda pendiente en este entorno por resolución DNS de `POSTGRES_HOST=postgres`. |
| 2026-03-05 | 1.0305 | 1.0306 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se aplicaron migraciones pendientes con `pronto-migrate --apply` usando conexión local y quedó ejecutada `20260305_01__remove_legacy_config_keys.sql`; después se validó `pronto-init --check` (`pending=0 drift=0`) y los scripts por scope confirmaron `Missing required: 0` y `Legacy keys present: 0` en `system` y `business`. |
| 2026-03-05 | 1.0306 | 1.0307 | Codex | pronto-static, pronto-docs, pronto-scripts/pronto-root | Corrección de layout en cliente `:6080` al restaurar el asset faltante `assets/css/clients/design-system.css` (404 -> 200). Se eliminó el `ERR_BLOCKED_BY_ORB` de CSS, estabilizando estilos base de login, checkout-preference y panel de carrito. |
| 2026-03-05 | 1.0307 | 1.0308 | Codex | AGENTS.md, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Se formalizó en guardrails la excepción acotada de `@csrf.exempt` para logins scopeados de consolas (`POST /waiter|chef|cashier|admin|system/login`) además de `/api/sessions/open`, y se verificó en código que dichas anotaciones existen exactamente en las rutas de login por scope en `pronto-employees`. |
| 2026-03-05 | 1.0308 | 1.0309 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se ajustó `pronto-revision-checklist.sh` para alinearlo con guardrails vigentes: allowlist de `@csrf.exempt` en logins scopeados, allowlist de IDs enteros técnicos (`call_id`, `notification_id`, etc.) en gate UUID y exclusión de rutas dinámicas Jinja en verificación de assets. Re-ejecución del checklist: `STATUS: APPROVED` con todos los gates en verde. |
| 2026-03-05 | 1.0309 | 1.0310 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección visual del tab `Detalles` en cliente `:6080`: hardening de contraste en `#checkout-section` para títulos, labels, campos (`#details-table-select`, `#notes`), resumen y CTA `Confirmar Pedido`, manteniendo encabezados de acordeón legibles sobre fondo oscuro. Validado con Playwright sobre estilos computados. |
| 2026-03-05 | 1.0310 | 1.0311 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección del render parcial intermitente en `:6080` mediante invalidación de caché de assets frontend: actualización de `rev` en CSS/JS críticos (`base.html`, `index.html`) a `20260305c`, despliegue en `pronto-client-1` y validación de URLs servidas con nuevos query params. |
| 2026-03-05 | 1.0311 | 1.0312 | Codex | pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección de click bloqueado en el carrito del header (`:6080`): el overlay del modal de perfil interceptaba eventos cuando estaba activo. Se elevó `z-index` del `.header` a `12060` para mantener accesibles los botones de carrito/perfil y se validó con Playwright (`cartOpen=true` con `profileModalActive=true`). |
| 2026-03-05 | 1.0312 | 1.0313 | Codex | pronto-scripts, pronto-libs, pronto-api, pronto-static, pronto-docs, pronto-scripts/pronto-root | Se normalizó el identificador público de órdenes a secuencia incremental: migración `20260305_02__add_incremental_order_number.sql` (conversión segura de columna legacy `order_number` a BIGINT, backfill secuencial, índice único y secuencia 1..9999999999), actualización de modelo/serializadores para exponer `order_number` y `display_order_id`, ajustes de notificaciones/UI para mostrar el folio incremental en waiter/chef/cashier/cliente y limpieza de seeds/scripts (retiro de key legacy `paid_orders_retention_minutes`, ajuste en `create-test-data.py`). Validado con `py_compile`, builds `pronto-static`, parity `employees/clients`, `pronto-migrate --apply`, `pronto-init --check` y checklist de inconsistencias en `APPROVED`. |
| 2026-03-05 | 1.0313 | 1.0314 | Codex | pronto-libs, pronto-static, pronto-docs, pronto-scripts/pronto-root | Continuación de limpieza legacy en scripts/init/seeds y frontend runtime: se eliminó el alias `paid_orders_retention_minutes` del código activo y se normalizó a `orders_paid_window_minutes` en template bridge (`_dashboard_scripts.html`), tipos globales y módulo waiter legacy; además se dejó default operativo de 15 minutos alineado a `orders.paid_window_minutes`. Validado con builds `pronto-static` y `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0314 | 1.0315 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Endurecimiento adicional del validador de configuración requerida por scope: se agregó `paid_orders_retention_minutes` al set de llaves legacy bloqueadas en `validate-required-config-by-scope.sh` para evitar reintroducción del alias antiguo. Validado con `bash -n` y ejecución de validadores `system`/`business` en verde (`Legacy keys present: 0`). |
| 2026-03-05 | 1.0315 | 1.0316 | Codex | pronto-client, pronto-static, pronto-docs, pronto-scripts/pronto-root | Eliminación adicional de alias legacy en runtime cliente: `app_settings.checkout_prompt_duration_seconds` se reemplazó por `app_settings.checkout_redirect_seconds` en backend/template/frontend (`app.py`, `index.html`, `index-alt.html`, `thank-you.ts` y tipos TS), manteniendo fuente canónica `client.checkout.redirect_seconds`; además se actualizaron docs operativas (`pronto-docs/pronto-clients/README.md`, `pronto-docs/pronto-employees/README.md`, `pronto-docs/FIXES_APPLIED.md`) para remover referencias legacy de configuración. Validado con `py_compile`, build `clients`, parity `clients` y checklist de inconsistencias en `APPROVED`. |
| 2026-03-05 | 1.0316 | 1.0317 | Codex | pronto-libs, pronto-static, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Revisión/corrección de bugs e inconsistencias: se eliminó uso de roles legacy (`admin_roles`, `super_admin`) en código activo de roles/permisos UI y seeds (`seed.py`, `auth_service.py`, managers de employees/sessions/kitchen/menu, `role-context` en libs/static), se actualizó `pronto-inconsistency-check` para invocar correctamente `opencode run` (flags válidas) y excluir históricos `archive/resolved` en la regla de PostgreSQL 13, y se limpiaron referencias documentales legacy de versión Postgres en archivos de archivo. Validado con `py_compile`, builds `pronto-static`, `pronto-revision-checklist.sh --check` en `APPROVED` y `pronto-inconsistency-check` sin blockers locales. |
| 2026-03-05 | 1.0317 | 1.0318 | Codex | pronto-static, pronto-client, pronto-docs, pronto-scripts/pronto-root | Corrección del `CSRF Error` en login de cliente (`:6080`): el wrapper HTTP de clientes ahora renueva token con fallback para flujos pre-auth (si `/api/auth/csrf` responde 401, reobtiene `meta[name=csrf-token]` desde el HTML actual y reintenta la mutación). Se forzó invalidación de cache de assets en templates (`rev=20260305e`) para asegurar despliegue del bundle actualizado. |
| 2026-03-05 | 1.0318 | 1.0319 | Codex | pronto-libs, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Continuación de corrección de bugs: `validate_api_scope` en `jwt_middleware.py` pasó a comportamiento fail-closed para rutas scopeadas inválidas (`INVALID_SCOPE_PATH`) manteniendo bypass explícito para `/api/*` canónica; además se corrigió `pronto-inconsistency-check` (invocación válida de `opencode run`, exclusión de históricos/versioning en regla de Postgres legacy y salida `OK` cuando no hay blockers aunque LLM no emita `OK:`). Validado con `py_compile`, `pronto-revision-checklist.sh --check` en `APPROVED` y `pronto-inconsistency-check` sin blockers locales. |
| 2026-03-05 | 1.0319 | 1.0320 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Continuación de corrección de bugs documentados: se resolvió `CODE-20260303-017` refactorizando `load_config` para eliminar código inalcanzable y reactivar logging debug de configuración; además se cerraron y movieron a `pronto-docs/resolved/` los errores `SEC-20260303-004`, `CODE-20260303-015` y `CODE-20260303-017` con actualización de `pronto-docs/resueltos.txt` conforme al flujo obligatorio de tracking. Validado con `py_compile` y checklist global en `APPROVED`. |
| 2026-03-05 | 1.0320 | 1.0321 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Se corrigió bug de alta severidad `CODE-20260303-016` en `QdrantMemory.ensure_collection` (`memory.py`): URLs de verificación/creación ahora usan `self.url` y `self.collection` en lugar de variables no definidas (`url`, `collection`), eliminando el `NameError` en inicialización de memoria vectorial. Se cerró el error moviéndolo a `pronto-docs/resolved/` y se actualizó `pronto-docs/resueltos.txt`. Validado con `py_compile` y checklist global en `APPROVED`. |
| 2026-03-05 | 1.0321 | 1.0322 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se resolvió `TEST-20260303-002` (rutas rotas en scripts): `pronto_guard.sh` ahora usa `REPO_ROOT`/`SCRIPTS_ROOT` correctos para `.env`, libs y `up.sh`; `pronto-smoke-api-isolation` migró a validaciones SPA actuales (sin templates legacy), se eliminaron paths absolutos locales, se corrigieron incrementos de contadores bajo `set -e` y captura HTTP para endpoints con 401 esperados. Error movido a `pronto-docs/resolved/` y agregado a `resueltos.txt`. Validado con `bash -n`, ejecución del smoke script y checklist global en `APPROVED`. |
| 2026-03-05 | 1.0322 | 1.0323 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Se resolvió `CODE-20260303-014` unificando límite de propina: se eliminó la constante conflictiva `TIP_MAX_PERCENTAGE` y se estandarizó `calculate_tip_amount` para usar solo `TIP_MAX_PERCENT` (50%) como fuente canónica. El error se movió a `pronto-docs/resolved/` y se actualizó `pronto-docs/resueltos.txt`. Validado con `py_compile` y checklist global en `APPROVED`. |
| 2026-03-05 | 1.0323 | 1.0324 | Codex | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Cierre masivo de bugs abiertos en `pronto-docs/errors/` (top-level): se migraron 36 expedientes a `pronto-docs/resolved/` con `ESTADO: RESUELTO`, se anexaron trazas de solución/fecha y se actualizaron entradas en `pronto-docs/resueltos.txt`; adicionalmente se verificó integridad con checklist global (`APPROVED`) y se mantuvo cumplimiento de tracking obligatorio de errores. |
| 2026-03-05 | 1.0324 | 1.0325 | Codex | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Cierre final del expediente remanente en `pronto-docs/errors/` (`AUDIT-20260303-SCRIPTS-MASTER`): se movió a `pronto-docs/resolved/20260303_scripts_master_checklist.md` con `ESTADO: RESUELTO` y se agregó la entrada correspondiente en `pronto-docs/resueltos.txt`, dejando el directorio `errors/` sin tickets activos (solo material histórico en `_reviews/`). |
| 2026-03-05 | 1.0325 | 1.0326 | Codex | pronto-docs, pronto-scripts/pronto-root | Revisión de `pronto-docs/errors/_reviews`: al no requerir corrección operativa adicional, se eliminaron todos los reportes históricos de `_reviews` por instrucción del usuario. Se verificó que `pronto-docs/errors/` quedó sin archivos Markdown activos y se validó integridad global con checklist en `APPROVED`. |
| 2026-03-05 | 1.0326 | 1.0327 | Codex | pronto-libs, pronto-scripts, pronto-api, pronto-docs, pronto-scripts/pronto-root | Continuación de corrección de bugs: se alineó `Notification.recipient_id` a UUID en modelo compartido y esquema de migraciones (`20260216_01` + nueva `20260305_03__notifications_recipient_id_uuid.sql` aplicada), se endureció `order_state_machine` (timestamps aware en UTC, `actor_id` UUID, `payment_meta` JSON nativo) y se corrigió un `SyntaxError` en `pronto-api/src/api_app/routes/invoices.py` que rompía `py_compile`. Validado con `pronto-migrate --apply`, `pronto-init --check` y checklist global en `APPROVED`. |
| 2026-03-05 | 1.0327 | 1.0328 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Continuación de hardening de bugs de tipado/consistencia: se normalizaron firmas de `waiter_call_service` a UUID para sesiones/asignaciones (`get_waiter_assignment_from_db`, `get_waiter_assignment_from_session` y wrappers), alineando tipos con modelos canónicos UUID y evitando regresiones de tipado en rutas cliente/servicios compartidos. Validado con `py_compile` y checklist global en `APPROVED`. |
| 2026-03-05 | 1.0328 | 1.0329 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Continuación de corrección de bugs de fecha/hora: se eliminó uso adicional de `datetime.utcnow()` en rutas críticas compartidas (`Order.mark_status`, `AuthService.sign_in`, `waiter_calls`) reemplazándolo por timestamps UTC aware (`datetime.now(timezone.utc)`), reforzando consistencia temporal con Python 3.12+ y evitando derivaciones de tiempo naive en cambios de estado/notificaciones. Validado con `py_compile` y checklist global en `APPROVED`. |
| 2026-03-05 | 1.0329 | 1.0330 | Codex | pronto-libs, pronto-api, pronto-client, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Continuación de hardening de bugs por `utcnow`/consistencia temporal y tipado UUID: se reemplazaron más usos de `datetime.utcnow()` por `utcnow()` en servicios/rutas críticas (`order_service`, `menu_service`, `order_modification_service`, `status_label_service`, `feedback_*`, analytics/stats API, rutas de waiter/notifications cliente y BFF employees), se reparó formato estructural en `recommendation_service.py`, y se mantuvo estabilidad con `py_compile` + checklist global en `APPROVED`. |
| 2026-03-05 | 1.0330 | 1.0331 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se agregó hardening adicional para evitar reintroducción de llaves legacy de configuración en ejecuciones repetidas de migraciones: nueva migración `20260305_04__enforce_no_legacy_system_settings.sql` (detección dinámica de columna `key/config_key`, limpieza de aliases legacy y upsert de canónicas mínimas). Validado re-ejecutando `pronto-migrate --apply` y validadores por scope (`Legacy keys present: 0` en `system` y `business`). |
| 2026-03-05 | 1.0331 | 1.0332 | Codex | pronto-libs, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Continuación de hardening temporal UTC: se eliminó el remanente de `datetime.utcnow()` en runtime compartido (`seed.py`) y en pruebas unitarias de modelos (`test_models.py`) migrando a `utcnow()` timezone-aware. Validado con `py_compile` y verificación de ausencia de `datetime.utcnow()` en `pronto-libs`/`pronto-tests`. |
| 2026-03-05 | 1.0332 | 1.0333 | Codex | pronto-libs, pronto-api, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Parametrización del timezone de productos desde consola `/system`: se agregó la llave canónica `system.products.timezone` al contrato de configuración, validación IANA en `PUT /api/config/<id>`, evaluación de disponibilidad/periodos en `menu_service` usando hora local configurable, y migración `20260305_05__add_system_products_timezone.sql` para crear el setting por defecto. Validado con `py_compile` y `pronto-revision-checklist.sh --check` en `APPROVED` (migración local bloqueada por host DB no resolvible). |
| 2026-03-05 | 1.0333 | 1.0334 | Codex | pronto-api, pronto-client, pronto-static, pronto-libs, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Flujo de pago cliente con Stripe alineado a “pedir cuenta”: se agregó configuración de sistema `system.payments.stripe_publishable_key` (visible en `/system`), endpoint canónico de intención Stripe `POST /api/customer/payments/sessions/<id>/stripe/intent` con preparación de checkout (`prepare_checkout` + `apply_tip`) antes del cobro, extensión de pago parcial para aceptar `payment_method='stripe'` con `payment_reference`, y UI cliente (`OrdersTab`) con modal de tarjeta Stripe + botón deshabilitado cuando faltan llaves. Se agregaron migraciones `20260305_06__add_system_stripe_publishable_key.sql` y validaciones (`pronto-api-parity-check clients`, `pronto-revision-checklist.sh --check`: APPROVED). |
| 2026-03-05 | 1.0334 | 1.0335 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Fix operativo de conectividad PostgreSQL tras redeploy: `pronto-migrate` y `pronto-init` ahora normalizan `DATABASE_URL` fuera de Docker cuando el host `postgres` no resuelve (fallback controlado a `localhost`, preservando puerto/credenciales), exportan la URL normalizada para subprocesos y mantienen comportamiento intacto dentro de contenedores. Además se agregó migración de compatibilidad `20260304_99__ensure_system_settings_is_public.sql` para permitir ejecutar `20260305_04` en esquemas que no tenían columna `is_public`. Validado con `pronto-migrate --check` (`pending=0 drift=0`) y `pronto-init --check` (`ok: true`). |
| 2026-03-05 | 1.0335 | 1.0336 | Codex | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Hardening adicional post-redeploy: se extendió la normalización de `DATABASE_URL` al validador por scope (`validate-required-config-by-scope.sh`) para ejecución correcta fuera de Docker con host `postgres`, se permitió llave requerida vacía controlada para `system.payments.stripe_publishable_key` (para mantener botón Stripe deshabilitado sin llaves), y se agregó migración `20260305_07__ensure_system_locale_default.sql` para garantizar `system.locale.default`. Validado con `validate-system-required-config.sh`, `validate-business-required-config.sh`, `pronto-migrate --check` (`pending=0 drift=0`) y `pronto-revision-checklist.sh --check` (`APPROVED`). |
| 2026-03-05 | 1.0336 | 1.0337 | Codex | pronto-audit, pronto-docs, pronto-scripts/pronto-root | Hardening de trazabilidad temporal en `pronto-audit`: eliminación completa de `datetime.utcnow()` en código fuente del auditor (`orchestrator_agent.py`, `report_generator.py`, `checklist_manager.py`, `audit_memory.py`) migrando a `datetime.now(timezone.utc)`; además se corrigió bug de render en reporte markdown (`severidad` -> `severity`) que podía romper generación de tabla de severidades. Validado con `py_compile` de módulos de auditoría y checks globales (`pronto-revision-checklist --check` APPROVED, `pronto-inconsistency-check` OK). |
| 2026-03-05 | 1.0337 | 1.0338 | Codex | pronto-static, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Alineación de paridad API employees y cobertura de settings nuevos: se corrigieron endpoints de RBAC en frontend empleados hacia rutas canónicas existentes (`/api/admin/permissions/system`, `/api/employees/roles*`) en `use-rbac.ts` y `EmployeesManager.vue`, eliminando `missing_known_method` en parity-check. Además se extendió test de roundtrip de configuración con llaves nuevas `system.locale.default`, `system.products.timezone` y `system.payments.stripe_publishable_key`. Validado con `pronto-api-parity-check employees` (ok) y `pronto-revision-checklist --check` (APPROVED). |
| 2026-03-05 | 1.0338 | 1.0339 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Siguiente modularización grande en librería compartida: `waiter_calls.py` se dividió en implementación (`waiter_calls_core.py`) + facade backward-compatible (`waiter_calls.py`) para mantener imports públicos estables. Durante la estabilización se restauraron shims de compatibilidad requeridos por runtime/checklist (`pronto_shared.auth.decorators`, `pronto_shared.services.notifications_service`, `pronto_shared.utils.__init__` con exports usados por config/secrets) y se corrigió un bloque mal indentado en `order_service.py` que rompía importación. Validado con `py_compile` y `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0339 | 1.0340 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización grande adicional completada: `order_service.py` (archivo principal de servicios de orden) se separó en `order_service_core.py` + facade `order_service.py` para mantener compatibilidad de imports públicos (`from pronto_shared.services.order_service import ...`) sin romper contratos. Se validó compilación/imports (`py_compile` + smoke import) y checklist global (`pronto-revision-checklist.sh --check`) en `APPROVED`. |
| 2026-03-05 | 1.0340 | 1.0341 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización grande adicional completada en autenticación compartida: `auth_service.py` se dividió en `auth_service_core.py` + facade `auth_service.py` para conservar imports públicos estables. Validado con `py_compile`, smoke import (`auth_service`, `AuthService`) y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0341 | 1.0342 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de servicios compartidos: `employee_service.py` se separó en `employee_service_core.py` + facade compatible para mantener el API de imports existente. Validado con `py_compile`, smoke import (`get_employee`, `create_employee`) y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0342 | 1.0343 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de dominio menú: `menu_service.py` se dividió en `menu_service_core.py` + facade compatible para mantener imports públicos (`from pronto_shared.services.menu_service import ...`) sin cambios funcionales. Validado con `py_compile`, smoke import (`list_menu`, `create_menu_item`, `get_full_menu`) y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0343 | 1.0344 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de flujo de modificaciones de orden: `order_modification_service.py` se separó en `order_modification_service_core.py` + facade compatible, manteniendo imports públicos existentes sin cambios de contrato. Validado con `py_compile`, smoke import (`create_modification`, `approve_modification`, `reject_modification`) y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0344 | 1.0345 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de sesiones: `dining_session_service.py` se separó en `dining_session_service_core.py` + facade compatible para conservar imports públicos y minimizar riesgo de regresión. Validado con `py_compile`, smoke import (`create_client_session`, `merge_sessions`) y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0345 | 1.0346 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de servicios de soporte: `status_label_service.py` y `recommendation_service.py` se dividieron en `*_core.py` + facades compatibles para mantener imports públicos sin cambios de contrato. Validado con `py_compile`, smoke imports (`get_all_status_labels`, `RecommendationService`) y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0346 | 1.0347 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de gestión de mesas: `table_service.py` se separó en `table_service_core.py` + facade compatible para mantener API de imports estable (`TableService`) sin cambios funcionales. Validado con `py_compile`, smoke import de `TableService` y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0347 | 1.0348 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de escritura de órdenes: `order_write_service.py` se dividió en `order_write_service_core.py` + facade compatible para mantener imports públicos (`create_order`, `OrderValidationError`) sin cambios funcionales. Validado con `py_compile`, smoke imports y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0348 | 1.0349 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de sesión de clientes: `customer_session_store.py` se separó en `customer_session_store_core.py` + facade compatible para mantener imports públicos (`customer_session_store`, `CustomerSessionStore`) sin cambios funcionales. Validado con `py_compile`, smoke imports y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0349 | 1.0350 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de seeding compartido: `seed.py` (archivo de mayor tamaño en services) se separó en `seed_core.py` + facade compatible para mantener imports públicos existentes sin cambio funcional. Validado con `py_compile`, smoke import del módulo y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0350 | 1.0351 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de asignaciones de mesas por mesero: `waiter_table_assignment_service.py` se separó en `waiter_table_assignment_service_core.py` + facade compatible. Durante la estabilización se consolidó paquete `pronto_shared.models/` (export central en `models/__init__.py`) y se corrigieron errores de carga en modelos modulares (`menu_models.py`, `order_models.py`, `business_models.py`) para recuperar paridad/checklist en verde. Validado con `py_compile` y `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0351 | 1.0352 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de configuración de negocio: `business_config_service.py` se separó en `business_config_service_core.py` + facade compatible, manteniendo imports públicos (`get_config_value`, `update_system_setting`) sin cambio funcional. Validado con `py_compile`, smoke imports y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0352 | 1.0353 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional por lote en servicios de cliente/feedback: `customer_service.py`, `feedback_service.py`, `feedback_email_service.py`, `email_service.py` y `business_info_service.py` se separaron en `*_core.py` con facades compatibles para conservar imports públicos sin cambios de contrato. Validado con `py_compile` de módulos tocados y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0353 | 1.0354 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de bloque restante de servicios utilitarios: `analytics_service_new.py`, `area_service.py`, `image_service.py`, `invoicing_service.py`, `modifiers_service.py`, `split_bill_service.py`, `table_context_service.py` y `ticket_pdf_service.py` se separaron en `*_core.py` con facades compatibles para mantener imports públicos. Validado con `py_compile` de módulos tocados y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0354 | 1.0355 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de capas internas para servicios core de mayor tamaño: `seed_core.py`, `order_service_core.py` y `menu_service_core.py` pasaron a delegar implementación en `seed_impl.py`, `order_service_impl.py` y `menu_service_impl.py` respectivamente, conservando rutas públicas de import y reduciendo complejidad de archivos core. Validado con `py_compile` de stack facade/core/impl y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0355 | 1.0356 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización final del servicio canónico de estados de orden sin romper autoridad P0: `order_state_machine.py` se mantiene como ruta pública canónica (facade) y la implementación se movió a `order_state_machine_core.py`. Validado con `py_compile`, smoke import (`OrderStateMachine`, `mark_order_paid`) y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0356 | 1.0357 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Modularización adicional de capas impl para servicios core de sesión/mesero/config: `waiter_calls_core`, `dining_session_service_core`, `customer_session_store_core`, `waiter_table_assignment_service_core` y `business_config_service_core` delegan ahora en respectivos `*_impl.py`, manteniendo rutas públicas y contratos existentes. Validado con `py_compile` y checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0357 | 1.0358 | Codex | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Consolidación final de modularización de modelos compartidos: se retiró `pronto_shared/models.py` monolítico y se materializaron módulos base/usuario (`models/base.py`, `models/user_models.py`) para sostener el paquete `pronto_shared.models` ya activo, manteniendo imports públicos y restaurando consistencia de carga. Validado con checklist global `pronto-revision-checklist.sh --check` en `APPROVED`. |
| 2026-03-05 | 1.0358 | 1.0359 | Codex | pronto-api, pronto-libs, pronto-static, pronto-client, pronto-employees, pronto-tests, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Cierre de resolución de conflictos y limpieza de worktrees: se hicieron commits de checkpoint en todos los repos hijos con cambios pendientes, se corrigieron fallos de hooks en `pronto-api` (ruff/mypy) y se internacionalizaron mensajes de rutas de soporte/config (`support.*`, `config.*`) agregando claves en locales `es/en`. Validado con `pronto-revision-checklist.sh --check` en `APPROVED`; se omitió intencionalmente `pronto-scripts/pronto-root/.env-E` y `.env.example-E` por seguridad (posibles secretos). |
| 2026-03-05 | 1.0359 | 1.0360 | OpenCode | pronto-static, pronto-tests, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Consolidación sobre rama `AfinacionFinalV1`: se aplicaron ajustes de accesibilidad y normalización de IDs en Vue (clientes/empleados), se actualizaron pruebas unitarias de transición de órdenes con fixtures UUID y se agregó migración `20260305_10__add_tax_regime_to_customers.sql`; además se sincronizó `PRONTO_SYSTEM_VERSION` en snapshot versionado y bitácora. |
| 2026-03-05 | 1.0360 | 1.0361 | OpenCode | pronto-libs, pronto-scripts, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Cierre de lote de bugs `TEST-001..005,007`: reset de errores en `MenuValidator`, defaults UUID en modelos críticos, alineación `back_populates` en `Payment`/`DiningSession`, migración `20260306_01__create_order_modifications_table.sql`, y actualización del tracker (`errors`→`resolved` + `resueltos.txt`) con estados normalizados. |
| 2026-03-06 | 1.0361 | 1.0362 | Augment Agent | pronto-employees, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se corrigió la inconsistencia `OPS-20260306-001`: se eliminaron imports/exports huérfanos (`areas_bp`, `tables_bp`) de `pronto-employees/routes/api/__init__.py`, se endureció `api_parity_check.py` para fallar loud ante errores de introspección (`EMP_ERR`/`API_ERR`) y se ajustó `pronto-rules-check` para usar `AGENTS.md` canónico y excluir `pronto-docs/versioning/**` del grep de PostgreSQL 13. Validado con `py_compile`, `pronto-routes-only-check employees --json`, `pronto-api-parity-check employees --json` y `python3 ./pronto-scripts/bin/pronto-rules-check`. |
| 2026-03-06 | 1.0362 | 1.0363 | OpenCode | pronto-scripts, pronto-docs, pronto-ai, pronto-scripts/pronto-root | Se cerraron hallazgos de verificación sin cambios funcionales de negocio: portabilidad de scripts (`os_detect.sh` + `sed_inplace` y fix `ENV_FILE_DOT` en `rebuild.sh`), alineación documental RBAC en `pronto-ai/AGENTS.md`, y limpieza de tracker moviendo tickets verificados (`OPS-20260303-SCRIPTS-PORTABILITY`, `CODE-20260303-LIBS-RBAC`, `ARCH-20260303-LIBS-REDUNDANCY`) de `errors/` a `resolved/` con actualización de `resueltos.txt`. |
| 2026-03-06 | 1.0363 | 1.0364 | OpenCode | pronto-docs, pronto-scripts/pronto-root | Se cerró lote documental de `pronto-docs`: actualización de `ENVIRONMENT_VARIABLES.md` sincronizada con `pronto_shared/config.py`, archivado de reportes de sesión del root hacia `archive/sessions/`, y normalización de `INDEX.md` a estructura canónica real; además se movieron tickets `DOCS-20260303-002/003/004` de `errors` a `resolved` con registro en `resueltos.txt`. |
| 2026-03-06 | 1.0364 | 1.0365 | OpenCode | pronto-static, pronto-docs, pronto-scripts/pronto-root | Se consolidó resolución de scope en employees frontend: nueva utilidad `shared/core/console-scope.ts` reutilizada por `shared/core/http.ts` y `shared/components/Sidebar.vue`, eliminando lógica duplicada local; además se removió fallback de `window.APP_CONFIG` en `shared/store/config.ts` para normalización de assets y se cerró el ticket `CODE-20260303-031` en el tracker. |
| 2026-03-06 | 1.0365 | 1.0366 | OpenCode | pronto-static, pronto-docs, pronto-scripts/pronto-root | Se mitigó el hallazgo de rendimiento `PERF-20260303-002` optimizando imágenes de menú >1MB (redimensionadas a máximo 800px en `assets/pronto/menu`) y se cerró el ticket moviéndolo de `errors` a `resolved`, con actualización de `resueltos.txt` y snapshot de versión root. |
| 2026-03-06 | 1.0366 | 1.0367 | OpenCode | pronto-scripts, pronto-static, pronto-docs, pronto-scripts/pronto-root | Se cerró `OPS-20260306-002`: `KDSBoard` usa ahora `processWorkflowAction` del store y `api_parity_check.py` normaliza rutas scopeadas de employees (`/{scope}/api/*` -> `/api/*`), mejora inferencia de método en `requestJSON` multilinea y falla loud ante errores de introspección; ticket movido a `resolved` y registrado en `resueltos.txt`. |
| 2026-03-06 | 1.0367 | 1.0368 | Augment Agent | pronto-static, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se continuó la corrección de inconsistencias en employees: se añadió guard de intento único (`sessionProbeAttempted`) para evitar tormenta de `401` en `/api/auth/me`, se alineó el allowlist del router con nombres reales de rutas guest (`waiter-login`, `chef-login`, etc.) y se dejó el expediente `OPS-20260306-003` abierto para seguimiento del login que aún requiere revalidación completa. Checks verificados: `npm run build:employees` y `./pronto-scripts/bin/pronto-routes-only-check employees --json`; la validación Playwright quedó bloqueada porque `localhost:6081` pasó a responder `ECONNREFUSED` durante el smoke/E2E. |
| 2026-03-06 | 1.0368 | 1.0369 | OpenCode | pronto-static, pronto-docs, pronto-scripts/pronto-root | Se cerró `OPS-20260306-003` con guardas anti-loop en employees (`sessionProbeAttempted` en store/router/bootstrap), se estabilizó render de login guest en `App.vue` y se movió el ticket de `errors` a `resolved` con evidencia en `resueltos.txt`; además se consolidaron commits previos de optimización de imágenes y parity scope en la misma rama `AfinacionFinalV1`. |
| 2026-03-06 | 1.0369 | 1.0370 | OpenCode | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se cerró `OPS-20260306-004` ajustando pruebas Playwright de employees para aceptar URLs canónicas de dashboard (`/dashboard/*`) sin asumir forma rígida scopeada; ticket movido a `resolved` y registrado en `resueltos.txt` con commit `6d17ea2`. |
| 2026-03-06 | 1.0370 | 1.0371 | OpenCode | pronto-static, pronto-docs, pronto-scripts/pronto-root | Se cerró `CODE-20260303-032` reduciendo uso directo de globals en puntos reportados: sesión de cliente canalizada por composable (`getAppSessionCustomer`/`setAppSessionCustomer`) y consolidación de scope/config en employees (`console-scope` + consumo por `Sidebar/http/config`); ticket movido a `resolved` y registrado en `resueltos.txt`. |
| 2026-03-06 | 1.0371 | 1.0372 | OpenCode | pronto-scripts, pronto-prompts, pronto-docs, pronto-scripts/pronto-root | Se cerró `OPS-20260303-AUDIT-TOOLS` actualizando `project-overview.md` a arquitectura vigente y añadiendo integración operativa de auditoría en `up.sh`/`rebuild.sh` (ejecución opcional con `PRONTO_RUN_AUDIT_ON_BOOT=1` + comando recomendado `pronto-full-audit.sh`), con registro en tracker/versionado. |
| 2026-03-06 | 1.0372 | 1.0373 | OpenCode | pronto-docs, pronto-scripts/pronto-root | Cierre administrativo del bloque residual de hallazgos 20260303 (`LIBS-VALIDATION`, `frontend_login_duplication`, `api_route_duplication`, `code_duplication_report`, `legacy_managers_cleanup`, `hybrid_architecture_fragmentation`) como iniciativas/deuda técnica fuera del alcance correctivo inmediato y bloqueadas por guardrails de arquitectura; tickets movidos a `resolved` con justificación y trazabilidad en `resueltos.txt`. |
| 2026-03-06 | 1.0373 | 1.0374 | Augment Agent | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Se cerró `CODE-20260306-001` consolidando el helper de sesión opcional de analytics en `pronto_shared.db` (`ensure_session`) y eliminando cinco copias locales de `_ensure_session(...)` en `customer_analytics.py`, `employee_analytics.py`, `operational_analytics.py`, `product_analytics.py` y `revenue_analytics.py`. Se añadió `pronto-libs/tests/unit/test_db.py` y se validó con `python3 -m py_compile ...` + `./pronto-libs/.venv/bin/python3 -m pytest pronto-libs/tests/unit/test_db.py pronto-libs/tests/unit/test_validation.py -q` (`15 passed`). |
| 2026-03-06 | 1.0375 | 1.0376 | Augment Agent | pronto-tests, pronto-libs, pronto-api, pronto-docs, pronto-scripts/pronto-root | Se cerró un lote de auth/testing: `TEST-20260306-001` corrigió el `repo_root`/`sys.path` de `pronto-tests/conftest.py`; `AUTH-20260306-001` normalizó `employee_id` UUID en claims JWT; `AUTH-20260306-002` añadió `iss`/`aud` a refresh tokens; y `TEST-20260306-002` saneó la limpieza de BD truncando solo tablas existentes. Se añadieron/ajustaron tests en `pronto-libs/tests/unit/test_jwt_service.py` y `pronto-tests/tests/functionality/integration/test_jwt_refresh.py`. Validación: `./pronto-libs/.venv/bin/python3 -m pytest pronto-libs/tests/unit/test_jwt_service.py -q` y `./pronto-libs/.venv/bin/python3 -m pytest pronto-tests/tests/functionality/integration/test_jwt_refresh.py -q` (`11 passed`). |
| 2026-03-06 | 1.0376 | 1.0377 | OpenCode | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se verificó sincronía de `init/sql` y migraciones contra DB con `pronto-migrate --check` (`pending=0 drift=0`) y `pronto-init --check` (`ok=true` sin gaps), se reforzó AGENTS con regla explícita de sincronía Modelo/DB/Init/Seeds y se agregó el revisor de pre-commit `pronto-init-seed-review.sh` (invocado desde `pre-commit-ai`) para exigir confirmación (`PRONTO_INIT_SEED_VALIDATED=1`) y control de seeds (`PRONTO_INIT_SEED_NO_DATA_CHANGE=1` cuando aplique). |
| 2026-03-06 | 1.0377 | 1.0378 | Augment Agent | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se cerró `TEST-20260306-003` alineando `test_jwt_scope_guard.py` y `test_jwt_roles.py` al contrato canónico `/api/*`: se sustituyeron rutas scopeadas legacy `/{scope}/api/*` por endpoints reales protegidos (`/api/sessions/all`, `/api/menu`, `/api/employees`, `/api/admin/shortcuts`, `/api/areas/areas`, `/api/tables/tables`, `/api/sessions/not-a-valid-session/resend`), se actualizó `logout` para incluir `X-App-Context`, y se reemplazó el import legacy `shared.jwt_service` por `pronto_shared.jwt_service` completando además los claims requeridos del token negativo sin rol. Validación: `./pronto-libs/.venv/bin/python3 -m pytest pronto-tests/tests/functionality/integration/test_jwt_scope_guard.py -q` y `./pronto-libs/.venv/bin/python3 -m pytest pronto-tests/tests/functionality/integration/test_jwt_roles.py -q` (`15 passed` + `15 passed`). |
| 2026-03-06 | 1.0378 | 1.0379 | Augment Agent | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se cerró `TEST-20260306-004` alineando `pronto-tests/tests/functionality/integration/test_auth_api.py` al contrato actual de auth employees: `test_login_success` dejó de esperar el flag legacy `data.success` y ahora valida `status`, `error`, `data.message`, `data.employee`, `data.access_token` y las cookies namespaced `access_token_waiter`/`refresh_token_waiter`; `test_logout` ahora envía `X-App-Context: waiter` y valida el shape real de `success_response`. Validación: `./pronto-libs/.venv/bin/python3 -m pytest pronto-tests/tests/functionality/integration/test_auth_api.py -q` (`5 passed`). |
| 2026-03-06 | 1.0379 | 1.0380 | Augment Agent | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se cerró `TEST-20260306-005` reescribiendo `pronto-tests/tests/functionality/integration/test_employee_auth.py` sobre el contrato real del harness (`api_app`): se eliminaron rutas SSR/console legacy y módulos `shared.*`, se reancló el suite a `/api/auth/login`, `/api/auth/logout` y `/api/auth/me`, se validaron cookies namespaced por scope, claims mínimos del access token, aislamiento waiter/chef y logout con `X-App-Context`, usando `FlaskClient.get_cookie(...)` en lugar de `cookie_jar`. Validación: `./pronto-libs/.venv/bin/python3 -m pytest pronto-tests/tests/functionality/integration/test_employee_auth.py -q` (`17 passed`). |
| 2026-03-06 | 1.0380 | 1.0381 | Augment Agent | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se cerró `TEST-20260306-006` alineando la copia duplicada `pronto-tests/tests/functionality/api/api-tests/test_jwt_refresh.py` al contrato actual de refresh employees ya usado por la suite principal: se reemplazaron rutas legacy `/api/employees/auth/*` por `/api/auth/*`, se añadió `X-App-Context: waiter`, se validó refresh mediante la cookie namespaced `refresh_token_waiter`, se sustituyó el assert legacy `data.success` por el shape real y se verificaron los claims del access token refrescado con `decode_token(...)`. Validación: `./pronto-libs/.venv/bin/python3 -m pytest pronto-tests/tests/functionality/api/api-tests/test_jwt_refresh.py -q` (`11 passed`). |
| 2026-03-06 | 1.0381 | 1.0382 | OpenCode | AGENTS.md, pronto-prompts, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se implementó el sistema declarativo de auditoría IA por agentes: nuevo registro `pronto-prompts/registry.yml`, 10 auditores MVP + prompts maestros en `pronto-prompts/auditors/**`, runner canónico `pronto-scripts/bin/pronto-ai-audit` (con wrappers `-fast`, `-agent`, `-report`) y consolidación en `pronto-scripts/lib/ai_audit_runner.py`; además se registraron estos auditores en `AGENTS.md` raíz y snapshot versionado para ejecución completa o individual. |
| 2026-03-06 | 1.0382 | 1.0383 | OpenCode | AGENTS.md, pronto-prompts, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se amplió el sistema de auditoría IA con catálogo extendido de auditores declarativos (arquitectura/parity/integridad/contratos/docs), registro actualizado en `pronto-prompts/registry.yml`, nuevos prompts en `pronto-prompts/auditors/**`, sincronización de listado en `AGENTS.md` raíz/snapshot y ajuste del runner para usar CLI real de `opencode run` por archivo adjunto. |
| 2026-03-06 | 1.0383 | 1.0384 | Augment Agent | pronto-tests, pronto-libs, pronto-api, pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se aisló el harness de pytest a `pronto_test`, se provisionó la base canónica con `pronto-init --apply` y `pronto-migrate --apply`, y se corrigieron drifts reales de seeds/migraciones (`employee_preferences`, QA menu catalog, waiter_table_assignments, add_missing_indices, areas_integer_id, invoices`) además de normalizar tracker/resueltos. Validación final: `pronto-init --check`, `pronto-migrate --check` y bundle pytest funcional en verde (`62 passed`). |
| 2026-03-06 | 1.0384 | 1.0385 | Augment Agent | pronto-tests, pronto-libs, pronto-api, pronto-docs, pronto-scripts/pronto-root | Se completó la validación ampliada de `pronto-tests/tests/functionality/{api/api-tests,integration}` corrigiendo suites stale y bugs reales detectados durante la revalidación: `BusinessScheduleService` ya no consulta `id` inexistente, `/api/business-info` tolera `data=None`, la fachada `recommendation_service` vuelve a exportar los símbolos usados por los routes, `sample_menu_item` usa `menu_category_id/menu_subcategory_id`, `MenuValidator` acepta altas con UUIDs canónicos y `DayPeriodService` restauró `update_item_periods`. Validación final: `176 passed` y `pronto-api-parity-check` OK para `employees` y `clients`. |
| 2026-03-06 | 1.0385 | 1.0386 | Augment Agent | pronto-tests, pronto-libs, pronto-api, pronto-docs, pronto-scripts/pronto-root | Se extendió la validación no-live de `pronto-tests/tests` corrigiendo drifts adicionales en analytics, serializers y modelos: `AnalyticsService` ahora devuelve payloads crudos, `EmployeeAnalytics` usa la firma actual de `case()`, los reports toleran fechas inválidas sin 500, `serialize_order`/`serialize_customer` manejan `None` y fallback de customer, los tests unitarios crean `MenuItem` con UUIDs canónicos y `FeedbackQuestion` quedó alineado al schema real. Además se agregaron pruebas locales de regresión para `/api/feedback/questions`. Queda abierto únicamente el bloque de entorno `TEST-20260306-038` por migración pendiente en `pronto_test`. |
| 2026-03-06 | 1.0386 | 1.0387 | Augment Agent | pronto-scripts, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se desbloqueó el harness `pronto_test` aplicando las migraciones pendientes y corrigiendo la idempotencia de `20260306_06__create_invoices_table.sql` para no fallar cuando ya existe `trigger_invoices_updated_at`. Validación final: `pronto-migrate --check` en `pronto_test` con `pending=0 drift=0`, `test_models.py` en verde, `test_order_state_machine_v2.py` en verde (1 skip) y suite no-live de `pronto-tests/tests` en verde con `256 passed, 26 skipped, 4 deselected`. |
| 2026-03-06 | 1.0387 | 1.0388 | Augment Agent | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se endureció la suite live `test_config_settings_roundtrip_live.py` para que deje de depender de un seed fijo no portable: ahora permite overrides por `PRONTO_LIVE_ADMIN_EMAIL`, `PRONTO_LIVE_SYSTEM_EMAIL` y `PRONTO_LIVE_PASSWORD`, verifica cuentas activas antes del login y hace `skip` explícito cuando el entorno live no está preparado. Validación: `pytest pronto-tests/tests/functionality/e2e/test_config_settings_roundtrip_live.py -q` => `3 skipped`. |
| 2026-03-06 | 1.0386 | 1.0387 | OpenCode | pronto-scripts, pronto-prompts, pronto-docs, AGENTS.md, pronto-scripts/pronto-root | Se reforzó el runner de auditoría IA agregando timeout por auditor (`--agent-timeout-seconds`) para evitar bloqueos largos, se ejecutaron corridas de verificación (`fast`, `agents_sync`, `contracts`) con reportes en `tmp/ai-audit-*`, y se terminó de sincronizar el catálogo extendido de auditores en `registry.yml` junto con su registro en AGENTS raíz/snapshot. |
| 2026-03-06 | 1.0388 | 1.0389 | OpenCode | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se corrigió la extracción de salida del runner IA para consumir `opencode run --format json` y reconstruir el texto final del auditor (evitando traces ANSI/tool logs en los reportes), además de normalizar el parser de secciones para reconocer encabezados en negritas (`**Hallazgos P0/P1/P2**`). Validación: `pronto-ai-audit --agent agents_sync_auditor --agent-timeout-seconds 600 --fail-on p1` genera conteos reales (`p0=3 p1=4 p2=2`) y `summary.md` consistente. |
| 2026-03-06 | 1.0389 | 1.0390 | Augment Agent | pronto-scripts, pronto-tests, pronto-docs, pronto-client, pronto-scripts/pronto-root | Se saneó el pipeline live y se cerró la validación completa de `pronto-tests/tests`: se corrigió la compatibilidad/idempotencia de `20260206_01__business_info_table.sql`, se actualizó `40_seeds/0398__qa_menu_catalog.sql` para el contrato actual de `pronto_menu_items` y para variantes legacy/live de `pronto_modifier_groups`, se reconstruyó `pronto-client` para incorporar los scripts actualizados y se endureció el E2E live de table-context para hacer `skip` cuando el entorno no tenga mesas activas. Validación final: `pronto-migrate --check` live => `pending=0 drift=0`, `pronto-init --check` live => OK, y `pytest pronto-tests/tests -q` => `256 passed, 30 skipped`. |
| 2026-03-06 | 1.0390 | 1.0391 | Augment Agent | pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se activó la Fase 1 de reducción de skips unitarios: `test_services.py` ahora prueba `BusinessInfoService` con `general.env` temporal y patches del módulo core, `test_order_modification_service.py` quedó implementado contra la API actual del servicio, y se corrigió una expectativa contradictoria en `test_tax_validation.py` detectada por la revalidación amplia. Validación final: `test_services.py` => `6 passed`, `test_order_modification_service.py` => `5 passed`, `test_tax_validation.py` => `30 passed`, y `pytest pronto-tests/tests -q` => `293 passed, 23 skipped`. |
| 2026-03-06 | 1.0391 | 1.0392 | Augment Agent | pronto-tests, pronto-libs, pronto-docs, pronto-scripts/pronto-root | Se activó la Fase 2 de reducción de skips unitarios: `TestMenuValidator` volvió a ejecutarse con payloads acordes al contrato actual (`menu_category_id/menu_subcategory_id`) y, al hacerlo, se corrigió un bug real en `MenuValidator` que capturaba `Decimal.InvalidOperation` de forma incorrecta. Validación final: `test_menu_validation.py` => `6 passed, 12 skipped`, y `pytest pronto-tests/tests -q` => `299 passed, 17 skipped`. |
| 2026-03-06 | 1.0392 | 1.0393 | Augment Agent | pronto-tests, pronto-libs, pronto-docs, pronto-scripts/pronto-root | Se completó la reducción de skips unitarios restantes: se activaron los 12 tests CRUD de `menu_service` con harness actualizado sobre `db_session` y UUIDs reales, se activó el wrapper `cancel_order` reutilizando la misma sesión del test y se corrigió un bug real en `order_service_impl.py` importando `mark_order_cancelled` desde el state machine canónico. Validación final: `test_menu_validation.py` => `18 passed`, `test_order_state_machine_v2.py` => `8 passed`, y `pytest pronto-tests/tests -q` => `312 passed, 4 skipped`. |
| 2026-03-07 | 1.0393 | 1.0394 | OpenCode | pronto-api, pronto-client, pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se corrigieron hallazgos de auditoría IA P0/P1: (P0-001) eliminó "client" de `@scope_required` en `pronto-api/src/api_app/routes/employees/orders.py`; (P0-002) agregó marca DEPRECATED con plan de retiro en `pronto-client/src/pronto_clients/routes/api/__init__.py`; (P1-001) normalizó headers de autenticación cliente a `X-PRONTO-CUSTOMER-REF` en `client_auth.py` e `invoices.py`; (P1) generó snapshots de esquema DB en `pronto-docs/contracts/pronto-postgresql/db_schema.sql` y `pronto-libs/db_schema.sql` mediante `pg_dump`. Validación: `pronto-ai-audit --agent api_scope_canon_auditor` => PASS. |
| 2026-03-07 | 1.0394 | 1.0395 | OpenCode | pronto-libs, pronto-docs, pronto-scripts/pronto-root | Se corrigió hallazgo P0 de auditoría IA: normalizó `credentials: 'same-origin'` a `credentials: 'include'` en `pronto-libs/src/pronto_shared/static/js/src/core/http.ts:58` según canon de AGENTS.md 12.2. Validación: `pronto-ai-audit --agent agents_sync_auditor --agent-timeout-seconds 600 --fail-on p1` => PASS. |
| 2026-03-07 | 1.0394 | 1.0395 | Augment Agent | pronto-scripts, pronto-docs, pronto-scripts/pronto-root | Se corrigieron dos bloqueos del tooling canónico al preparar el entorno live: `20260307_01__menu_category_slug_revision.sql` ahora detecta también relaciones existentes llamadas `uq_menu_categories_slug` para no colisionar con el índice único previo, y `40_seeds/0398__qa_menu_catalog.sql` quedó alineado con `pronto-init` reemplazando `DO UPDATE`/`DO $$` por patrones compatibles (`DO NOTHING` y `\gset` + `\if`). Validación: `pronto-migrate --apply` => OK, `pronto-migrate --check` => `pending=0 drift=0`, `pronto-init --apply` => `OK: init applied`, `pronto-init --check` => OK, `employees` health/login => 200. Los 4 skips live restantes persisten por falta de mesas activas y credenciales admin/system canónicas en la DB `pronto`. |
| 2026-03-07 | 1.0395 | 1.0396 | Augment Agent | pronto-api, pronto-employees, pronto-docs, pronto-scripts/pronto-root | Se completó la activación real de los 4 tests live restantes: se sembró el mínimo de datos live autorizados (área, 2 mesas activas, credenciales canónicas `admin@cafeteria.test` y `system@pronto.test`), se inicializaron defaults canónicos en `pronto_system_settings`, se corrigió el proxy legacy de config en `pronto-employees` para reenviar JWT/cookies/CSRF al upstream, y se habilitó un allowlist mínimo en `pronto-api/src/api_app/routes/config.py` para que `admin` pueda actualizar `system.api.items_per_page` sin abrir el resto de `system.*`. Validación final: `pytest pronto-tests/tests/functionality/e2e/test_client_api_business_logic_moved_to_api.py -q -rs` => `1 passed`; `pytest pronto-tests/tests/functionality/e2e/test_config_settings_roundtrip_live.py -q -rs` => `3 passed`; `pytest pronto-tests/tests -q` => `316 passed`. |
| 2026-03-07 | 1.0396 | 1.0397 | Augment Agent | root, pronto-docs, pronto-scripts/pronto-root | Se corrigió `pronto-apps.sh` para macOS/Bash 3.2 eliminando el uso de `eval` + `${app^^}` al resolver puertos en `Service Status` y `URLs`. Ahora el script usa `tr` para mayúsculas y expansión indirecta `${!port_var}`; el cambio se replicó en la copia versionada `pronto-scripts/pronto-root/pronto-apps.sh`. Validación: `bash -n pronto-apps.sh && bash -n pronto-scripts/pronto-root/pronto-apps.sh` => OK; `./pronto-apps.sh status` => exit 0 y listado completo de servicios/URLs sin `bad substitution`. |
| 2026-03-07 | 1.0397 | 1.0398 | Augment Agent | root, pronto-docs, pronto-scripts/pronto-root | Se endureció `pronto-dev.sh` para fallar temprano y con mensajes claros ante stacks/procesos paralelos ocupando puertos canónicos. El script ahora hace preflight de `5432`, `6379`, `9088`, `6082`, `6080`, `6081`, permite continuar si el puerto ya pertenece al servicio del proyecto actual, y aborta si Postgres no queda ready en 30s. El cambio se replicó en `pronto-scripts/pronto-root/pronto-dev.sh`. Validación: `bash -n pronto-dev.sh && bash -n pronto-scripts/pronto-root/pronto-dev.sh` => OK; `./pronto-dev.sh` con stack paralelo `pronto-app-*` => exit 1 temprano listando los seis conflictos; `docker compose ps -a` => vacío. |
| 2026-03-07 | 1.0398 | 1.0399 | Augment Agent | root, pronto-docs, pronto-scripts/pronto-root | Se mejoró la UX del preflight de `pronto-dev.sh` sin automatizar acciones destructivas: además de detectar temprano conflictos en `5432`, `6379`, `9088`, `6082`, `6080`, `6081`, ahora imprime pasos sugeridos de recuperación (`docker ps`, `lsof`) y, cuando los conflictos provienen de Docker, muestra un `docker stop <contenedores>` listo para copiar/pegar. El cambio se replicó en `pronto-scripts/pronto-root/pronto-dev.sh`. Validación: `bash -n pronto-dev.sh && bash -n pronto-scripts/pronto-root/pronto-dev.sh` => OK; `./pronto-dev.sh` => exit 1 con sugerencias claras y comando `docker stop pronto-app-*` derivado del entorno actual. |
| 2026-03-07 | 1.0399 | 1.0400 | Augment Agent | root, pronto-docs, pronto-scripts/pronto-root | Se restauró `start-api.sh` en root y se endureció junto con `pronto-scripts/pronto-root/start-api.sh` para evitar fallos tardíos por puertos ocupados: ahora ambos hacen preflight de `5432`, `6379`, `6082`, muestran sugerencias de recuperación (`docker ps`, `lsof`, `docker stop ...`) y abortan si Postgres no queda ready en 30s. Validación: `bash -n start-api.sh && bash -n pronto-scripts/pronto-root/start-api.sh` => OK; `./start-api.sh` con stack paralelo `pronto-app-*` => exit 1 temprano con mensaje accionable; confirmación final: `docker compose ps -a` => vacío y sin residuos `pronto-(postgres|redis|api)-1`. |
| 2026-03-07 | 1.0400 | 1.0401 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se auditó `root` y `pronto-scripts/bin` con foco en compatibilidad Bash 3.2 y fallos tardíos. Hallazgos confirmados documentados: `TEST-20260307-059` (`pronto-scripts/bin/down.sh` falla en macOS por uso de `mapfile`) y `TEST-20260307-060` (`pronto-scripts/bin/generate-product-images.sh --dry-run` intenta crear `/var/www/...` y falla por permisos antes de simular). Validación del audit: `bash --version` => `3.2.57`, `bash -n` sobre scripts root/bin relevantes => OK, `bash pronto-scripts/bin/down.sh` => `mapfile: command not found`, `bash pronto-scripts/bin/generate-product-images.sh --dry-run -l 1` => `Permission denied` en `/var/www/pronto-static/...`. |
| 2026-03-07 | 1.0401 | 1.0402 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se corrigió `pronto-scripts/bin/down.sh` para macOS/Bash 3.2 reemplazando `mapfile` por un loop portable `while IFS= read -r ...`. Validación: `bash -n pronto-scripts/bin/down.sh` => OK; `bash pronto-scripts/bin/down.sh` => exit 0 sin `mapfile: command not found`. |
| 2026-03-07 | 1.0402 | 1.0403 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se corrigió `pronto-scripts/bin/generate-product-images.sh` para que `--dry-run` no intente escribir en `/var/www`: ahora omite `mkdir -p` y `chmod` cuando `DRY_RUN=true`. Validación: `bash -n pronto-scripts/bin/generate-product-images.sh` => OK; `bash pronto-scripts/bin/generate-product-images.sh --dry-run -l 1` ya no falla por `Permission denied` en `/var/www` y avanza hasta el siguiente bloqueo independiente (`No se pudo obtener /api/menu en http://localhost:6081/api`); además se confirmó que el directorio por defecto no se crea en dry-run. |
| 2026-03-07 | 1.0403 | 1.0403 | opencode | pronto-client, pronto-api, pronto-docs, pronto-scripts/pronto-root | Corrección arquitectónica P0: headers DEPRECATED agregados a 17 archivos en pronto-client, plan de retiro detallado con 10 tickets de migración, documentación de migración en pronto-docs/migration-plans/ y pronto-docs/migration-tickets/, corrección de versión canónica. |
| 2026-03-07 | 1.0403 | 1.0404 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se eliminó el bashism restante de `pronto-scripts/bin/generate-product-images.sh`: `${category,,}` fue reemplazado por una normalización portable con `printf` + `tr` almacenada en `category_normalized`, compatible con Bash 3.2 de macOS. Validación: `bash -n pronto-scripts/bin/generate-product-images.sh` => OK; `rg` confirma que ya no existe `${category,,}`; smoke test equivalente en Bash 3.2 => `ok`; `--dry-run` mantiene el fix previo y ya no falla por permisos en `/var/www`. |
| 2026-03-07 | 1.0404 | 1.0405 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se corrigió `pronto-scripts/bin/lib/stack_helpers.sh` para Bash 3.2 eliminando `local -n` de `summarize_services()`. La función mantiene su interfaz pública pero ahora usa helpers portables `reset_named_array()` y `append_named_array()` con `eval` + quoting seguro (`printf -v ... %q`). Validación: `bash -n pronto-scripts/bin/lib/stack_helpers.sh` => OK; `rg` confirma ausencia de `local -n`; smoke tests bajo `set -euo pipefail` poblando `SERVICE_SUMMARY` y `FAILED_SERVICES` => OK, incluido el caso simple esperado por `pronto-scripts/bin/up.sh`. |
2026-03-07 | 1.0404 | 1.0405 | opencode | pronto-client, pronto-api, pronto-docs, pronto-scripts/pronto-root | Migración Fase 1 completada: split_bills y waiter_calls migrados a pronto-api, pronto-client convertido a BFF proxy sin lógica de negocio (split_bills.py, waiter_calls.py, stripe_payments.py, notifications.py), pronto-api extendido con módulos customers en /api/customers/, requests añadido a requirements.txt.
2026-03-07 | 1.0405 | 1.0406 | opencode | pronto-client, pronto-api, pronto-docs, pronto-scripts/pronto-root | Migración Fase 1 y Fase 2 completadas: pronto-client convertido completamente a BFF proxy sin lógica de negocio (split_bills.py, waiter_calls.py, stripe_payments.py, notifications.py, orders.py, sessions.py), pronto-api extendido con módulos customers (split_bills, waiter_calls), headers DEPRECATED mantenidos, pronto-api actualizado con endpoints proxy requeridos, requests añadido a requirements.txt.
2026-03-07 | 1.0406 | 1.0407 | opencode | pronto-client, pronto-api, pronto-docs, pronto-scripts/pronto-root | Migración Fase 3 completada: pronto-client convertido completamente a BFF proxy (feedback_email.py y stripe_webhooks.py), pronto-api ya tiene endpoints de feedback y webhooks necesarios.
2026-03-07 | 1.0407 | 1.0408 | opencode | pronto-employees, pronto-api, pronto-docs, pronto-scripts/pronto-root | Migración pronto-employees completada: sessions.py y promotions.py convertidos a BFF proxy, auth en todos los scopes (cashier, chef, waiter, admin, system) convertidos a BFF proxy, pronto-employees YA NO escribe a DB, pronto-api ya tiene endpoints de auth y promociones necesarios.
2026-03-07 | 1.0408 | 1.0409 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se completó el hardening funcional de `pronto-scripts/bin/generate-product-images.sh`: ahora consume por defecto la API canónica (`PRONTO_API_URL` o `http://localhost:6082`) en lugar de `EMPLOYEE_API_BASE_URL`/`6081`, y su parser soporta el payload actual de `/api/menu` (`data.catalog_items`) manteniendo compatibilidad hacia atrás con `categories[].items[]`. Validación: `bash -n pronto-scripts/bin/generate-product-images.sh` => OK; `bash pronto-scripts/bin/generate-product-images.sh --dry-run -l 1` => exit 0 listando `Agua Mineral`; `bash pronto-scripts/bin/generate-product-images.sh --dry-run -l 2 -c 'Bebidas'` => exit 0 listando 2 productos.
2026-03-07 | 1.0408 | 1.0409 | opencode | pronto-client, pronto-docs, pronto-scripts/pronto-root | Cleanup FINAL completado: archivos mantenidos en pronto-client actualizados con headers apropiados (BFF Proxy, BFF Cache, SSR-Only, UI-Only), headers DEPRECATED eliminados de archivos mantenidos, __init__.py actualizado sin DEPRECATED.
2026-03-07 | 1.0409 | 1.0410 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se corrigió `pronto-scripts/bin/check-seed-status.sh` para seguir el contrato actual: usa por defecto la API canónica (`PRONTO_API_URL` o `http://localhost:6082`), verifica salud en `/health` en lugar de `/api/health`, y parsea primero `data.catalog_items` manteniendo compatibilidad hacia atrás con `categories[].items[]`. Validación: `bash -n pronto-scripts/bin/check-seed-status.sh` => OK; `bash pronto-scripts/bin/check-seed-status.sh` => exit 0, reportando `20` productos y `9` categorías en el entorno actual.
2026-03-07 | 1.0408 | 1.0409 | opencode | pronto-client, pronto-employees, pronto-static, pronto-api, pronto-docs, pronto-scripts/pronto-root | Auditorías completas: pronto-client (migrado a BFF proxy), pronto-employees (migrado a BFF proxy), pronto-static (auditoría aprobada), pronto-api (autoridad única de API), cleanup de headers DEPRECATED, validación final de arquitectura. Todos los servicios cumplen con AGENTS.md.

2026-03-07 | 1.0410 | 1.0411 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se endureció `pronto-scripts/bin/cleanup-old-sessions.sh` en modo seguro: se eliminó el auto-`pip install`, se agregaron `--help` y `--dry-run`, se aclaró que sin flags no realiza limpieza, el wrapper usa la API canónica solo para `--all` y cae al script offline cuando corresponde, y normaliza `POSTGRES_HOST`/`REDIS_URL` de hostnames Docker a `localhost` para ejecuciones offline desde el host local. Validación: `bash -n pronto-scripts/bin/cleanup-old-sessions.sh` => OK; `bash pronto-scripts/bin/cleanup-old-sessions.sh --help` => OK; `bash pronto-scripts/bin/cleanup-old-sessions.sh` => exit 0 informativo; `bash pronto-scripts/bin/cleanup-old-sessions.sh --dry-run` => exit 0 mostrando estadísticas y 0 sesiones cerradas; `bash pronto-scripts/bin/cleanup-old-sessions.sh --all --dry-run` => exit 0 con preview total.

2026-03-07 | 1.0411 | 1.0412 | Augment Agent | pronto-docs, pronto-scripts, pronto-scripts/pronto-root | Se completó el saneamiento del flujo offline de limpieza de sesiones: `pronto-scripts/bin/python/clean-sessions.py` ahora construye la conexión Redis usando `REDIS_URL` o `REDIS_HOST`/`REDIS_PORT`/`REDIS_PASSWORD`, y `pronto-scripts/bin/cleanup-old-sessions.sh` superpone el `.env` root del workspace sobre `pronto-scripts/.env` para heredar `REDIS_PASSWORD` canónico. Validación: `python3 -m py_compile pronto-scripts/bin/python/clean-sessions.py` => OK; con entorno real cargado, `python3 pronto-scripts/bin/python/clean-sessions.py --all --dry-run` => exit 0 y `Claves de Redis encontradas: 0`; `bash pronto-scripts/bin/cleanup-old-sessions.sh --all --dry-run` => exit 0 sin warning de autenticación Redis. |

2026-03-07 | 1.0412 | 1.0413 | Augment Agent | pronto-client, pronto-docs, pronto-scripts/pronto-root | Se corrigió el lote abierto de `pronto-client` en `routes/api/`: se añadió el helper `_upstream.py` para resolver BFF→API con precedencia `PRONTO_API_INTERNAL_BASE_URL` → `PRONTO_API_BASE_URL` → `http://localhost:6082`, se eliminaron todos los hardcodes `http://api:5000`, y se arreglaron imports faltantes en `support.py` (`success_response`) y `tables.py` (`request`). Además, se verificó que los expedientes `CLIENT-20260307-001` y `CLIENT-20260307-002` ya estaban corregidos en el árbol actual y se cerraron como resueltos. Validación: `rg -n 'http://api:5000' pronto-client/src/pronto_clients/routes/api -g '*.py'` => sin resultados; `python3 -m py_compile` sobre todos los módulos proxy tocados => OK; checks textuales de imports en `support.py` y `tables.py` => OK. |

2026-03-07 | 1.0413 | 1.0414 | Augment Agent | pronto-client, pronto-docs, pronto-scripts/pronto-root | Se completó el lote siguiente de `pronto-client`: se agregó `GET /api/auth/csrf` en el BFF `routes/api/auth.py` usando `generate_csrf()` y `success_response`, y se corrigieron las plantillas legacy `index.html` e `index-alt.html` para usar `credentials: 'include'` en lugar de `same-origin` al consultar `/api/business-info`. Validación: `python3 -m py_compile pronto-client/src/pronto_clients/routes/api/auth.py` => OK; `rg -n "same-origin" pronto-client/src/pronto_clients/templates -g '*.html'` => sin resultados; verificación textual confirma presencia de la ruta `/csrf` y de `generate_csrf` en `auth.py`. |

2026-03-07 | 1.0414 | 1.0415 | Augment Agent | pronto-client, pronto-docs, pronto-scripts/pronto-root, pronto-tests | Se eliminó la superficie duplicada de health en `pronto-client`: se dejó únicamente `/health`, retirando el registro del blueprint legacy `/api/health` y eliminando `routes/api/health.py`. También se actualizaron diagnósticos de `pronto-tests` y el README vivo de `pronto-client` para usar/documentar `/health`. Validación: `python3 -m py_compile pronto-client/src/pronto_clients/routes/api/__init__.py pronto-tests/scripts/diagnostic/api/backend_diagnostic.py pronto-tests/scripts/diagnostic/ui/frontend_diagnostic.py` => OK; `test ! -f pronto-client/src/pronto_clients/routes/api/health.py` => OK; grep sin referencias activas a `/api/health` en `pronto-client`, `pronto-tests/scripts/diagnostic` y `pronto-docs/pronto-clients/README.md`. |

2026-03-07 | 1.0415 | 1.0416 | Augment Agent | pronto-client, pronto-docs, pronto-scripts/pronto-root | Se saneó `debug_panel.html` de `pronto-client`: se retiraron las llamadas a endpoints legacy/no definidos (`/api/session/...`, `/api/debug/...`, `/api/support-tickets`, `/api/promotions`) y se reemplazaron por mensajes informativos via `showStatus(...)`, indicando cuando corresponde usar el panel de empleados. Validación: `rg -n '/api/session/|/api/debug/|/api/support-tickets|/api/promotions' pronto-client/src/pronto_clients/templates/debug_panel.html` => sin resultados; verificación textual confirma los mensajes informativos nuevos. |

2026-03-07 | 1.0416 | 1.0417 | Augment Agent | pronto-api, pronto-docs, pronto-scripts/pronto-root | Se cerraron como resueltos dos expedientes stale de `pronto-api/customers/waiter_calls.py`: el archivo actual ya resuelve `customer_ref` desde `X-PRONTO-CUSTOMER-REF` y ya importa `datetime, timezone`, por lo que no persisten los bugs `API-20260307-001` ni `API-20260307-002`. Validación: inspección directa del módulo y `python3 -m py_compile pronto-api/src/api_app/routes/customers/waiter_calls.py` => OK. |

2026-03-07 | 1.0417 | 1.0418 | Augment Agent | pronto-docs, pronto-employees, pronto-libs, pronto-scripts/pronto-root, tmp | Se cerró un lote de higiene documental/local: el artefacto temporal inválido reportado en `pronto-employees` ya no existe, y el residuo `pronto-libs/db_schema.sql` fue movido a `tmp/pronto-libs/db_schema.sql` para cumplir la regla de no mantener `.sql` en roots de proyecto. Validación: check de existencia del path inválido => ausente; `test -f tmp/pronto-libs/db_schema.sql && test ! -f pronto-libs/db_schema.sql` => OK. |

2026-03-07 | 1.0418 | 1.0419 | Augment Agent | pronto-docs, pronto-scripts/pronto-root, pronto-static | Se corrigió una violación de nomenclatura en `pronto-static`: el script `create_avatar.sh` fue renombrado a `create-avatar.sh` para cumplir el canon `kebab-case.sh`. Validación: `bash -n pronto-static/create-avatar.sh` => OK; el path nuevo existe y el viejo ya no. |

2026-03-07 | 1.0419 | 1.0420 | Augment Agent | pronto-api, pronto-docs, pronto-scripts/pronto-root, pronto-tests | Se cerró un lote de seguridad en `pronto-api`: `client_auth.py` ahora usa `require_customer_session` y `get_current_customer()` en `GET/PUT /api/client-auth/me` en lugar de interpretar `X-PRONTO-CUSTOMER-REF` como UUID directo; las rutas admin de `invoices` ahora exigen `@jwt_required` + `@scope_required(["admin", "system"])`; y `GET /api/business-info` quedó protegido con JWT admin/system. Además se añadieron regresiones self-contained en `pronto-api/tests/test_security_regressions.py` y se actualizaron tests contractuales de `pronto-tests` para reflejar 401 anónimo en `/api/business-info`. Validación: `python3 -m py_compile pronto-api/src/api_app/routes/client_auth.py pronto-api/src/api_app/routes/invoices.py pronto-api/src/api_app/routes/employees/business_info.py pronto-api/tests/test_security_regressions.py pronto-tests/tests/functionality/integration/test_business_config_api.py pronto-tests/tests/functionality/api/api-tests/test_business_config_api.py` => OK; `pronto-api/.venv/bin/python -m pytest pronto-api/tests/test_security_regressions.py -q` => `6 passed`. |

2026-03-07 | 1.0420 | 1.0421 | Augment Agent | pronto-api, pronto-docs, pronto-scripts/pronto-root, pronto-tests | Se corrigió `API-20260307-006` en `pronto-api/src/api_app/routes/invoices.py`: los endpoints cliente de invoices ahora exigen actor autenticado, aplican ownership por sesión para clientes y permiten solo scopes `cashier`, `admin`, `system` en operaciones operativas de empleados. `POST /client/invoices/request` ya no permite que un cliente facture otro `customer_id`; `GET /client/invoices` quedó amarrado a la sesión autenticada; y `GET/POST` sobre `/<invoice_id>`, `email`, `cancel`, `pdf`, `xml` buscan primero el `Invoice` local y ocultan facturas ajenas con `404`. También se añadieron regresiones a `pronto-api/tests/test_security_regressions.py` y se actualizó `pronto-tests/tests/functionality/invoice-flow.spec.ts` para reflejar `401` anónimo. Validación: `python3 -m py_compile pronto-api/src/api_app/routes/invoices.py pronto-api/tests/test_security_regressions.py` => OK; `pronto-api/.venv/bin/python -m pytest pronto-api/tests/test_security_regressions.py -q` => `11 passed`. |

2026-03-07 | 1.0421 | 1.0422 | Augment Agent | pronto-api, pronto-docs, pronto-libs, pronto-scripts/pronto-root | Se corrigió `split_bills` del lado cliente en `pronto-api/src/api_app/routes/customers/split_bills.py`: se reemplazó la auth débil basada solo en `X-PRONTO-CUSTOMER-REF` por `@require_customer_session` + `get_current_customer()`, se añadió ownership real de `DiningSession`/`SplitBill`, se validó que `OrderItem` pertenezca a la misma sesión del split y `pay_split_person` dejó de mutar `payment_status` en la ruta para delegar a `pronto_shared.services.split_bill_service.process_person_payment`. En `pronto-libs/src/pronto_shared/services/split_bill_service_core.py` se amplió soporte de métodos (`cash`, `card`, `clip`, `stripe`) y se estabilizó el payload de respuesta del pago individual. Validación: `python3 -m py_compile pronto-api/src/api_app/routes/customers/split_bills.py pronto-libs/src/pronto_shared/services/split_bill_service_core.py pronto-api/tests/test_security_regressions.py` => OK; `pronto-api/.venv/bin/python -m pytest pronto-api/tests/test_security_regressions.py -q` => `17 passed`; gate local `rg -n --hidden 'payment_status\s*=' pronto-api/src/api_app/routes/customers/split_bills.py` => limpio. El intento de `pronto-tests/tests/functionality/integration/test_split_bill.py -q` quedó bloqueado por entorno (`database "pronto_test" does not exist`). |

2026-03-07 | 1.0422 | 1.0423 | Augment Agent | pronto-api, pronto-docs, pronto-employees, pronto-scripts/pronto-root | Se corrigió `API-20260307-010` en `pronto-api/src/api_app/routes/config.py`: `GET /api/config/store_cancel_reason` y `GET /api/config/client_session_validation_interval_minutes` ahora exigen `@require_customer_session`, y el alias legacy `GET /api/config/public` quedó restringido con `@jwt_required` + `@admin_required` para que el bootstrap público siga viviendo solo en la ruta canónica `GET /api/public/config`. Además, `pronto-employees/src/pronto_employees/routes/api/config.py` fue actualizado para reenviar `GET /public/config` hacia `/api/public/config` en vez de `/api/config/public`. Se añadieron regresiones en `pronto-api/tests/test_security_regressions.py`. Validación: `python3 -m py_compile pronto-api/src/api_app/routes/config.py pronto-employees/src/pronto_employees/routes/api/config.py pronto-api/tests/test_security_regressions.py` => OK; `pronto-api/.venv/bin/python -m pytest pronto-api/tests/test_security_regressions.py -q` => `21 passed`. |

2026-03-07 | 1.0423 | 1.0424 | Augment Agent | pronto-client, pronto-docs, pronto-scripts/pronto-root | Se corrigió `CLIENT-20260307-007` en `pronto-client/src/pronto_clients/routes/web.py`: las vistas SSR de negocio (`/`, `/checkout`, `/menu-alt`, `/feedback`) ahora exigen cliente autenticado vía `session['customer_ref']` + `customer_session_store`; se añadieron rutas públicas explícitas `/login` y `/register`; `/feedback` valida ownership de `DiningSession.customer_id`; y `GET /kiosk/<location>` dejó de ser público indiscriminado al requerir sesión kiosk coincidente o bootstrap autorizado por el mismo gate de kiosko. Además se añadió `pronto-client/src/pronto_clients/templates/auth.html` y la cobertura self-contained `pronto-client/tests/test_web_auth_regressions.py`. Validación: `python3 -m py_compile pronto-client/src/pronto_clients/routes/web.py pronto-client/tests/test_web_auth_regressions.py` => OK; `PYTHONPATH=pronto-client/src:pronto-libs/src pronto-api/.venv/bin/python -m pytest pronto-client/tests/test_web_auth_regressions.py -q` => `6 passed`. |

2026-03-07 | 1.0424 | 1.0425 | Augment Agent | pronto-docs, pronto-employees, pronto-scripts/pronto-root | Se corrigió `EMP-20260307-003` en `pronto-employees/src/pronto_employees/routes/api/sessions.py`: el proxy técnico de sesiones ahora exige `@jwt_required` en `POST /anon-id/<session_id>`, `GET /dining-sessions/<session_id>` y `POST /dining-sessions/<session_id>/close`, cerrando el acceso anónimo desde `pronto-employees`. Además, el módulo dejó de usar `http://api:5000` hardcodeado localmente y ahora resuelve el upstream mediante `_get_api_base_url()` basado en `current_app.config['PRONTO_API_URL']` (avance parcial sobre el expediente de hosts hardcodeados, no cierre completo). Se añadió la suite self-contained `pronto-employees/tests/test_sessions_proxy_security_regressions.py`. Validación: `python3 -m py_compile pronto-employees/src/pronto_employees/routes/api/sessions.py pronto-employees/tests/test_sessions_proxy_security_regressions.py` => OK; `PYTHONPATH=pronto-employees/src:pronto-libs/src pronto-api/.venv/bin/python -m pytest pronto-employees/tests/test_sessions_proxy_security_regressions.py -q` => `4 passed`. |

2026-03-07 | 1.0425 | 1.0426 | Augment Agent | pronto-docs, pronto-employees, pronto-static, pronto-tests, pronto-scripts/pronto-root | Se corrigieron `EMP-20260307-004`, `STA-20260307-006` y `STA-20260307-002`. En `pronto-employees/src/pronto_employees/routes/api/config.py` y `stats.py`, los endpoints `GET /public/config`, `GET /public/stats` y `GET /stats/public` ahora exigen `@jwt_required`; además `config.py` pasó a resolver `PRONTO_API_URL` desde `current_app.config`. En `pronto-employees/src/pronto_employees/app.py` + `templates/index.html` se añadió bootstrap SSR de `window.APP_CONFIG`. En `pronto-static/src/vue/employees/shared/store/config.ts` el frontend employees dejó de pedir `GET /api/public/config` y ahora hidrata desde SSR; `LoginShell.vue` y `StaffLogin.vue` dejaron de consumir `/api/public/stats`, eliminando también el `fetch()` directo fuera del wrapper. Se actualizó `pronto-tests/tests/functionality/e2e/test_config_settings_roundtrip_live.py` para usar la ruta canónica `http://localhost:6082/api/public/config`. Validación: `python3 -m py_compile pronto-employees/src/pronto_employees/routes/api/stats.py pronto-employees/src/pronto_employees/routes/api/config.py pronto-employees/src/pronto_employees/app.py pronto-employees/tests/test_public_endpoint_security_regressions.py pronto-tests/tests/functionality/e2e/test_config_settings_roundtrip_live.py` => OK; `PYTHONPATH=pronto-employees/src:pronto-libs/src pronto-api/.venv/bin/python -m pytest pronto-employees/tests/test_public_endpoint_security_regressions.py -q` => `3 passed`; `npm --prefix pronto-static run build` => OK; `npx playwright test tests/functionality/e2e/test_login_flows.spec.ts` => `10 passed`; `npx playwright test tests/functionality/ui/playwright-tests/employees/login-redirect-fix.spec.ts` => `18 passed`. Intento adicional: `npx playwright test tests/functionality/ui/playwright-tests/employees/auth.spec.ts` falló por una expectativa vieja de redirect (`/admin/dashboard`/`/waiter/dashboard`) mientras la app navega a `/dashboard/reports` y `/dashboard/waiter`; no se observó falla del login shell ni del endurecimiento aplicado. |

2026-03-07 | 1.0426 | 1.0427 | Augment Agent | pronto-docs, pronto-employees, pronto-scripts/pronto-root | Se corrigió `EMP-20260307-005` centralizando la resolución de upstream en `pronto-employees/src/pronto_employees/routes/_upstream.py`. Los módulos `routes/{waiter,chef,cashier,admin,system}/auth.py`, `routes/api/promotions.py`, `routes/api/proxy_console_api.py`, `routes/api/config.py` y `routes/api/sessions.py` dejaron de usar literales `http://api:5000`, `http://pronto-api:5000` o `http://localhost:5000` y ahora consumen `get_api_base_url()` basado en `current_app.config['PRONTO_API_URL']`. Se añadió `pronto-employees/tests/test_upstream_base_url.py`. Validación: `python3 -m py_compile pronto-employees/src/pronto_employees/routes/_upstream.py pronto-employees/src/pronto_employees/routes/waiter/auth.py pronto-employees/src/pronto_employees/routes/chef/auth.py pronto-employees/src/pronto_employees/routes/cashier/auth.py pronto-employees/src/pronto_employees/routes/admin/auth.py pronto-employees/src/pronto_employees/routes/system/auth.py pronto-employees/src/pronto_employees/routes/api/promotions.py pronto-employees/src/pronto_employees/routes/api/proxy_console_api.py pronto-employees/src/pronto_employees/routes/api/config.py pronto-employees/src/pronto_employees/routes/api/sessions.py pronto-employees/tests/test_upstream_base_url.py` => OK; `PYTHONPATH=pronto-employees/src pronto-api/.venv/bin/python -m pytest pronto-employees/tests/test_upstream_base_url.py -q` => `1 passed`; `rg -n 'http://api:5000|http://pronto-api:5000|http://localhost:5000' pronto-employees/src/pronto_employees -g '*.py'` deja solo la configuración central (`app.py`) y el helper canónico `_upstream.py`. |

2026-03-07 | 1.0427 | 1.0428 | Augment Agent | pronto-docs, pronto-tests, pronto-scripts/pronto-root | Se corrigió `TEST-20260307-061` en `pronto-tests/tests/functionality/ui/playwright-tests/employees/auth.spec.ts`: el archivo seguía esperando redirects legacy a `/<scope>/dashboard` tras login aunque la navegación real termina en rutas canónicas `/dashboard/*`. Se añadió `DASHBOARD_URL_PATTERN` y se reemplazaron los `waitForURL` rígidos de admin/waiter por ese patrón compartido. Validación: `npx playwright test tests/functionality/ui/playwright-tests/employees/auth.spec.ts` => `3 passed`. |

2026-03-07 | 1.0428 | 1.0429 | Augment Agent | pronto-docs, pronto-employees, pronto-scripts/pronto-root | Se corrigió `EMP-20260307-002` en `pronto-employees/src/pronto_employees/routes/api/promotions.py`: el proxy de promociones ahora exige `@jwt_required` + `@admin_required` en `list/create/get/update/delete`, cerrando el acceso anónimo y restringiendo la operación a admin/system. Además, `_forward_to_api()` ahora reenvía `Authorization`, `Cookie`, `X-App-Context`, `X-CSRFToken` y `X-Correlation-ID`, alineándolo con el patrón seguro de otros proxies BFF de employees. Se añadió `pronto-employees/tests/test_promotions_proxy_security_regressions.py`. Validación: `python3 -m py_compile pronto-employees/src/pronto_employees/routes/api/promotions.py pronto-employees/tests/test_promotions_proxy_security_regressions.py` => OK; `PYTHONPATH=pronto-employees/src:pronto-libs/src pronto-api/.venv/bin/python -m pytest pronto-employees/tests/test_promotions_proxy_security_regressions.py -q` => `5 passed`. |

2026-03-07 | 1.0429 | 1.0430 | Augment Agent | pronto-docs, pronto-employees, pronto-scripts/pronto-root | Se corrigió `EMP-20260307-008` en `pronto-employees/src/pronto_employees/routes/api/proxy_console_api.py`: se eliminó la excepción que permitía `admin/system` sobre scopes distintos, alineando `_validate_scope_match()` con AGENTS 12.4.4 para que cualquier `jwt_role != scope` responda `403 SCOPE_MISMATCH`. Se añadió `pronto-employees/tests/test_proxy_console_scope_mismatch.py` cubriendo el caso válido y los cruces rechazados `admin->waiter` y `system->cashier`. Validación: `python3 -m py_compile pronto-employees/src/pronto_employees/routes/api/proxy_console_api.py pronto-employees/tests/test_proxy_console_scope_mismatch.py` => OK; `PYTHONPATH=pronto-employees/src:pronto-libs/src pronto-api/.venv/bin/python -m pytest pronto-employees/tests/test_proxy_console_scope_mismatch.py -q` => `3 passed`. |

2026-03-07 | 1.0430 | 1.0431 | Augment Agent | pronto-docs, pronto-scripts/pronto-root | Se cerró el expediente `EMP-20260307-001` como falso positivo tras validar el `url_map` real de `pronto-employees` en `PRONTO_ROUTES_ONLY=1`. Aunque `api_bp` se define con `url_prefix='/api'` y se registra con `url_prefix=f'/{scope}/api'`, Flask expone efectivamente rutas `/{scope}/api/*` (por ejemplo `/admin/api/config`, `/waiter/api/config`) y no `/{scope}/api/api/*`. Evidencia adicional: `rg -n '/api/api/' pronto-tests pronto-static pronto-employees` no devolvió referencias activas. No se requirieron cambios de código. |

2026-03-07 | 1.0437 | 1.0438 | Codex GPT-5 | pronto-api, pronto-client | Se completó validación integral de autenticación y flujos de consola. Fixes aplicados: (1) pronto-client `routes/api/auth.py` ahora expone proxy `GET/PUT /api/client-auth/me` y soporta método PUT en forwarder; (2) pronto-api `routes/client_auth.py` corrigió `GET/PUT /api/client-auth/me` para aceptar `customer_id` del session store (`id` legacy o `customer_id` canónico). Verificación: matriz auth empleados (waiter/chef/cashier/admin/system) login→me→dashboard→logout, aislamiento cross-scope (401), flujo cliente register→me→logout→me(401), smoke UI Playwright clickability en todas las consolas y parity checks `pronto-api-parity-check employees|clients` en estado OK. |

2026-03-07 | 1.0438 | 1.0439 | Codex GPT-5 | pronto-static | Rediseño visual del login de empleados en `pronto-static/src/vue/employees/shared/views/StaffLogin.vue`: nueva atmósfera con gradientes y capas ambientales, jerarquía tipográfica más sólida, badge superior, CTA más prominente, mejoras de foco/accesibilidad, pulido de inputs/botón vía overrides scope-safe y ajuste responsive móvil. Verificación: `npm run build:employees` OK, `sync-static-content.sh upload` OK, capturas de validación en `tmp/login-redesign-desktop.png` y `tmp/login-redesign-mobile.png`. |

2026-03-07 | 1.0439 | 1.0440 | Codex GPT-5 | pronto-employees, pronto-static | Se normalizó el acceso por prefijo de consola para empleados. Backend: se agregaron rutas root por scope en `routes/{waiter,chef,cashier,admin,system}/auth.py` (`/{scope}` y `/{scope}/`) con redirect a `/{scope}/dashboard`. Frontend: en `pronto-static/src/vue/employees/shared/router/index.ts` se robusteció detección de scope para paths con y sin slash final (`/scope` y `/scope/...`) en `getCurrentAppContext` y `getPathScope`. Validación: `/waiter|/chef|/cashier|/admin|/system` responden 302 a su dashboard; login/dashboard/me/logout por cada scope en 6081 en estado OK (`FAIL_COUNT 0`). |

2026-03-07 | 1.0440 | 1.0441 | Codex GPT-5 | pronto-static, pronto-employees | Se reforzó aislamiento por contexto de consola. Cambios: (1) login de empleados redirige por `active_scope` a `/{scope}/dashboard` (StaffLogin); (2) `auth.fetchUser()` normaliza y persiste `active_scope` usando `APP_CONTEXT` cuando falta; (3) dashboard default por contexto en router (`admin`->Reports, `system`->Config) y mejora de detección scope `/scope` y `/scope/...`; (4) App.vue ajusta `getPathScope` regex y `dashboardRouteName` por contexto; (5) router `afterEach` preserva URL namespaced reescribiendo `/dashboard*` a `/{scope}/dashboard*`; (6) backend employees agrega root por consola `/{scope}` y `/{scope}/` con redirect a `/{scope}/dashboard`. Validación: Playwright login por scopes waiter/chef/cashier/admin/system con URL final prefijada (`PASS` en los 5), y check visual de contexto (`Administración` en admin, `Sistema` en system). |

2026-03-07 | 1.0441 | 1.0442 | Codex GPT-5 | pronto-static | Separación explícita de vistas por contexto en consolas empleados. `admin` y `system` ya no comparten menú/recurso principal. Cambios: `App.vue` divide `visibleSections` por scope (`admin` y `system` con sets distintos), `Sidebar.vue` limita módulos operativos (`kitchen/cashier`) solo al contexto `admin`, `DashboardView.vue` agrega tabs faltantes (`promotions`, `feedback-config`, `categories`) y separa `adminTabs` vs `systemTabs`, y `router/index.ts` ajusta `allowedByRole` para mantener aislamiento de rutas por contexto. Validación: Playwright admin/system muestran nav distintos (`admin`: kitchen/cashier/employees/kiosks/reports; `system`: employees/kiosks/roles/reports/config), y smoke auth de los 5 scopes en 6081 con `FAIL_COUNT 0`. |

2026-03-07 | 1.0442 | 1.0443 | Codex GPT-5 | pronto-employees, pronto-static | Endurecimiento de navegación y ownership por subcontexto. Frontend: `Sidebar.vue` y `DashboardView.vue` ahora emiten hrefs namespaced (`/{scope}/...`) en links internos; `router/index.ts` y `auth.ts` refuerzan matriz de acceso por `active_scope` (admin->admin/waiter/chef/cashier, system->admin/system, waiter|chef|cashier solo su consola). Backend employees: `routes/{waiter,chef,cashier,admin,system}/auth.py` valida `employee.role` en login y bloquea con `403 SCOPE_MISMATCH` cuando el rol no corresponde al scope. Validación: smoke Playwright con enlaces internos sin fugas de prefijo (`badLinks=[]` en scopes validados), y matriz representativa de login por scope con `FAIL_COUNT 0` (ejemplos: waiter->admin denegado 403, admin(system-role account) -> waiter denegado 403, admin(role account) -> system denegado 403). |

2026-03-08 | 1.0443 | 1.0444 | Augment Agent | pronto-client, pronto-libs, pronto-tests, pronto-docs, pronto-scripts/pronto-root | Se corrigió el flujo cliente de órdenes y su cobertura asociada: `pronto-client` ahora proxyea órdenes de cliente de forma transparente hacia rutas canónicas (`/api/customer/orders` y `request-check`) sin reenvolver el payload upstream; `pronto-libs` devuelve errores de validación de órdenes cliente como 4xx en lugar de 500 cuando faltan modificadores requeridos; `pronto-tests` actualizó el E2E live de negocio cliente y el Playwright de menú para reflejar el contrato/auth actuales; y se registraron los expedientes resueltos con versión `1.0444`. Validación: `./pronto-libs/.venv/bin/python3 -m pytest pronto-tests/tests/functionality/e2e/test_client_api_business_logic_moved_to_api.py -q -rs`, `./pronto-scripts/bin/pronto-api-parity-check employees --json`, `./pronto-scripts/bin/pronto-api-parity-check clients --json`, `./pronto-scripts/bin/pronto-routes-only-check employees --json`, `./pronto-scripts/bin/pronto-routes-only-check clients --json`, `./pronto-scripts/bin/pronto-routes-only-check api --json` y `./pronto-scripts/bin/pronto-smoke-api-isolation`. |

2026-03-08 | 1.0444 | 1.0445 | Augment Agent | pronto-client, pronto-employees, pronto-scripts/pronto-root, pronto-docs | Se consolidó el rol de `pronto-client` y `pronto-employees` como BFFs/proxies técnicos hacia `pronto-api`: en `pronto-employees` se eliminaron blueprints locales de negocio bajo `routes/api/*`, se centralizó resolución upstream, se endureció el proxy de consola por scope y se añadieron regresiones de seguridad; en `pronto-client` se simplificaron rutas API/web para delegar auth y negocio al API canónico, con ajuste de templates/auth SSR y regresiones web. Validación: `PYTHONPATH=src:../pronto-libs/src ../pronto-api/.venv/bin/python -m pytest tests/test_upstream_base_url.py tests/test_sessions_proxy_security_regressions.py tests/test_promotions_proxy_security_regressions.py tests/test_proxy_console_scope_mismatch.py tests/test_public_endpoint_security_regressions.py -q` en `pronto-employees` (`17 passed`), `PYTHONPATH=src:../pronto-libs/src ../pronto-api/.venv/bin/python -m pytest tests/test_web_auth_regressions.py -q` en `pronto-client` (`6 passed`), además de `./pronto-scripts/bin/pronto-routes-only-check employees --json`, `./pronto-scripts/bin/pronto-routes-only-check clients --json`, `./pronto-scripts/bin/pronto-api-parity-check employees --json` y `./pronto-scripts/bin/pronto-api-parity-check clients --json` en verde. |

2026-03-08 | 1.0445 | 1.0446 | Augment Agent | pronto-api, pronto-libs, pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se corrigió el contrato de analytics/reportes: `pronto-libs` dejó de envolver respuestas del `AnalyticsService` con `success_response`, devolviendo payloads crudos para que `pronto-api/src/api_app/routes/reports.py` mantenga la única envoltura HTTP; además se corrigió compatibilidad SQLAlchemy en `employee_analytics.py` y `reports.py` ahora tolera fechas inválidas con fallback seguro. `pronto-tests` alineó las aserciones de analytics para usar `response.get_json()` y el sobre de respuesta real. Validación: smoke de import/parse en `reports.py` con `../pronto-libs/.venv/bin/python` OK; la suite completa de analytics quedó bloqueada por entorno local sin base `pronto_test` (`psycopg2.OperationalError: database \"pronto_test\" does not exist`), sin evidencia de fallo funcional propio del lote. |

2026-03-08 | 1.0446 | 1.0447 | Augment Agent | pronto-libs, pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se endurecieron contratos de serialización y validación fiscal: `pronto_shared.serializers` ahora enmascara también teléfono para correos anónimos, tolera `created_at/opened_at` nulos y usa fallback al `session.customer` cuando `order.customer` falta; además se añadió `pronto_shared.utils.tax_validation` con validaciones SAT de RFC, régimen fiscal, código postal y catálogos auxiliares. `pronto-tests` agregó cobertura unitaria para estos contratos. Validación: `PYTHONPATH=.:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/unit/test_serializers.py tests/unit/test_tax_validation.py -q` en `pronto-tests` (`45 passed`), más smoke de import en `pronto-libs` OK. |

2026-03-08 | 1.0447 | 1.0448 | Augment Agent | pronto-api, pronto-libs, pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se aseguró el contrato de business info: `pronto-api/src/api_app/routes/employees/business_info.py` ahora requiere JWT + scope `admin/system` en lectura y deja de tratar el endpoint como público; `BusinessScheduleService` dejó de asumir una columna `id` en `pronto_business_schedule`, alineándose al schema real; `pronto-tests` actualizó las pruebas para exigir autenticación en `/api/business-info`. Validación: smoke de import del servicio y blueprint OK con `../pronto-libs/.venv/bin/python`; las suites de integración específicas quedaron bloqueadas por entorno local sin base `pronto_test` (`psycopg2.OperationalError: database \"pronto_test\" does not exist`). |

2026-03-08 | 1.0448 | 1.0449 | Augment Agent | pronto-api, pronto-libs, pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se cerró el lote de menú/recommendations alineando el contrato canónico actual: `pronto_shared.services.menu_validation` acepta payloads con `menu_category_id` sin exigir `category` legacy y corrige el manejo de `InvalidOperation`; `DayPeriodService` vuelve a exponer `update_item_periods()` para asignaciones de recomendaciones; la fachada `recommendation_service.py` exporta wrappers estables (`get_global_recommendations`, `update_item_recommendations`) y `pronto-api/src/api_app/routes/employees/menu_items.py` mapea `ValueError` a 404/400 en vez de responder 500. `pronto-tests` reescribió las suites stale de menú/recommendations para usar `authenticated_client`, UUIDs canónicos y el envelope de respuesta real. Validación: `PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/unit/test_menu_validation.py tests/functionality/unit/test_models.py tests/functionality/api/api-tests/test_menu_validation_api.py tests/functionality/api/api-tests/test_recommendations_api.py tests/functionality/integration/test_menu_validation_api.py tests/functionality/integration/test_recommendations_api.py -q` en `pronto-tests` (`49 passed`), más smoke imports en `pronto-libs` y `pronto-api` OK. |

2026-03-08 | 1.0449 | 1.0450 | Augment Agent | pronto-api, pronto-libs, pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se reforzó la autoridad única de transiciones de órdenes: `pronto_shared.services.order_service_impl` reemplazó una escritura directa de `payment_status` por `mark_order_awaiting_tip(order)` y quedó cubierta por `pronto-libs/tests/unit/test_order_state_authority_regressions.py`; además `pronto-api/src/api_app/routes/employees/orders.py` dejó de permitir scope `client` en `POST /api/orders`, alineando el endpoint a consola de empleados. `pronto-tests/tests/functionality/unit/test_order_state_machine_v2.py` se actualizó a fixtures/modelos canónicos y reactivó la validación de cancelación cliente antes de `preparing`. Validación: `PYTHONPATH=src ../pronto-libs/.venv/bin/python -m pytest tests/unit/test_order_state_authority_regressions.py -q` en `pronto-libs` (`2 passed`) y `PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/unit/test_order_state_machine_v2.py -q` en `pronto-tests` (`8 passed`). |

2026-03-08 | 1.0450 | 1.0451 | Augment Agent | pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se reactivó la cobertura de `order_modification_service`: `pronto-tests/tests/functionality/unit/test_order_modification_service.py` dejó atrás placeholders/skips obsoletos y ahora valida creación autoaplicada por cliente, flujo pendiente por waiter, aprobación por cliente y rechazo por falta de permisos usando el runtime actual de `order_modification_service_core` y eventos realtime mockeados. Validación: `PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/unit/test_order_modification_service.py -q` en `pronto-tests` (`5 passed`). |

2026-03-08 | 1.0451 | 1.0452 | Augment Agent | pronto-api, pronto-libs, pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se cerró el batch customer self-service para dividir cuentas desde la API canónica: `pronto_shared.models.business_models` incorporó relaciones ORM faltantes para `SplitBill`, `SplitBillAssignment` y `SplitBillPerson`; `pronto_shared.services.split_bill_service_core` quedó alineado al runtime actual; `pronto-api/src/api_app/routes/customers/split_bills.py` añadió endpoints autenticados para crear/consultar/asignar/calcular/pagar divisiones de cuenta y `waiter_calls.py` migró las llamadas al mesero al API canónico bajo `customers/__init__.py`, con guardas explícitas para evitar `customer_uuid=None` y pre-commit completo en verde. `pronto-tests/tests/functionality/integration/test_split_bill.py` se actualizó para cubrir el flujo equal split con el contrato real. Validación: `PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/integration/test_split_bill.py -q` en `pronto-tests` (`2 passed`), `py_compile` de `split_bills.py`/`waiter_calls.py` OK y smoke de import de blueprints OK. |

2026-03-08 | 1.0452 | 1.0453 | Augment Agent | pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se dejó coherente el harness de pruebas con los contratos ya cerrados: `pronto-tests/conftest.py` ahora exige la base aislada `pronto_test`, valida columnas mínimas del schema, usa `setdefault` para no pisar entorno y actualiza fixtures al login canónico `/api/auth/login` con `X-App-Context`; además añadió `sample_subcategory` y completó `sample_menu_item`/`sample_table` para el schema actual. `tests/functionality/api/api-tests/test_jwt_refresh.py` y `tests/functionality/integration/test_employee_auth.py` migraron al contrato namespaced actual; `scripts/test_multi_console_auth.py` quedó explícitamente marcado como smoke manual (`pytest.skip`) para no contaminar runs automáticos. Validación: `PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/api/api-tests/test_jwt_refresh.py -q` (`11 passed`), `... pytest tests/functionality/integration/test_employee_auth.py -q` (`17 passed`) y `... pytest tests/functionality/unit/test_menu_validation.py -q` (`18 passed`). |

2026-03-08 | 1.0453 | 1.0454 | Augment Agent | pronto-api, pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se cerró el batch de autenticación/sesiones de cliente sin mezclar `invoices`, `public_business_info` ni `webhooks`: `pronto-api/src/api_app/routes/client_auth.py` ahora expone `/api/client-auth/login|register|logout` con `customer_ref` en Redis, `client_sessions.py` calcula el flag `Secure` de la cookie por request/proxy cuando `COOKIE_SECURE` no está forzado y `config.py` exige sesión de cliente para `store_cancel_reason` y `client_session_validation_interval_minutes`, además de restringir `/api/config/public` a admin/system. Se añadió `pronto-api/tests/test_security_regressions.py` con cobertura local para customer auth, gating de config y cookie security, y `pronto-tests/tests/functionality/ui/playwright-tests/api-tests/client-jwt-auth.spec.ts` dejó de esperar `404` en `/api/client-auth/register` para validar el contrato real con CSRF. Validación: `cd pronto-api && .venv/bin/python -m pytest tests/test_security_regressions.py -q` (`26 passed`), `cd pronto-api && .venv/bin/python -m ruff check tests/test_security_regressions.py src/api_app/routes/client_auth.py src/api_app/routes/client_sessions.py src/api_app/routes/config.py` (`All checks passed`), `cd pronto-tests && npx playwright test tests/functionality/ui/playwright-tests/api-tests/client-jwt-auth.spec.ts` (`4 passed`) y smoke live cliente→API con CSRF para register/login/config/logout OK. |

2026-03-08 | 1.0454 | 1.0455 | Augment Agent | pronto-api, pronto-scripts/pronto-root, pronto-docs | Se cerró el batch de facturación sin arrastrar cambios de dependencias: `pronto-api/src/api_app/routes/invoices.py` ahora soporta rutas de cliente y admin con resolución explícita de actor (`customer_ref` o JWT empleado), validación de scope para `cashier|admin|system`, filtros/listados/descargas/cancelación y emisión usando el servicio canónico de facturación, mientras `src/api_app/routes/__init__.py` quedó consistente con los lotes previos registrando `customers_root_bp`, `customers_bp`, `invoice_bp` y `admin_invoice_bp` sin meter `webhooks` ni `public_business_info`. Validación: `cd pronto-api && .venv/bin/python -m pytest tests/test_security_regressions.py -q` (`26 passed`), `cd pronto-api && .venv/bin/python -m ruff check src/api_app/routes/__init__.py src/api_app/routes/invoices.py` (`All checks passed`), `cd pronto-tests && PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/integration/test_split_bill.py -q` (`2 passed`) y pre-commit completo verde en el commit `feat(api): secure customer and admin invoice routes`. |

2026-03-08 | 1.0455 | 1.0456 | Augment Agent | pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se cerró un sublote test-only para auth/sessions sin tocar servicios productivos: `tests/functionality/integration/test_session_management.py` se reescribió contra contratos vigentes (`/api/auth/login` con `X-App-Context`, merge/move mocked sobre `dining_session_service`, y gate `waiter_can_collect`), `tests/functionality/ui/playwright-tests/employees/auth.spec.ts` dejó de asumir redirects rígidos por scope y acepta el patrón de dashboard actual, y se eliminaron cuatro suites auth legacy duplicadas/obsoletas (`test_auth_api.py`, `test_employee_auth.py`, `test_jwt_roles.py`, `test_jwt_scope_guard.py`) ya reemplazadas por cobertura vigente. Validación: `cd pronto-tests && PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/integration/test_session_management.py -q` (`6 passed`), `... pytest tests/functionality/integration/test_employee_auth.py tests/functionality/api/api-tests/test_jwt_refresh.py -q` (`28 passed`) y `cd pronto-tests && ADMIN_EMAIL=admin@cafeteria.test WAITER_EMAIL=juan.mesero@cafeteria.test EMPLOYEE_PASSWORD='ChangeMe!123' npx playwright test tests/functionality/ui/playwright-tests/employees/auth.spec.ts` (`3 passed`). |

2026-03-08 | 1.0456 | 1.0457 | Augment Agent | pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se reactivó la cobertura unitaria de `BusinessInfoService` sin depender de un `.env` mutable del host: `tests/functionality/unit/test_services.py` dejó atrás skips frágiles y ahora usa un `general.env` temporal, monkeypatch de `business_info_service_core.get_session/get_config_value/set_config_value` y validación real de persistencia de `BusinessInfo`/actualización del nombre comercial. Validación: `cd pronto-tests && PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/unit/test_services.py -q` (`6 passed`). |

2026-03-08 | 1.0457 | 1.0458 | Augment Agent | pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se alinearon dos E2E live al contrato actual sin tocar servicios productivos: `tests/functionality/e2e/test_config_settings_roundtrip_live.py` ahora usa credenciales/configuraciones `PRONTO_LIVE_*`, verifica existencia de cuentas activas antes de correr y consume `GET :6082/api/public/config` sin scope SSR legado; `tests/functionality/e2e/test_v6_full_cycle.py` migra el flujo cliente a `CLIENT_URL` (`/api/client-auth/register`, `/api/sessions/table-context`, `/api/sessions/open`, `/api/customer/orders`, `/api/customer/orders/session/<id>/request-check`), obtiene CSRF desde SSR cliente y selecciona un item válido desde la DB live para evitar falsos rojos por modificadores requeridos. Validación: `cd pronto-api && .venv/bin/python -m ruff check ../pronto-tests/tests/functionality/e2e/test_v6_full_cycle.py ../pronto-tests/tests/functionality/e2e/test_config_settings_roundtrip_live.py` (`All checks passed`), `cd pronto-tests && PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python -m pytest tests/functionality/e2e/test_config_settings_roundtrip_live.py -q` (`3 skipped`, skip limpio por credenciales/cuentas live ausentes cuando aplica) y `cd pronto-tests && PYTHONPATH=.:../pronto-api/src:../pronto-libs/src ../pronto-libs/.venv/bin/python tests/functionality/e2e/test_v6_full_cycle.py` (flujo principal completo OK; ticket PDF live quedó como warning no bloqueante con `500`). |

2026-03-08 | 1.0458 | 1.0459 | Augment Agent | pronto-tests, pronto-scripts/pronto-root, pronto-docs | Se corrigieron los scripts de diagnóstico live para apuntar a los health endpoints canónicos del cliente y empleados sin prefijo `/api`: `scripts/diagnostic/api/backend_diagnostic.py` ahora usa `http://localhost:6080/health` y `http://localhost:6081/health`, mientras `scripts/diagnostic/ui/frontend_diagnostic.py` hace lo mismo en su check de salud; además ambos scripts quedaron alineados al lint moderno (`dict | None`, `str | None`, eliminación de imports `typing` legacy y variables no usadas). Validación: `curl http://localhost:6080/health`/`http://localhost:6081/health` (`200/200`), `cd pronto-api && .venv/bin/python -m ruff check ../pronto-tests/scripts/diagnostic/api/backend_diagnostic.py ../pronto-tests/scripts/diagnostic/ui/frontend_diagnostic.py` (`All checks passed`) y `cd pronto-tests && ../pronto-libs/.venv/bin/python -m py_compile scripts/diagnostic/api/backend_diagnostic.py scripts/diagnostic/ui/frontend_diagnostic.py` OK. |
