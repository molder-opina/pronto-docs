## CSRF en `pronto-docs`

- `pronto-docs` no expone endpoints browser mutantes del producto.
- La documentación de CSRF canónica vive en los contratos de `pronto-api`, `pronto-client` y `pronto-employees`.
