## Redis keys en `pronto-employees`

`pronto-employees` no define namespace Redis canónico propio; si el runtime usa Redis, lo hace a través de servicios compartidos o backend, no como owner independiente de claves documentadas aquí.

| Key Pattern | Owner | Notes |
|---|---|---|
| n/a | n/a | Sin claves propias de esta superficie SSR/proxy |
