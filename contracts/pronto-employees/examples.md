## Ejemplos mínimos para `pronto-employees`

### Login admin
`POST /admin/login`

Body:
`{"email":"admin@pronto.local","password":"***"}`

### Orders vía proxy waiter
`GET /waiter/api/orders`

### KPIs vía proxy admin
`GET /admin/api/reports/kpis`

### Branding vía proxy admin
`GET /admin/api/branding/config`